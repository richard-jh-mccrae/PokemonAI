# Consolidated Python deps for the whole repo (runtime + tests).
# Direct requirements only; transitive deps (numpy, pandas, requests, ...) pull in
# automatically via the packages below. Mirrors the per-tool files:
#   tools/sim/requirements.txt, tools/meta_tracker/requirements.txt
# Native `cg` engine (ctypes) ships in src/cg — no pip install needed.

# --- Simulator harness (tools/sim) ---
kaggle-environments==1.30.1   # cabt env ships inside it; hard-pinned to ladder — see ADR-0010

# --- Meta tracker (tools/meta_tracker) ---
plotly>=6.0                   # self-contained HTML dashboard (JS inlined)
kaggle>=2.2                   # CLI: leaderboard / episodes / replay download (fetch.py)

# --- Tests (python -m pytest tests/ -q) ---
pytest>=8.0
pytest-cov>=5.0               # --cov coverage gate (.coveragerc fail_under=80)
