<!-- Strategy Proposal queue — blunder-buster round 2026-07-13 (build 2d2a113; TWO agents: mega_lucario +
dragapult_ex). User narrowed scope to data/corrections/mega_lucario_20260713_2d2a113 +
data/corrections/dragapult_ex_20260713_2d2a113-dirty. Contract:
.claude/skills/update-strategy/references/strategy_proposal_contract.md

Worklist = 15 open corrections across 4 games:
  mega_lucario ep 85785067 (f14 CRIT missed_win, f41 bad_target [tactical-SKIP, triaged], f42/f54 slow_setup)
               ep 85785606 (f1 CRIT misattachment, f19 wrong_supporter, f21 CRIT wrong_supporter)
  dragapult_ex ep 85785609 (f4 wasted_resource, f22 misattachment, t8 slow_setup [turn])
               ep 85786096 (t2 CRIT slow_setup [turn], f24 CRIT slow_setup, f38 slow_setup, f70 CRIT slow_setup, f86 CRIT slow_setup)

TERMINAL OUTCOMES — 15 proposal-routed (11 clusters below), 0 deferred:
  proposal-routed 15: every decision retest_one FIXED=False (still-open) / turn planned=null throughout; none
    forgoes a KO or a high-value attack (all are setup/tempo frames). Routing evidence = real-Pilot retests
    (tools/train/retest_one.py) + card/rule facts read at SOURCE (data/EN_Card_Data.csv, docs/rules.md,
    src/common). Card facts pinned in each spec. Fixtures: tests/fixtures/corrections/*.json (this round).
  dp 85786096-t2 (CRITICAL) — the retreat-to-item-lock maneuver, SUPPORT-EX-PIVOT variant — was first routed
    capability-gap and DEFERRED (human gate, 2026-07-13) because its dependency (the offensive
    `disruptor_lock_maneuver` build) lived on claude/cggame-parity-handoff-425a45, UNMERGED here. On the
    user's instruction this branch was REBASED onto main (which merged PR #92 / commit 4e38243), so
    disruptor_lock_maneuver is now IN-TREE + default-ON. Re-probed against the merged code: t2 STILL blunders
    (decide()=[1], FIXED=False) — its gate `_can_lock_line_with_disruptor` excludes a support-ex pivot Active.
    With the merge-blocker gone the user GRADUATED it to an open planner-code proposal (below, the 11th
    cluster). cgpy lethal_verify is win-only and irrelevant to this tempo line.

CRITICAL cohort (7): f14, f1, f21 (ml) + t2, f24, f70, f86 (dp). ALL SEVEN route to proposal-routed (t2
  graduated from capability-gap after the rebase unblocked it). NOTE the CRITICAL flag is a MUST-FIX marker —
  update-strategy should drain the CRITICAL proposals (go-down-swinging, dont-switch, energy-color-routing,
  stall-gust-accel, risky-ruins, disruptor-lock-from-a-support-ex-pivot) first.

CROSS-DECK: all general-hypothesis clusters are authored `for: general` (deck-agnostic mechanism) even where
  provenance is single-deck; the two deck-strategy clusters (ml lunar-cycle, dp risky-ruins) stay in-deck. No
  cluster pools members across BOTH decks this round (the general fixes touch different rules per deck). -->


<!-- ============================== mega_lucario ============================== -->

## go-down-swinging: arm the doomed active that can still attack this turn
- id: go-down-swinging-arm-the-doomed-active
- source: blunder-buster
- target_layer: general-hypothesis
- candidate_signal: needs a new per-option signal `attach_enables_active_attack` (this ATTACH crosses the Active from can't-pay to can-pay a *worthwhile* / KO attack THIS turn) — model on the EXISTING `attach_completes_biggest_attack` (pilot.py:2358) but for the CHEAPEST worthwhile attack, NOT wincon-Role-gated (so it covers Solrock = secondary_attacker+engine). Plus existing `Board.active_attack_payable` + `active_doomed`.
- verification_contract: verifier
- provenance: correction 85785606:f21 (CRITICAL) + 85785606:f19 + 85785606:f1 (CRITICAL, degenerate proxy) [mega_lucario] | fixtures tests/fixtures/corrections/ml_arm_doomed_solrock_f21.json (PRIMARY gate) + ml_solrock_attack_over_stallgust_f19.json + ml_start_solrock_proxy_f1.json (proxy → re-point to f21) | prior art must-not-regress: dont-feed-the-doomed's genuine-famine stall (no accel/attack available → must still fire)
- status: applied
- resolution (2026-07-14, update-strategy batch 2): APPLIED. New unifying Board signal `active_arm_available` (pilot.py: doomed-irrelevant — a real ATTACKER, not a utility body, whose biggest attack one more Energy COMPLETES, and no ready benched wincon to retreat into). Three coordinated edits: (1) new `arm-the-doomed-active` rung (+20, baseline_energy.py) on `_ATTACH→_ACTIVE and active_doomed and active_arm_available`; (2) `dont-feed-the-doomed` stands down on `active_arm_available` (its go-down-swinging exemption); (3) `gust-for-the-stall` gated on `not (my_active_energy==0 and active_attack_payable)` (an UNARMED-but-armable Active attacks, an already-armed one may still stall). retest f21 [3]→[0] + f19 [0]→[1] FIXED; f1 covered (proxy). `active_arm_available` (completes-cost-1 Solrock, not cost-2 Makuhita, not utility Lunatone, not when bench-wincon-ready) keeps the lethal-recover / feed-accel / gust suite green. Whole suite green (2766 pass; 1 unrelated flaky card-effects test passes in isolation).
- for: general

**Spec (authoring spec — thin fodder):**
Card facts (data/EN_Card_Data.csv): **Solrock** id676 (Basic {F}, 110 HP) — Cosmic Beam {F} = **70**, "does
nothing if there is no Lunatone on your Bench," and it **ignores Weakness/Resistance**. **Lunatone** id675
(the benched draw engine). **Riolu** id677 → **Mega Lucario ex** id678 (Aura Jab {F}/130, Mega Brave {F}{F}/270)
— a single hop, un-evolvable this turn without a Mega Lucario ex in hand.

The doomed-active family abandons an Active that can still pay a worthwhile attack THIS turn. All three
frames are the mega_lucario **Solrock opener** going idle. **f21 (CRITICAL, turn 3):** attaching the lone {F}
to the Active Solrock arms Cosmic Beam 70 (Lunatone is benched, so it's live); instead the Pilot banks the
{F} on a **benched Riolu** it can't evolve this turn — `concentrate-energy-on-wincon` (+25) pumps a pre-evo
that produces no attack, while `dont-feed-the-doomed` (−30) **penalizes** the Active Solrock, so bench-Riolu
+20 beats active-Solrock −20 (correct=[0]). **f19 (same turn):** the Pilot plays a Boss's-Orders stall-gust
(+10) and banks the {F} on the bench Riolu, never swinging Solrock (correct=[1] = attach→Solrock).

Root defect (retest-confirmed): the doomed-active family has no "go down swinging" exemption. Three
coordinated general-layer edits:
1. **`dont-feed-the-doomed`** (baseline_energy.py:128/129, −30): add `and not c.attach_enables_active_attack`
   — the exact stand-down its wincon sibling `dont-overbuild-the-doomed-wincon` (:195) already grants via
   `attach_completes_biggest_attack`, making the doomed-active family symmetric across wincon and off-Line
   bodies.
2. **NEW positive rung** `arm-the-doomed-active` (baseline_energy.py): fires on an ATTACH to the ACTIVE that
   crosses it from can't-pay to can-pay a worthwhile attack this turn, weighted ~+20 to clear
   `concentrate-energy-on-wincon` (+25) — mirror `feed-the-firing-accelerator` (+35, which already beats
   concentrate + stands dont-feed-the-doomed down). Standing the −30 down alone leaves Solrock ~+4 < Riolu +17,
   so the positive rung is required.
3. **`gust-for-the-stall`** (doctrine_gust.py, +10): add gate `and not c.board.active_attack_payable` — align
   the mild stall-gust with its famine sibling `stall-gust-over-dev-when-starved` (+95) which already carries it,
   so a payable attack this turn is never skipped for a stall-gust (fixes f19).

WHY it wins: 70 real damage banked before Solrock dies beats banking a 2nd energy on a bench pre-evo that
can't attack; `active_doomed` is only a worst-case forward read (opp Riolu → Mega Lucario ex OHKO next turn)
and must not veto a payable attack now. `attach_completes_biggest_attack` is already True for Solrock (single
attack, cost 1, 0→1). f1 is a degenerate proxy (chosen==correct==start-Solrock-active, W-route-blind) — its
re-measure gate re-points to the f21 attach board. SAFETY: the genuine-famine stall (no accel and no payable
attack anywhere) still fires — the signal is False there.

---

## don't switch off an energized Active for no gain (setup)
- id: dont-switch-off-the-energized-active-in-setup
- source: blunder-buster
- target_layer: general-hypothesis
- candidate_signal: `Board.my_active_energy` + the existing motive guards `active_doomed` / `bench_wincon_ready` / `active_best_attack_locked` — no new infra; the fix is a `when()`-clause REMOVAL on the EXISTING `dont-play-switch-for-no-gain` (baseline_retreat.py:62).
- verification_contract: verifier
- provenance: correction 85785067:f14 (CRITICAL, mega_lucario) | fixture tests/fixtures/corrections/ml_dont_switch_off_energized_active_f14.json | regression guard: the covered ml f30 case (unpowered-Riolu-for-unpowered-Riolu) must still fire
- status: applied
- resolution (2026-07-14, update-strategy batch 1): APPLIED. Dropped the `and c.board.my_active_energy == 0` conjunct from `dont-play-switch-for-no-gain` (baseline_retreat.py), keeping the three motive guards; rationale reworded to "no sanctioned switch motive". retest 85785067-14 [0] Switch → [2] End FIXED (Switch nets −8 < End 0). f30 regression + whole suite green (2765+registry pass).
- for: general

**Spec (authoring spec — thin fodder):**
`dont-play-switch-for-no-gain` (baseline_retreat.py:62, −8) carries a `board.my_active_energy == 0` clause,
so it stands down when the Active has energy — **precisely when a no-gain Switch is worst.** **f14
(CRITICAL, turn 1):** the ideal opener is on board — energized **Solrock** Active + **Lunatone** benched — and
the agent plays **Switch** (score 0), benching the energized attacker and promoting an unpowered Lunatone,
which ALSO breaks Cosmic Beam's Lunatone-on-Bench requirement. Switch [0] and End [2] tie at 0; the
option-index tie-break took the wasteful Switch (the guard was suppressed by the energy clause).

**Author:** DROP the `my_active_energy == 0` clause — a no-gain switch is wasteful regardless of active
energy (more so when energized). Keep the three motive guards intact (`not active_doomed`,
`not bench_wincon_ready`, `not active_best_attack_locked`): they already protect every sanctioned switch
motive (escape a doomed Active; promote a ready benched wincon via `retreat-to-ready-attacker`; swap a
transient-locked nuke via `swap-out-the-locked-attacker`). With the clause dropped, −8 nets Switch below End
(0) → End wins → FIXED. The rationale should read "no sanctioned switch motive" rather than "bare Active."
Regression: the existing covered ml f30 case (unpowered Riolu for unpowered Riolu) still fires. Card facts:
Solrock id676 Cosmic Beam {F}/70 needs benched Lunatone; Lunatone id675 Lunar Cycle needs Solrock in play.

---

## Lunar Cycle refuels a dead hand (support / non-wincon Active)
- id: lunar-cycle-refuel-the-dead-hand
- source: blunder-buster
- target_layer: deck-strategy
- candidate_signal: `Board.my_hand_size` (pilot.py:309) + engine-online test (LUNATONE & SOLROCK in in_play_ids) + `active_is_weak_preevo` (pilot.py:2773) — all exist; optionally name a helper `hand_dead_but_engine_online` (authoring work, not a capability-gap).
- verification_contract: verifier
- provenance: correction 85785067:f42 + 85785067:f54 [mega_lucario] | fixtures tests/fixtures/corrections/ml_lunar_cycle_dead_hand_f42.json + ml_lunar_cycle_dead_hand_support_active_f54.json | sibling prior art must-not-regress: ml_lunar_cycle_over_last_f_attach_f16.json (the weak-preevo case the pair was built for)
- status: applied
- resolution (2026-07-14, update-strategy batch 2): APPLIED. Broadened BOTH mega_lucario Lunar-Cycle rules (strategy.py) — the guard `dont-lunar-cycle-away-the-last-attachable-f` (−30) stands down, and the positive `lunar-cycle-the-weak-preevo-last-f` (+20→+30) fires, on `active_is_weak_preevo OR (my_hand_size ≤ 1 AND (not active_attack_payable OR active_doomed) AND not active_arm_available)` — a dead-hand famine, EXCEPT when the Active is worth arming (yields to `arm-the-doomed-active` via the shared `active_arm_available` signal, so f21 Solrock attacks while f42 Makuhita / f54 Lunatone draw). retest f42 [2]→[4] + f54 [2]→[4] FIXED; f16 weak-preevo case unchanged. Whole suite green.
- for: deck:mega_lucario

**Spec (authoring spec — thin fodder):**
The last-{F} Lunar-Cycle pair — guard `dont-lunar-cycle-away-the-last-attachable-f` (strategy.py:289, −30) +
positive `lunar-cycle-the-weak-preevo-last-f` (strategy.py:309, +20) — only stands down / fires when
`active_is_weak_preevo` (Active is Riolu/Makuhita). It was built for ml 85058574 f16 (active = Riolu) and
never generalized. In BOTH **f42** (active = doomed **Makuhita**, wincon Mega Lucario ex BENCHED at 0e) and
**f54** (active = **Lunatone**, a support body) the hand is a lone Basic {F} (dead hand) and the engine is
ONLINE (Solrock + Lunatone in play), so the native draw-3 is legal — but the guard keeps −27 and
`concentrate-energy-on-wincon` (+25) wins the attach onto the benched Mega (score 29).

**Author:** broaden BOTH the guard's stand-down AND the positive rung to also trigger on a **dead-hand
famine**: engine online AND `my_hand_size <= 1` AND the lone {F} attach can't become this-turn damage (the
only attach target is a benched body / a non-{F}-ready support Active — never a genuinely {F}-ready attacker
whose attack the attach enables now). The positive rung must clear the competing +25 attach onto the
fully-evolved benched Mega (weight ~+30, higher than the weak-preevo +20 whose competitor was only a
Riolu-active). Ladder-tune. WHY it wins: at turn-3/5 dead-hand setup, draw 3 dominates sinking the only card
into a premature bench attach; the discarded {F} is NOT lost — **Mega Lucario ex** id678 **Aura Jab** ({F}/130)
re-attaches up to 3 Basic {F} from the discard pile to the Bench, so it is deferred fuel. Card facts:
**Lunatone** id675 Lunar Cycle = once/turn, if Solrock in play, discard a Basic {F} from hand → draw 3.
`active_is_weak_preevo` (pilot.py:2773) excludes Makuhita/Lunatone by design.

---

## defensive stall-gust target picker is keystone/ex-blind
- id: gust-stall-target-keystone-aware
- source: blunder-buster
- target_layer: general-hypothesis
- candidate_signal: `CardStat.is_ex_body` (the 2-prize-ex fact) + the existing `_stall_target_is_keystone` helper the PLAY-side `gust-to-strand-the-key-attacker` already uses. No new infra (a Tactical term in `_gust_stall_target_tactical`).
- verification_contract: verifier
- provenance: correction 85785067:f41 [mega_lucario; this-round tactical SKIP, triaged to proposal-routed] | fixture tests/fixtures/corrections/ml_gust_stall_strand_the_ex_f41.json
- status: applied
- resolution (2026-07-14, update-strategy batch 1): APPLIED. Added `_STALL_EX_BONUS = 3` and made `_gust_stall_target_tactical` (doctrine_gust.py) return `retreatCost + (3 if stat.is_ex_body else 0)`. retest 85785067-41 [0] opp Riolu → [2] opp Meowth ex FIXED (2-prize keystone ex strand). Whole suite green.
- for: general

**Spec (authoring spec — thin fodder):**
`GustMixin._gust_stall_target_tactical` (doctrine_gust.py:158-174) scores each energyless opponent-bench gust
target by `stat.retreatCost` ALONE — blind to the keystone/ex/prize dimension the PLAY-side already encodes via
`_stall_target_is_keystone` + `gust-to-strand-the-key-attacker` (+20). **f41 (turn 3):** our Active is a dying
Makuhita (10/80) so `_gust_target_tactical` = 0 (no offensive gust) and retreat-cost is the sole governing
score — it drags up a retreat-2 **Riolu** (1-prize pre-evo, the opp has a duplicate benched) over a retreat-1
**Meowth ex** (2-prize ex, weak {F} to our Fighting deck, can't attack without 3 Energy).

**Author:** add a keystone bonus in `_gust_stall_target_tactical` when the strandable energyless target
`is_ex_body` (mirror `_stall_target_is_keystone`), large enough to overcome a small retreat-cost gap
(retreat costs are 1-2 here; +3 makes Meowth ex 1+3=4 beat Riolu 2), kept **<< KO_SCORE** so a real gust-KO
target always outranks a stall pick. WHY it wins: aligns the target picker with the play-side doctrine — strand
the 2-prize keystone ex (a win-condition-class body that burns a full turn to retreat and sets up a future
2-prize Fighting OHKO) over a fungible 1-prize pre-evo. Card facts: **Meowth ex** id1071 (Basic ex, 170 HP,
{C}, Weakness {F}, Retreat 1, Tuck Tail ●●●/60); **Riolu** id677 (Basic, 80 HP, Retreat 2).

SECONDARY LEAK (source-confirmed, not outcome-changing at f41 but fix it in the same change): the promote
cluster's `_SWITCH`-gated rules mis-fire on OPPONENT gust targets because `_SWITCH` is overloaded (own retreat
OR opp gust). Here `dont-promote-onto-their-path` (baseline_promote.py:10) penalized opp Lunatone/Makuhita by
−8 with our-promote semantics. Owner-guard the promote SWITCH rules to own-side only
(`option.playerIndex == yourIndex`), the same guard the gust scorers use.


<!-- ============================== dragapult_ex ============================== -->

## route energy by the recipient's color (attack-cost UNION ability-fuel)
- id: energy-color-to-recipient-routing
- source: blunder-buster
- target_layer: general-hypothesis
- candidate_signal: needs a new signal — the per-in-play-body REQUIRED energy colors = attack-cost `energyTypes` UNION **ability-fuel colors** (parsed from ability clauses like "if this Pokémon has any {X} Energy attached"), exposed both as an in-play set (for the ToHand fetch) and per-recipient (for the AttachTo match), + the per-option attached-energy `EnergyType` (CardStat.energyType at the option's area index).
- verification_contract: verifier
- provenance: correction 85785609:f22 + 85786096:f86 (CRITICAL) [dragapult_ex] | fixtures tests/fixtures/corrections/dp_fetch_the_ability_color_f22.json + dp_attach_the_recipients_color_f86.json
- status: applied
- resolution (2026-07-14, update-strategy follow-up): APPLIED. New `CardStat.abilityEnergyTypes` (`parse_card_ability_energy` in card_text.py parses "if this Pokémon has any {X} Energy attached" → type codes; Munkidori Adrena-Brain → (7,), no false positives). Two Board signals (pilot.py): `in_play_required_colors` = attack colors ∪ ability-fuel colors of my in-play bodies; `in_play_unfueled_ability_colors` = fuel colors of in-play bodies that LACK that colour attached. Two rungs (doctrine_fetch.py): **`fetch-the-ability-fuel-color`** (+5, TO_HAND — out-ranks the +3 attack-colour tie-break: grab {D} for a bare Munkidori over a redundant in-hand {R}) and **`attach-off-color-at-fixed-recipient`** (−8, ATTACH_TO). Key finding: at ATTACH_TO (ctx 22) the option carries no `inPlayArea`/`inPlayIndex`, so the fixed recipient is NOT identifiable per-option — the sound design is a board-union demote (an energy no in-play body can use is wasted regardless of recipient), not a per-recipient match. Added `_ATTACH_TO`/`_MOVE_CARD` to context.py. retest f22 [0]→[3] {D} FIXED; f86 (CRITICAL) [0]→[1] {P} FIXED; f18/f21 anchors green. Whole suite 2775 green. Tests: tests/strategy/test_blunder_20260713_routing.py.
- for: general

**Spec (authoring spec — thin fodder):**
The Pilot routes energy by COLOR only at the ToHand fetch, and even there only credits ATTACK-cost colors —
so it dumps off-color energy the recipient can't use. Two coordinated touch-points on ONE new signal (the
recipient's required colors = attack-cost UNION ability-fuel):

- **(a) ToHand fetch (f22):** `fetch-the-attack-color` (doctrine_fetch.py:377, +3) credits a fetched Basic
  Energy only if its type ∈ `in_play_attack_colors` (pilot.py:2266). **Munkidori** id112 is in play; its
  ATTACK is Mind Bend {P}, but its ABILITY **Adrena-Brain** needs **{D}** attached to move ≤3 damage
  counters. {D} is never an attack cost in this deck, so the six {R}/{P} copies score +3 and the {D} that
  turns the ability ON scores 0 — the agent grabbed a redundant {R} (already in hand). Extend the fetch
  credit to **ability-fuel colors**, ranked ABOVE an attack color whose line isn't online yet AND above a
  color already in hand, so {D} beats {R} here (Munkidori's damage-move is usable now; the {R}{P} dragon
  line needs two evolutions — only base Dreepy in play).
- **(b) AttachTo (f86, CRITICAL):** at `select_context == ATTACH_TO` the recipient is FIXED and EVERY energy
  option scores **0** (no rule engages color) — the pick is arbitrary and it dumped {D} onto the dragon line.
  Add a rung that ranks the attached energy by whether its color ∈ the FIXED recipient's required colors
  (attack-cost UNION ability-fuel); demote (~−8) an off-color energy the recipient can use for neither,
  mirroring the +3 tie-break of the ToHand rule.

SOUNDNESS GUARD (do not regress f22): {D}→Munkidori is CORRECT (ability fuel), so the off-color test MUST
credit ability-fuel colors, not only attack costs — Munkidori's attack (Mind Bend {P}) is {P}, so an
attack-cost-only signal would miss the {D} need entirely. Card facts (EN_Card_Data.csv): dragon line all need
Fire+Psychic and zero Darkness — **Dreepy** id119 Bite {R}{P}, **Drakloak** id120 Dragon Headbutt {R}{P},
**Dragapult ex** id121 Phantom Dive {R}{P} (Jet Headbutt {C}); **Munkidori** id112 Adrena-Brain needs {D},
Mind Bend {P}. Energy codes FIRE=2 / PSYCHIC=5 / DARKNESS=7; deck pool 4 Psychic / 3 Fire / 2 Darkness
(Darkness exists solely to fuel Munkidori).

---

## famine stall-gust must stand down when an accelerator can power the Active
- id: stall-gust-stand-down-when-accel-can-power-the-active
- source: blunder-buster
- target_layer: general-hypothesis
- candidate_signal: needs a new signal `active_attack_payable_via_accel: bool` on Board — True when the hand holds a PLAYABLE `energy_accel`/`tutor_energy` card (Crispin id1198) whose fetch-and-attach-from-deck raises the Active's payable units to >= its minAttackCost this turn (attached + accel-attach >= minAttackCost).
- verification_contract: verifier
- provenance: correction 85786096:f70 (CRITICAL, dragapult_ex) | fixture tests/fixtures/corrections/dp_stall_gust_false_famine_accel_f70.json | must-not-regress: the genuine-famine anchor (no accel card in hand → signal False → stall-gust still fires)
- status: applied
- resolution (2026-07-14, update-strategy batch 2): APPLIED. New Board signal `active_attack_payable_via_accel` (pilot.py: a PLAYABLE hand `tutor_energy` card — Crispin — whose deck-fetch-and-attach raises the Active to attack-payable this turn; fail-closed, kept separate from `active_attack_payable`). Added `and not active_attack_payable_via_accel` to the three famine-gust rungs (`stall-gust-over-dev-when-starved`, `gust-for-the-stall`, `gust-to-strand-the-key-attacker`, doctrine_gust.py). retest f70 [1] Boss's-Orders→[2] Crispin FIXED. Genuine-famine (no accel) anchor still stalls. Whole suite green.
- for: general

**Spec (authoring spec — thin fodder):**
The energy-FAMINE stall-gust family fires off `_active_attack_payable` (pilot.py:3617), which computes
`payable = attached + best_hand_attach` (pilot.py:2550) and **IGNORES deck-fetchable accel energy** — a FALSE
famine. **f70 (CRITICAL, turn 8):** the Active is our WINCON **Dragapult ex** (id121, 320 HP, 0 energy) and
**Crispin** (id1198, `energy_accel`/`tutor_energy`) is in hand — it fetches a Basic Energy from the deck and
attaches it, powering **Jet Headbutt** (● = 1 colorless) THIS turn (and banks a 2nd Energy for benched
Drakloak). But `stall-gust-over-dev-when-starved` (doctrine_gust.py, +95, dragapult tuned 92) reads
`not active_attack_payable` = True and buries Crispin (+55) under Boss's-Orders (+105).

**Author:** add `active_attack_payable_via_accel` and gate the famine family — `stall-gust-over-dev-when-starved`,
plus the co-firing `gust-for-the-stall` (+10) and `gust-to-strand-the-key-attacker` (+20) — with
`and not c.board.active_attack_payable_via_accel`. WHY it wins: powering the wincon to attack advances the win
AND attacks this turn; a defensive durdle stall-gust leaves the wincon energyless and doomed. SAFETY: at the
genuine-famine anchor (zero Energy anywhere, no accel card) the signal is False and stall-gust still fires —
no regression. Card facts: Dragapult ex id121 Jet Headbutt ●/70 + Phantom Dive {R}{P}/200; Crispin id1198
(fetch 2 Basic Energy of different types, attach 1); Drakloak id120 {R}{P}/70.

---

## don't pre-bench a redundant standalone-utility Basic (setup)
- id: dont-pre-bench-a-redundant-utility
- source: blunder-buster
- target_layer: general-hypothesis
- candidate_signal: `card_is_redundant` (pilot.py = cid in board.in_play_ids) + `card_is_line_preevo` — both exist, no new infra.
- verification_contract: verifier (decline-test gate — mirror tests/strategy/test_setup_bench_decline.py — plus score-neutrality, ADR-0034)
- provenance: correction 85785609:f4 (dragapult_ex) | fixture tests/fixtures/corrections/dp_dont_pre_bench_redundant_munkidori_f4.json | mirrors mega_lucario `dont-bench-a-redundant-engine-piece` (_PLAY complement) + `dont-pre-bench-the-supporter-tutor` (_SETUP_BENCH sibling)
- status: applied
- resolution (2026-07-14, update-strategy follow-up): APPLIED. New Board signal `setup_placed_ids` (pilot.py `_setup_placed_ids`: card ids placed on my Active/Bench during the pregame, read from MOVE_CARD logs — the just-placed Active shows ONLY in logs, obs reads active=[None], so obs-zone `in_play_ids` misses it; scoped to turn 0). New rung **`dont-pre-bench-a-redundant-utility`** (−15, doctrine_fetch.py): `_SETUP_BENCH and card_id ∈ setup_placed_ids and not card_is_line_preevo` → `bench-fill-a-basic` +12 − 15 = −3 declines the optional bench (a 2nd Dreepy line-preevo stays exempt). Degenerate record (decline = take-fewer, no index — can't be W-route-FIXED), so gated by a decline-test per the contract: decide(f4)=[] declines; strip the pregame log → `setup_placed_ids` empty → the Basic is still benched (proves the fix rides the signal, not a blanket decline). Whole suite 2775 green.
- for: general

**Spec (authoring spec — thin fodder):**
NEW general negative rung in doctrine_fetch.py beside `dont-pre-bench-the-supporter-tutor`. At the PREGAME
bench placement (`_SETUP_BENCH`, minCount 0), DECLINE placing a Basic whose id is already in play
(`card_is_redundant`) UNLESS it is a wincon-line pre-evo you want multiples of (`not card_is_line_preevo`
guard). **f4:** the agent benches a 2nd **Munkidori** while one is already Active. Munkidori id112 is a
standalone damage-mover utility (NOT a line pre-evo, NOT a `supporter_tutor`), so the 2nd copy is a prize
liability + a scarce bench slot, worth more kept as **Ultra Ball** discard-fodder. Weight −15 (mirror the
sibling): `bench-fill-a-basic` (+12, doctrine_fetch.py:530) is the only rule firing; `_greedy_grab` (:294)
take-fewer declines when best score <= 0, so 12−15 = −3 declines the optional bench. The `not
card_is_line_preevo` guard keeps benching a 2nd **Dreepy** (a line pre-evo — you DO want multiple attackers).

WHY it wins: declining at SETUP_BENCH loses nothing (a 2nd copy can still be benched later from hand if the
board wants it) but preserves the discard-cost card for Ultra Ball. This is the `_SETUP_BENCH` generalization
(off tags) of mega_lucario's deck-specific `dont-bench-a-redundant-engine-piece` (−25, _PLAY). GATE NOTE: the
record is DEGENERATE (the engine enumerates only the bench option; "decline" is take-fewer trimming to empty,
not an index), so FIXED can't measure it — gate via a decline-test like `test_setup_bench_decline.py` on the
f4 fixture, plus score-neutrality. Card facts: Munkidori id112 (Basic, 110 HP, {P}, Adrena-Brain needs {D},
Mind Bend {P}); it is already the Active at this frame, so the hand copy is redundant.

---

## evolve the ENERGIZED line body first (WHICH-body tie-break)
- id: evolve-the-energized-line-body-first
- source: blunder-buster
- target_layer: general-hypothesis
- candidate_signal: `evolve_body_energy` + `card_is_line_preevo` (both exist, pilot.py) — no new signal.
- verification_contract: verifier
- provenance: correction 85785609:t8s0 (dragapult_ex; turn scope — `planned=null` throughout the span, so the Planner never committed → general-hypothesis, NOT planner-code) | fixture tests/fixtures/corrections/dp_evolve_energized_line_body_first_f82.json | mirrors the existing wincon-Role `evolve-the-energized-body-first` (+5, baseline_evolution.py)
- status: applied
- resolution (2026-07-14, update-strategy batch 1): APPLIED. New +5 rung `advance-the-energized-line-body-first` (baseline_evolution.py, when=`_EVOLVE and card_is_line_preevo and evolve_body_energy>0`, status assumed) + registered in test_baseline_clusters.py evolution cluster. retest 85785609-82 [2] bench-Dreepy → [1] energized active-Dreepy FIXED (20 vs 15, beats the backwards needy tiebreak). Whole suite green.
- for: general

**Spec (authoring spec — thin fodder):**
NEW general rung mirroring `evolve-the-energized-body-first` (+5, baseline_evolution.py) but gated on MID-line
pre-evos instead of the wincon Role: `id="advance-the-energized-line-body-first"; when = option_type==_EVOLVE
and card_is_line_preevo and (evolve_body_energy or 0) > 0; weight +5` (a WHICH-body tie-break on top of
`advance-the-evolution-line` +15; a KO still dominates on KO_SCORE). **f82 (turn 8):** both mid-line Evolve
options — evolve the **energized Active Dreepy** vs a **bare bench Dreepy** — tie at 15 (only
`advance-the-evolution-line` fires); the decide() secondary sort key `attach_to_needy_line` (pilot.py:906)
then breaks the tie TOWARD the bare bench copy, because for an Evolve it resolves `attach_target_needs=True`
on the energy-less bench Dreepy and False on the energized Active — **exactly backwards for an evolve.** You
want to advance the body that ALREADY carries energy so the evolved form can attack THIS turn. +5 makes the
energized Active win outright (20 vs 15).

WHY it wins: evolving the energized Active brings **Drakloak** id120 Dragon Headbutt ({R}{P}/70) online this
turn and lifts the body to 90 HP; evolving a bare bench Dreepy yields a Drakloak that can't attack (wasted
tempo). Card facts: **Dreepy** id119 (Active 30/70, energy {P}+{R}) → Drakloak id120 (90 HP, Dragon Headbutt
{R}{P}/70 payable with that exact energy this turn).

RELATED (out of scope for this rung — NOTED, not built): the human's full line is a same-turn tempo KO
(evolve → **Crispin** fetch+attach {D} → **Munkidori** Adrena-Brain moves ≤3 counters onto the opp Active →
Dragon Headbutt KO). That multi-step NON-winning-KO combo (ability damage-move + attack) is NOT composed by
the Lethal Solver (its win-family returns early: opp Active 1 prize < my 5 remaining, opp has bench,
planner.py:279) nor the Turn Planner (`planned=null`) — a separate Turn-Planner capability-gap, not this
rung's job. This rung realizes the first (and highest-value) step; the greedy reactive Munkidori handlers
(`_pick_counter_add_target` / `_pick_counter_remove_source`) already target well once the ability fires.

---

## refuel a starved hand over holding a REDUNDANT line piece
- id: refuel-a-starved-hand-over-a-redundant-line-piece
- source: blunder-buster
- target_layer: general-hypothesis
- candidate_signal: the `_payoff_immediate_preevo_set` in-play variant (line base already on board in sufficient quantity) + `wincon_in_hand` — thin helpers on existing line fields, no new infra.
- verification_contract: verifier
- provenance: correction 85786096:f38 (dragapult_ex) | fixture tests/fixtures/corrections/dp_refuel_over_redundant_line_piece_f38.json | exact complement of `hold-wincon-with-base-dont-shuffle` (strengthens the hold only when wincon_in_hand — no overlap)
- status: applied
- resolution (2026-07-14, update-strategy batch 2): APPLIED. New Board signal `immediate_preevo_in_play` (pilot.py: the payoff's immediate pre-evo — e.g. Drakloak — is already on the board, via `_payoff_immediate_preevo_set`). Added stand-down `and not (immediate_preevo_in_play and not wincon_in_hand)` to `hold-line-piece-dont-shuffle` (doctrine_shuffle_refresh.py): a redundant hand line-piece (its immediate pre-evo already benched, payoff not in hand to deploy) no longer blocks the refuel. retest f38 [2] Itchy-Pollen→[0] Lillie's FIXED. Whole suite green.
- for: general

**Spec (authoring spec — thin fodder):**
Refine the general shuffle-refresh rung `hold-line-piece-dont-shuffle` (doctrine_shuffle_refresh.py, −25) so
it STANDS DOWN when the held hand line-piece is REDUNDANT. Current when(): `_PLAY and "shuffle_hand" in tags
and board.line_preevo_in_hand and not board.wincon_in_play`. **f38 (turn 4, badly behind — Budew Active, no
Dragapult ex anywhere, 6 prizes untaken):** the agent attacks with Budew's Itchy Pollen (item-lock, +10)
instead of **Lillie's Determination** (which shuffles + draws **8** here — exactly 6 prizes remain). The −25
fires on the **Drakloak** in hand even though two Drakloak are already benched (redundant) AND Dragapult ex is
not in hand to deploy this turn.

**Author:** add stand-down `and not (line_preevo_in_play_in_sufficient_quantity and not wincon_in_hand)` — when
the base the payoff evolves from is ALREADY on the board (use the `_payoff_immediate_preevo_set` in-play
variant, so a benched Dreepy needing a hand Drakloak still holds) AND the payoff itself is NOT in hand
(nothing to deploy regardless), the held copy is surplus, so shuffling it to dig for the buried payoff is the
setup play. Exact complement of `hold-wincon-with-base-dont-shuffle` (strengthens the hold only when
wincon_in_hand) — no overlap. WHY it wins: with −25 removed, Lillie's returns to +20 (`dig-before-commit`) and
beats the +10 item-lock attack, so the Pilot refuels its wincon instead of ending the turn under-developed.
Card facts: **Lillie's Determination** id1227 (shuffle_hand) = shuffle hand → draw 6 (8 if exactly 6 prizes
remain); **Budew** Itchy Pollen id235 = no-cost, 10 dmg, one-turn item-lock (no KO); Drakloak id120 Stage-1
pre-evo; Dragapult ex id121 Stage-2 payoff (not in play or hand). General fix applies identically to
Staryu→Mega Starmie ex and Riolu→Mega Lucario ex.

---

## Risky Ruins needs a real net-value gate (symmetric self-chip)
- id: risky-ruins-net-value-gate
- source: blunder-buster
- target_layer: deck-strategy
- candidate_signal: needs a new Context field `risky_ruins_net_positive` = (opp remaining benchable non-{D} basics) − (our remaining non-{D} basics still to develop) > 0. The our-side term is board/hand-computable (the dominant early self-chip term); the opp-side term can lean on the Read/brief; simplest sound floor = stand down while we are still early-developing with multiple non-{D} basics left to lay down.
- verification_contract: verifier
- provenance: correction 85786096:f24 (CRITICAL, dragapult_ex) | fixture tests/fixtures/corrections/dp_risky_ruins_net_negative_vs_thin_setup_f24.json | Read context: brief cinderace_mega_starmie_ex already documents the opponent's brittle 3-Staryu root (the signal it fails to CONSUME)
- status: applied
- resolution (2026-07-14, update-strategy follow-up): APPLIED — with a SIMPLER sound floor than the proposed opp-aware net-value signal (no new infra). Gated `play-risky-ruins-when-net-positive` (strategy.py) so the place-to-chip clause requires the EXISTING `board.wincon_in_play`: don't deploy the SYMMETRIC self-chip while still assembling the fragile pre-payoff Dreepy line (before the 320-HP wincon is up we're the bench-heavier developing side the chip hurts more). The opp-stadium-denial clause stays unconditional (independent reason). retest f24 (CRITICAL) [5] Play Risky Ruins → [0] Attach→Fez FIXED — the disruptor-lock maneuver from batch 1 was firing +20 on the Fez attach but the Pilot played Risky Ruins FIRST (attach-energy-last sequencing); gating Risky Ruins to 0 lets the attach (score 30) win, so the two turn-2 fixes COMPOSE onto the exact `correct=[0]`. The full opp-aware net-value (their benchable non-{D} basics vs ours, via the Read) + skip-vs-{D}-decks remain deferred. Updated test_dragapult_ex_triggers.py (4 risky-ruins cases). Whole suite 2775 green.
- for: deck:dragapult_ex

**Spec (authoring spec — thin fodder):**
Refine `play-risky-ruins-when-net-positive` (strategy.py:91-101). Today it fires +15 whenever
`stadium_in_play is None OR opp_stadium_in_play` — it NEVER computes the net-value its name promises (its own
rationale defers "bench-our-Basics-first sequencing and skip-vs-{D}-decks refinement"). **f24 (CRITICAL, turn
2 vs a correctly-Read Cinderace/Mega Starmie ex, gamma 1.0):** **Risky Ruins** id1260 is SYMMETRIC — 20 damage
on any Basic non-{D} Pokémon a player benches. Our developing spread still has to bench many non-{D} basics
(4× **Dreepy** id119 @70 HP, **Munkidori** id112, **Dunsparce**, **Budew** — only **Fezandipiti ex** id140
{D} is immune), while the opponent is a thin fast set-up deck (~3 Staryu + 1 Cinderace, mostly deployed by T2,
no replacement path) with ~zero future bench-entries. So the stadium chips **US** far more — net-negative.

**Author:** gate the when() on the new `risky_ruins_net_positive` signal (or subtract a penalty when
net-negative) so +15 no longer fires when we are the bench-heavier developing side. WHY it wins: kills the
symmetric self-chip that damages our own 70-HP Dreepy line for zero return against an opponent with nothing
left to bench. GATE NOTE: the tagged correct=[0] (attach→Fez) will NOT become chosen even after the fix —
[0] carries `dont-feed-the-doomed` −30 (a separate mis-fire on the 210/210 full-HP Active, repaired by the
`go-down-swinging-arm-the-doomed-active` proposal above), so the develop attaches [1]/[2] (score 10) win once
[5] Risky Ruins is gated; the verifier pass condition = "Risky Ruins [5] no longer chosen / drops below the
develop options," NOT chosen==[0]. Card facts: Risky Ruins id1260 (MEG 127) symmetric bench-chip; the
non-{D} basics list above; Fezandipiti ex id140 {D}.

---

## extend the disruptor-lock maneuver to a support-ex PIVOT Active
- id: disruptor-lock-from-a-support-ex-pivot
- source: blunder-buster
- target_layer: planner-code
- candidate_signal: extend the SHIPPED `_can_lock_line_with_disruptor` (pilot.py, now in-tree post-rebase) — today its first clause requires `ma.id in _line_preevo_set()`; add an OR branch for a retreatable NON-attacking support-ex / pivot Active (not a developing wincon carrying Energy) behind a benched `item_lock` opener, under the SAME guards (`turn` ≤ 2, no wincon-Line body carries Energy, retreat reachable this turn on ≤1 more Energy, Active can't already KO). No new subsystem — the rung (`feed-the-line-for-disruptor-lock` +20) and the retreat step (`retreat-to-wall-the-line` / `hold-position-in-setup`, broadened to the offensive signal) already exist.
- verification_contract: verifier
- provenance: correction 85786096:t2s0 (CRITICAL, dragapult_ex; turn scope) | fixture tests/fixtures/corrections/dragapult_retreat_to_item_lock_fez_pivot_f25.json | builds on the SHIPPED `disruptor_lock_maneuver` (`_can_lock_line_with_disruptor` + `feed-the-line-for-disruptor-lock` +20; commits 4e38243 / 68181a9, merged to main via PR #92, in-tree after this branch's rebase) | sibling `85046350-20` (the line-preevo variant — now FIXED by the shipped build) | prior deferral history: docs/todo/retreat-to-item-lock-maneuver.md (§2026-07-13)
- status: applied
- resolution (2026-07-14, update-strategy batch 1): APPLIED. Extended `_can_lock_line_with_disruptor` (pilot.py) so the eligible Active is a line-preevo OR a retreatable support-ex pivot (`ma_stat.is_ex_body and ma_id not in _wincon_set()`); shared guards unchanged. Companion: added `and not c.board.can_lock_line_with_disruptor` to `dont-feed-the-doomed` (baseline_energy.py) — the Fez pivot is off-Line so that −30 fired on it (Dreepy is exempt as a line member). retest 85786096-25 [1] bench → [0] attach→Fez FIXED (score 30: feed-the-line +20, dont-feed-the-doomed stood down); maneuver step 1 materializes, retreat/promote-Budew/Itchy-Pollen ride the existing wired rungs. Whole suite green (disruptor/wall tests incl.).
- for: general

**Spec (authoring spec — thin fodder):**
The OFFENSIVE item-lock maneuver **`disruptor_lock_maneuver`** is BUILT and default-ON (runtime.py:41), but its
gate `_can_lock_line_with_disruptor` (pilot.py) fires ONLY when the Active is a fragile win-condition **LINE
pre-evo** (`ma.id in _line_preevo_set()`). **t2 (85786096, CRITICAL, turn 2 vs Cinderace/Mega Starmie ex):**
the Active is **Fezandipiti ex** (id140, a support-ex **PIVOT**, {D}, 210/210, retreat 1) with a benched
**Budew** (`item_lock`); the human's line is attach {R}→Fez → retreat (the {R} pays the cost 1) → promote
Budew → **Itchy Pollen** item-lock. Re-probed against the MERGED code (`retest_one 85786096-25`): `decide()=[1]`
bench, `feed-the-line-for-disruptor-lock` does NOT fire (Fez excluded by the line-preevo clause),
`dont-feed-the-doomed` (−30) sinks the Fez attach → still the blunder.

**Author:** extend `_can_lock_line_with_disruptor` (add an OR branch, or a sibling premise sharing the rung)
so the maneuver ALSO fires on a **retreatable non-attacking support-ex / pivot Active** behind a benched
`item_lock`, under the same guards it already applies to the line-preevo case (turn ≤ 2; nothing being
developed — no wincon-Line body carries Energy; retreat reachable this turn on ≤1 more Energy; Active can't
already KO). Then `feed-the-line-for-disruptor-lock` (+20) arms the Fez attach and the broadened
`retreat-to-wall-the-line` / `hold-position-in-setup` ride retreat → promote Budew → Itchy Pollen, exactly as
they do today for the Dreepy-line variant. WHY it wins: same disruption value (deny the opponent's Item setup
turn) from a pivot the deck WANTS to cycle out anyway — Fezandipiti ex is a natural sacrificial promote (its
Flip-the-Script payoff is post-KO). Recovery/tempo line, matchup-dependent — the existing
`disruptor_lock_maneuver` kill-switch is the lever if it underperforms. Guard: never retreat a live wincon
attacker into a worse board just to lock. VERIFY: `decide()` on
`dragapult_retreat_to_item_lock_fez_pivot_f25.json` materializes attach→Fez (step 1) and the retreat/promote/
lock follow; inert when the Active is neither a line pre-evo nor a retreatable support pivot. Card facts:
Fezandipiti ex id140 (Basic ex, {D}, 210 HP, retreat 1, Flip-the-Script draw-3-on-KO); Budew id235 Itchy
Pollen (no-cost, 10 dmg, one-turn item-lock).
