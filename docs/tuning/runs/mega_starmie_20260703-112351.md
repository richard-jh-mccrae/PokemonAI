# Tuning run — mega_starmie

- **When:** 2026-07-03T11:23:51  ·  **Build:** `mega_starmie_20260703_de77dc9`  ·  **Corrections:** 8
- **Conservatism (`reg`):** 0.25  ·  see [how this works](../methodology.md)
- **Weight fit:** 1/6 ranking constraints satisfied — authored weights **kept** (the fit could not satisfy more without overfitting) (the authored seeds alone satisfy 1).

## Weights tuned
- _None._ The fit ships a weight change only when it satisfies **strictly more** corrections than the authored seeds; here it did not, so the legible priors are kept. (Lower `--reg` to let clean corrections move weights more; the ladder A/B is the real test.) The leverage this round is in the new rules below.

## New rules proposed (need a human-authored `when()`)
- **wasted_resource** (ep 83454549 f36, seed w=20.0): fire at SelectContext='Discard' (turn 4) to prefer 'Cinderace, Ignition Energy' over 'Cinderace, Lillie's Determination'
  - “With this hand, its more important to prepare our second Mega Starmie, and then refresh our hand with 8 new cards.

Should have discarded Cinderace and Ignition enegry (because our active mega starmie”
- **wasted_resource** (ep 83457493 f33, seed w=20.0): fire at SelectContext='Main' (turn 4) to prefer 'End turn' over 'Play Night Stretcher'
  - “CRITICAL: Cinderace shall never be recycled with Night Stretch because it can never be played outside of match setup.”

## Corrections that need a rule, not a weight (unsatisfied)
_The weight fit cannot make `correct ≻ chosen` here by reweighting existing hypotheses — they are contradictory, or the distinction needs a new rule._
- ep 83455356 f11 [sequencing_error] `Play Crushing Hammer` → `Play Salvatore` — “CRITICAL: We just searched the deck with buddy-buddy poffin, thus we know that it contains Mega Starmie Exs. We should therefor have played Salvatore, evolved S…”
- ep 83456015 f35 [sequencing_error] `Attach Ignition Energy → Mega Starmie ex (active · 210/330 · 1⚡)` → `Play Wally's Compassion` — “CRITICAL: This is critical because of sequencing. Our opponent has mega starmie that can do 210 dmg. our active has 210 HP. the opponent's deck commonly runs ig…”
- ep 83456015 f47 [wrong_supporter] `Play Lillie's Determination` → `Play Wally's Compassion` — “CRITICAL: Use of Wally's Compassion has regressed. We desperately need to heal ourselves here, then attach Ignition ENergy, then KO opponent's active”
- ep 83457493 f20 [wrong_supporter] `Play Salvatore` → `Play Boss’s Orders` — “We do not have energy to power Cinderace, thus we are behind in tempo. The Salvatore however nice, doesnt help our immediate turn. we need to stall our opponent…”
- ep 83457493 f31 [wrong_supporter] `Play Boss’s Orders` → `Play Harlequin` — “CRITICAL: We need energy here and we need to disrupt opponent. Should play our two Buddy-buddy poffins to discard them (do not want them shuffled back into deck…”


## Authored this round (/blunder-buster 2026-07-03, store de77dc9)

8 corrections, 6 CRITICAL. 7 resolved **fixed**, 1 (e1db f47) refuted-shape pending human ack.
Full suite 1177 green; every fix carries a real-replay regression gate.

### Planner-layer code fixes (ADR-0031/0037 — no `when()`, retest + fixtured test are the gate)

- **a212c4eb516f (f11, CRITICAL)** — chose Crushing Hammer over the game-winning Salvatore line.
  Fix: win-rung **tier-4 evolution-tutor** (`rush_evolve`: deck-CERTAIN no-Ability direct evolution
  onto an in-play body → retreat → attach → min-bound KO → last-prize/bench-empty win; sound,
  tracker-anchored) + rank-grade `_tutor_evolve_ko_candidate` in ko_for_prizes (majority deck-odds).
  Retest: chosen `[1]→[6]`=correct, `planned` null→`ko_for_prizes` "evolution tutor unlocks a
  1-prize KO". Gates: 5 unit tests + `planner_a212.json` real-state test. Ledgered `covered`
  (planner-layer fixed; W-route is blind to a short-circuited pick).
- **6858e8b5861b (f35, CRITICAL)** — attached Ignition before Wally's Compassion (heal bounces all
  Energy → order is everything). Fix: stabilize_then_ko gate now accepts the **attach-carried KO**
  (`_ATTACH` tactical ≥ KO_SCORE) and `_heal_candidate` models the re-attach via
  `_best_hand_attach_units` — **Ignition = {C}{C}{C} on an Evolution → 3 units** (and 0 units when
  the hand holds none: a phantom re-attach can no longer fake a preserved KO). Retest: `[1]→[3]`,
  `planned` null→`stabilize_then_ko`. Gates: 2 unit + `planner_6858.json`. Ledgered `covered`.

### Pilot sequencing / signal fixes (code + tests)

- **eb9847fd543c (f38, CRITICAL)** — Boss's Orders sequenced ahead of the 3-prize Nebula KO as an
  "informative Supporter"; the gust swapped the defender and forfeited it. Fix (`_finish_turn_last`):
  a **gust play never sequences ahead of a menu KO** (its own KO-unlock still rides tier 0). The
  agent may still take endorsed non-defender-changing dev first (attack-last, ep83037962), so the
  gate pins "never the gust". Gates: sequencing unit test + `pilot_eb98.json`.
- **6f148d542bd4 (f31, CRITICAL)** — `gust-for-the-ko` endorsed Boss's with ZERO payable Energy.
  Fix: gust whether-to-play/offense baselines gated on **payable Energy this turn** (attached + best
  unspent hand attach; `_gust_best_ko_prizes` / `_active_ko_prizes` / `_gust_target_tactical`).
  Harlequin (the human's disruption/refresh) now wins the slot. Retest `[3]→[4]`. Gates:
  REQ-GUST-0012 unit + `pilot_6f14.json`. (The rationale's poffins-thin-before-Harlequin nicety is
  noted, not built — the tagged correct pick is honored.)

### Authored Hypotheses (Verifier-gated)

- **`dont-recycle-the-dead`** (doctrine_fetch, −40, assumed) — fixes **b3235464d60a (f33,
  CRITICAL)**: Night Stretcher recycling the setup-only Explosiveness Cinderace (hand-unplayable) won
  a 0-0 tie over End turn. New sound signal `Board.recycle_dead_only` (discard pool non-empty, every
  member stranded — reuses `_stranded_evolution_set`; Basic Energy never dead; unknown fails open).
  Verifier PASSED (cluster satisfied, 0 regressed). Retest `[0]→[3]`, margin 0→25.
- **`stall-gust-over-dev-when-starved`** (doctrine_gust, +95, assumed) — fixes **cd91b9f1d850
  (f20)**: with Cinderace facing the Riolu→Mega Lucario line and ZERO Energy anywhere, Salvatore's
  dig stack (98) beat the stall gust. Two new signals: **general forward-evolution doom**
  (`_forward_incoming_damage` now counts any affordable forward form's printed damage, W/R-adjusted —
  the deferred "evolves-into-attacker" read) and `Board.active_attack_payable`. Fires ONLY under the
  provable energy famine. Verifier PASSED. Retest `[0]→[3]`, margin 98→22 the other way.
- **`keep-key-cards-at-discard` premise-gated** (amendment) — fixes **c4f51ab337f1 (f36)**: the
  `discard_eot` keep is void once the Active already carries its biggest attack's cost (new
  `Board.active_fully_powered`) — pitch Ignition, keep Lillie's Determination (8-card refresh at 6
  prizes). Verifier PASSED. Retest `[0,1]→[0,2]`.

### Tuner method fix

- **Multi-pick featurize** (`tools/train/tuner/featurize.py`): W-route constraints now diff the SET
  DIFFERENCE of chosen/correct (first differing pair), not position 0 of each list — a Discard-2
  correction sharing element 0 was an empty-delta constraint, unsatisfiable by construction.
  `docs/tuning/methodology.md` §5.1 updated in the same change.

### e1db (f47, CRITICAL) — resolved `covered` per human ack (2026-07-03)

- The human re-asserted the intended sequence: "Wally's, attach Ignition, Nebula for KO". That line
  belongs to turn 8's **pre-attach** decision point — by the tagged f47 frame the manual attach was
  already spent (`energyAttached=true`), so at f47 itself the heal would forfeit the certain 3-prize
  Nebula KO (refutation-proof test pins the two facts). Rewinding the captured board to the
  pre-attach state, the f35 stabilize_then_ko fix **commits exactly that sequence** — Wally's first,
  the Ignition attach's KO (tac ≥ 1000) queued behind it
  (`test_e1db_intended_sequence_commits_from_the_turn8_pre_attach_state`). One fix covers both turns
  of the episode; ledgered `covered`.
