# Tuning run — mega_lucario

- **When:** 2026-07-05T03:15:49  ·  **Build:** `mega_lucario_20260703_194dc40-dirty`  ·  **Corrections:** 9
- **Conservatism (`reg`):** 0.25  ·  see [how this works](../methodology.md)
- **Weight fit:** 6/7 ranking constraints satisfied — fit **adopted** (the authored seeds alone satisfy 3).

## Weights tuned
- **`dig-before-commit`: 20 → 23.0** (+3.00, normal preference).
  - Moved by 2 correction(s) (1 want it higher, 1 lower). For example:
    - ep 83661652 f40 [wasted_resource] `Play Lillie's Determination` → `Play Riolu` — “CRITICAL: Just shuffled in the pre evolution to our main line attacker, Riolu! bad bad”
    - ep 83966336 f44 [slow_setup] `End turn` → `Play Lillie's Determination` — “CRITICAL: we have nothing in our hand and are desperately behind the opponent. should refill hand. even though hand contains mega lucario, we have no riolu in p…”
- **`fetch-a-starter`: 12 → 9.31** (-2.69, normal preference).
  - Moved by 2 correction(s) (0 want it higher, 2 lower). For example:
    - ep 83966336 f9 [slow_setup] `Solrock` → `Basic {F} Energy` — “CRITICAL: we need energy, be sure to fetch it with this.”
    - ep 83967841 f14 [slow_setup] `Lunatone` → `Basic {F} Energy` — “CRITICAL: we already have lunatone in hand but dont have any energy. we typically only ever need a single lunatone and a single solrcok in play at any given tim…”
- **`fetch-energy-when-starved`: 25 → 27.69** (+2.69, strong preference).
  - Moved by 2 correction(s) (2 want it higher, 0 lower). For example:
    - ep 83966336 f9 [slow_setup] `Solrock` → `Basic {F} Energy` — “CRITICAL: we need energy, be sure to fetch it with this.”
    - ep 83967841 f14 [slow_setup] `Lunatone` → `Basic {F} Energy` — “CRITICAL: we already have lunatone in hand but dont have any energy. we typically only ever need a single lunatone and a single solrcok in play at any given tim…”
- **`fetch-the-engine-first`: 20 → 17.31** (-2.69, normal preference).
  - Moved by 2 correction(s) (0 want it higher, 2 lower). For example:
    - ep 83966336 f9 [slow_setup] `Solrock` → `Basic {F} Energy` — “CRITICAL: we need energy, be sure to fetch it with this.”
    - ep 83967841 f14 [slow_setup] `Lunatone` → `Basic {F} Energy` — “CRITICAL: we already have lunatone in hand but dont have any energy. we typically only ever need a single lunatone and a single solrcok in play at any given tim…”
- **`fetch-when-it-fills-a-need`: 8 → 5.0** (-3.00, faint tiebreaker).
  - Moved by 2 correction(s) (0 want it higher, 2 lower). For example:
    - ep 83661652 f29 [sequencing_error] `Play Ultra Ball` → `Play Riolu` — “I would have saved the ultra ball a turn because we are just now laying down 3 basics who will not be able to evolve this turn.”
    - ep 83967841 f17 [wasted_resource] `Play Ultra Ball` → `End turn` — “CRITICAL: still just setting up. nothing needs evolving. just save the ultra ball for next turn.”
- **`hold-wincon-dont-shuffle`: -25 → -22.0** (+3.00, normal preference penalty).
  - Moved by 1 correction(s) (1 want it higher, 0 lower). For example:
    - ep 83966336 f44 [slow_setup] `End turn` → `Play Lillie's Determination` — “CRITICAL: we have nothing in our hand and are desperately behind the opponent. should refill hand. even though hand contains mega lucario, we have no riolu in p…”

## New rules proposed (need a human-authored `when()`)
- **wasted_resource** (ep 83966336 f14, seed w=20.0): fire at SelectContext='Main' (turn 1) to prefer 'End turn' over 'Play Premium Power Pro'
  - “CRITICAL: must never play Premium Power Pro when unable to attack this turn”
- **wasted_resource** (ep 83966336 f27, seed w=20.0): fire at SelectContext='Main' (turn 3) to prefer 'Play Lillie's Determination' over 'Play Team Rocket's Petrel'
  - “CRITICAL: You used Petrel to grab a supporter that we already have in hand. waste”

## Corrections that need a rule, not a weight (unsatisfied)
_The weight fit cannot make `correct ≻ chosen` here by reweighting existing hypotheses — they are contradictory, or the distinction needs a new rule._
- ep 83967841 f17 [wasted_resource] `Play Ultra Ball` → `End turn` — “CRITICAL: still just setting up. nothing needs evolving. just save the ultra ball for next turn.”

## Already reviewed (excluded from fresh work)
_Assessed in an earlier round and recorded in `data/corrections/reviewed.json` — not re-surfaced here. `refuted` are also dropped from the weight fit._
- ep 83661652 f3 **covered** `Meowth ex` → `Meowth ex` — “FIXED on investigation (not a mislabel): the Pilot literally could not DECLINE an optional single-pick (decide() always took maxCount, ignoring minCount=0). Added (a) single-pick take-fewer — decline a score<0 pick at a minCount=0 select; (b) dont-pre-bench-the-supporter-tutor (-15) at _SETUP_BENCH. decide(f3)=[0]->[] (declines benching Meowth ex — saved for the Active Spot / an in-game bench to trigger Last-Ditch Catch). W-route can't measure it (correct==chosen==[0] tag can't encode select-nothing); gated by tests/strategy/test_setup_bench_decline.py on setup_bench_decline_f3.json. Human reversed the refuted call 2026-07-04.”
- ep 83661652 f19 **covered** `Play Boss’s Orders` → `Attach Basic {F} Energy → Lunatone (active · 110/110 · 1⚡)` — “develop-a-basic-in-setup develops Riolu over the poor Boss's Orders stall (retest [4]->[5]); W-route-blind. The poor-stall blunder is gone.”
- ep 83661652 f31 **covered** `Riolu` → `Mega Lucario ex` — “Root is the f30 discard (now prevented by keep-line-base-at-discard): with Riolu no longer pitched, the discard->refetch waste never arises. On the isolated captured state (Riolu already discarded) grabbing the base is sound fetch-base-before-stranded-payoff doctrine.”
- ep 83661652 f33 **covered** `Attack with Power Gem` → `Play Riolu` — “develop-a-basic-in-setup: real Pilot develops a basic (Solrock) before the Meowth Power Gem chip (retest [3]->[1]); _finish_turn_last tiers the tier-0 develop before the tier-4 attack. First-dev-differs; W-route-blind (chip outscores the develop).”
- ep 83661652 f44 **covered** `Attack with Power Gem` → `Play Riolu` — “develop-a-basic-in-setup: real Pilot develops a basic (Makuhita) before the Meowth chip (retest [3]->[0]); same first-dev-differs as f33. The 'resisting to play basics' blunder is resolved.”

## Authored this round

Blunder round **2026-07-05** (build `mega_lucario_20260705_484d957`). **All six corrections carried
the uppercase CRITICAL marker** — one theme, *early-setup resource discipline*: the agent over-invests
during setup turns (grabs a redundant engine piece while energy-starved, plays a dead attack-buff,
burns a tutor it can't use, holds a wincon it can't deploy). Resolved serially, no fan-out. Every
correction reached **fixed** via a committed rule (retest through the real Pilot; SEED weights, no
`tuned.json` — leverage is entirely in the rules, so `tuned.json` = `{}`). Zero regressions across all
9 corrections; full suite green. Math: [methodology](../methodology.md).

**New infra (step 4b):** `Board.wincon_in_hand_undeployable` (`pilot.py`) — an EVOLUTION win-condition
in hand with NO base anywhere (not in play, and its Line pre-evolution neither in play nor hand) is a
dead card. False for a Basic-payoff wincon (directly benchable). Unit-tested in `test_pilot.py`
(`test_context_board_exposes_wincon_in_hand_undeployable` + the basic-payoff guard).

| # | Correction (CRITICAL) | Fix | Retest `chosen`→ / `margin`→ |
|---|---|---|---|
| A | f9 `slow_setup` — grabbed a redundant Solrock over Basic {F} Energy while starved; **and** ep83967841 f14 — grabbed a redundant Lunatone | **`fetch-energy-when-starved` seed 25→35** (`doctrine_fetch`): energy must DOMINATE every Pokémon grab in the famine (it already stood above `fetch-the-wincon` +30 by design, but was seeded below the grabs it was meant to beat) | f9 `[1]→[2]` m `0→0` ✅ · f14 `[2]→[0]` m `0→0` ✅ |
| B | f14 `wasted_resource` — played Premium Power Pro (dead this-turn {F} attack buff) turn-1-going-first | **`dont-play-damage-boost-when-cant-attack`** (`baseline_sequencing`, −12): a `CardStat.damageBoost>0` card is dead unless you attack this turn — nets it below End when `not active_attack_payable` | `[0]→[2]` m `0→12` ✅ |
| C | f27 `wasted_resource` — played Team Rocket's Petrel (narrow tutor) over Lillie's, tutoring a Supporter already held | **`demote-needless-search-supporter-in-setup`** (`doctrine_fetch`, −20): EXACTLY neutralises `dig-before-commit` (+20) so a needless `search` Supporter nets 0 → `_finish_turn_last` drops it from tier 1 to tier 4, letting the tier-3 refresh win the mutually-exclusive Supporter slot (still ties End, so plays as the only dig) | `[0]→[1]` m `0→20` ✅ |
| D | f44 `slow_setup` — ended the turn holding a Mega Lucario ex with no Riolu anywhere instead of refilling | **gate `hold-wincon-dont-shuffle` on `not wincon_in_hand_undeployable`** (`doctrine_shuffle_refresh`) — a dead payoff is shuffled away to dig for the base, not held | `[2]→[0]` m `5→0` ✅ |
| E | ep83967841 f17 `wasted_resource` — paid Ultra Ball's 2-card discard to tutor while starved + board developed | **`dont-costly-tutor-when-starved-and-developed`** (`doctrine_fetch`, −30): a `cost_discard`+`tutor_pokemon` play can't fix the Energy you lack when the Bench is already set — nets it below End | `[2]→[3]` m `23→12` ✅ |

**Guard tests:** `tests/strategy/test_blunder_20260705.py` (6 real-replay gates) + the two `test_pilot.py`
signal tests + the `test_shuffle_refresh.py` split (deployable-in-hand keeps the moderate hold; the new
undeployable case frees the refresh). Fixtures under `tests/fixtures/corrections/ml0705_*.json`.
Cluster C and D depend on a `_finish_turn_last` **sequencing threshold** (score ≤ 0 → tier 4), not a
ranking margin — the weight fit's featurization agrees they're satisfied, but the fixtures are the real
gate.
