# Tuning run — mega_starmie

- **When:** 2026-06-29T11:01:56  ·  **Build:** `mega_starmie_20260629_7accd2e-dirty`  ·  **Corrections:** 24
- **Conservatism (`reg`):** 0.25  ·  see [how this works](../methodology.md)
- **Weight fit:** 19/19 ranking constraints satisfied — authored weights **kept** (the fit could not satisfy more without overfitting) (the authored seeds alone satisfy 19).

## Weights tuned
- _None._ The fit ships a weight change only when it satisfies **strictly more** corrections than the authored seeds; here it did not, so the legible priors are kept. (Lower `--reg` to let clean corrections move weights more; the ladder A/B is the real test.) The leverage this round is in the new rules below.

## New rules proposed (need a human-authored `when()`)
- _None._

## Corrections that need a rule, not a weight (unsatisfied)
_The weight fit cannot make `correct ≻ chosen` here by reweighting existing hypotheses — they are contradictory, or the distinction needs a new rule._
- _None._

## Already reviewed (excluded from fresh work)
_Assessed in an earlier round and recorded in `data/corrections/reviewed.json` — not re-surfaced here. `refuted` are also dropped from the weight fit._
- ep 82523811 f59 **refuted** `Attach Basic {W} Energy → Mega Starmie ex` → `Attach Basic {W} Energy → Mega Starmie ex` — “Mis-tagged: chosen [2] and correct [6] both attach a Basic {W} Energy to the SAME benched Mega Starmie (bench0) — board-identical, no real blunder as encoded (different identical W cards). The rationale's true intent (attach a 2nd energy to the ACTIVE toward Nebula Beam) is now realized by the new build-active-wincon Hypothesis, which makes the agent attach to the active win-condition here.”
- ep 82523811 f95 **covered** `Attack` → `Attach Basic {W} Energy → Mega Starmie ex` — “Real Pilot decide() now picks the attach (build-active-wincon fires +20 on the active win-condition under its max attack cost; retest chosen [1]->[0]=correct). W-route flags it unsatisfiable only because the alternative is a KO attack (tac 1000.9) that dominates by score — but _finish_turn_last sequences the tier-1 attach BEFORE the tier-2 KO, so the energy is attached this turn and the KO still lands next frame (attack-last). Covered by build-active-wincon + attack-last sequencing.”
- ep 82524455 f6 **refuted** `Play Buddy-Buddy Poffin` → `Attach Basic {W} Energy → Staryu` — “Requires deck-content certainty (no Staryu left in deck) not soundly derivable from one obs: only 1 of 3 Staryu is visible; the other 2 could sit in the 6 hidden prizes, so 'a 2nd Buddy-Buddy whiffs' is a probabilistic read, not a fact. Playing bench-fill turn 1 is sound doctrine (prefer-bench-fill-first). Proper deck-tracking feature spawned as a follow-up task.”
- ep 82524455 f27 **covered** `Attack` → `Attach Basic {W} Energy → Mega Starmie ex` — “Real Pilot decide() now picks the attach (build-active-wincon fires +20; retest chosen [3]->[2]=correct). W-route unsatisfiable because the alternative is a KO attack dominating by score; _finish_turn_last sequences the tier-1 attach before the tier-2 attack so the energy is attached before the turn ends. Covered by build-active-wincon + attack-last sequencing.”
- ep 82525741 f58 **refuted** `Play Boss’s Orders` → `Attach Basic {W} Energy → Mega Starmie ex` — “Chosen Boss's Orders reaches a guaranteed KO (gust-for-the-ko fires: gust_best_ko_prizes 1 > active_ko_prizes 0 — gust the 70HP Staryu, Jetting Blow KOs it for a prize). The correct (attach + chip the 190HP active, no KO) forgoes that KO for positional chip; a positional weight must never override a KO (forgo-ko principle, ADR). Chip-the-bigger-fish-vs-take-the-small-KO lookahead deferred to a separate task.”
- ep 82525741 f81 **refuted** `Attach Basic {W} Energy → Mega Starmie ex` → `Attach Basic {W} Energy → Mega Starmie ex` — “Mis-tagged: correct [2] == chosen [2] (both attach {W} to bench0) — structurally unsatisfiable (no ranking constraint). Rationale's intent (2nd energy to the ACTIVE toward Nebula Beam) is now realized by build-active-wincon, which makes the agent attach to the active win-condition here.”


---

## Authored this round (/blunder-buster, 2026-06-29)

One round busting the `mega_starmie_20260629_7accd2e-dirty` log (30 corrections). **Open set (4 proposals + 9 W-route UNSATISFIED) resolved to a terminal outcome each; the re-run shows 0 proposals / 0 unsatisfied.** Method: [methodology](../methodology.md). Gates: per-cluster `verify().passed` (score-rankable, no regressions) **and** `retest fixed` (real Pilot `decide()`, incl. `_finish_turn_last`) **and** full `pytest` (498 passed). Where multiple identical `{W}` cards make several attach options board-identical, "fixed" is read as **board-equivalent** (same option_type + target + card id chosen).

### New infra built (step 4b)
- **`CardStat.maxDamageCost`** + **`CardStat.attacks`** (`scouting/provider.py`): cost of the highest-damage attack (Nebula Beam CCC=3) and the attack-id list. Test in `test_scouting_provider.py`.
- **`Context.attach_target_under_max`** (`pilot.py`): attach target carries fewer Energy than its max-damage attack cost (fail-closed). Mirror of `attach_target_needs`.
- **`Board.strongest_forward_bench`** + **`Context.target_is_strongest_forward`** + **`Board.bench_threat_present`** (`pilot.py`): the strongest forward-evolution threat among DAMAGE snipe targets, and whether any target is already energized (so the evolving-snipe stands down for `snipe-the-threat`).
- **`_attach_lethal_tactical`** + `_finish_turn_last` tier-0 for a lethal attach (`pilot.py`): a KO_SCORE-class lookahead — attaching this Energy unlocks a KO this turn (models Ignition → CCC=3 on an Evolution → Nebula Beam 210); take the win before digging. Fires only when the attach is *necessary*.
- **Function tag `discard_hand`** on Harlequin (1223) + Lillie's Determination (1227) — both "shuffle your hand into your deck".
- Tests in `test_blunder_20260629.py` (REQ-GEN-0025…0030).

### Hypotheses authored (all deck-agnostic → `general_strategy.py`, `status="testing"`)
- **`build-active-wincon`** (+20, normal band) — keep attaching to the ACTIVE win-condition until it can fire its biggest attack (not just its cheapest). Fixes the root of the misattachment/sequencing cluster.
  - fr77 (sequencing): chosen `[6]`→`[0]` (attach Active over a bench Staryu), **fixed**. fr110 (wasted_resource): `[2]`→`[4]`, **fixed**. fr95/fr27 **covered** (real `decide()` attaches before the tier-2 KO).
- **`attach-before-hand-shuffle`** (−60, near-imperative) — don't play a `discard_hand` card (Harlequin/Lillie's) while holding a reusable Energy you haven't attached; demotes it below the attach (and below 0 → sequenced last).
  - fr93: `[4]`(Harlequin)→`[1]` (attach), margin 950.9→ fixed. fr87: `[0]`(Lillie's)→`[1]`, **fixed**. fr77 (with build-active-wincon): **fixed**.
- **`dont-rush-evolve-without-target`** (−60) — penalise a `rush_evolve` tutor (Salvatore) with no pre-evolution to evolve.
  - fr84: `[3]`(Salvatore)→`[1]` (attach Active, board-equiv to correct `[4]`), **fixed**. fr110: **fixed**.
- **`snipe-the-strongest-evolving-threat`** (+20) — among non-energized evolving threats, snipe the biggest forward line (Riolu→Mega Lucario 270 over Makuhita→Hariyama 210); stands down when an energized threat is present.
  - fr41 (bad_target): `[3]`(Makuhita)→`[4]`(Riolu), margin 0→20, **fixed**. fr61: `[4]`→`[3]`(Riolu), margin 15→5, **fixed**.
- **`keep-key-cards-at-discard`** (−30) — at a cost-discard (Ultra Ball), don't discard a `discard_eot` burst Energy (Ignition) or the win-condition; shed redundant Supporters instead.
  - fr14 (wasted_resource): `[0,1]`(Ignition+Boss's)→`[1,2]` (keeps Ignition, discards a Wally's), correct `[2]` ⊆ chosen, **fixed**.
- **`prefer-rush-evolve-tutor` gate refined** — added `and not wincon_in_hand` (mirrors `tutor-the-wincon`): when the payoff is already in hand, evolve directly instead of spending a tutor.
  - fr23 (sequencing): `[0]`(Salvatore)→`[2]`(Evolve Mega Starmie), **fixed**.

### Lethal-attach (tactical, `_attach_lethal_tactical`)
- fr105 (sequencing): `[0]`(Harlequin)→`[4]`(attach **Ignition** → Nebula Beam 210 KOs the 200-HP Mega Lucario = the win), margin 69.9→**903.1**, **fixed**.

### Terminal-outcome ledger (open set)
| Correction | Outcome | Evidence |
|---|---|---|
| 82523811 f41 (bad_target) | **fixed** | snipe-the-strongest-evolving-threat; retest `[3]→[4]` |
| 82523811 f61 (bad_target) | **fixed** | snipe-the-strongest-evolving-threat; retest `[4]→[3]` |
| 82523811 f93 (sequencing) | **fixed** | attach-before-hand-shuffle + build-active-wincon; retest `[4]→[1]` |
| 82523811 f105 (sequencing) | **fixed** | lethal-attach (Ignition→Nebula); retest `[0]→[4]`, margin →903.1 |
| 82525101 f87 (sequencing) | **fixed** | attach-before-hand-shuffle + build-active-wincon; retest `[0]→[1]` |
| 82525741 f77 (sequencing) | **fixed** | build-active-wincon (Active over bench); retest `[6]→[0]` |
| 82523811 f84 (wasted_resource) | **fixed** (board-equiv) | dont-rush-evolve + build-active-wincon; attaches `{W}` to Active |
| 82525101 f110 (wasted_resource) | **fixed** | dont-rush-evolve + build-active-wincon; retest `[2]→[4]` |
| 82525101 f14 (wasted_resource) | **fixed** | keep-key-cards-at-discard; retest `[0,1]→[1,2]` |
| 82523811 f95 (sequencing) | **covered** | build-active-wincon + attack-last; real `decide()` `[1]→[0]` |
| 82524455 f27 (sequencing) | **covered** | build-active-wincon + attack-last; real `decide()` `[3]→[2]` |
| 82522726 f23 (sequencing) | **fixed** | prefer-rush-evolve-tutor `not wincon_in_hand` gate; retest `[0]→[2]` |
| 82523811 f59 (misattachment) | **refuted** | mis-tag: correct board-identical to chosen; intent now met by build-active-wincon |
| 82525741 f81 (misattachment) | **refuted** | mis-tag: correct == chosen; intent now met by build-active-wincon |
| 82525741 f58 (wasted_resource) | **refuted** | forgo-KO: gust reaches a guaranteed KO the chip can't |
| 82524455 f6 (wasted_resource) | **refuted** | needs unsound deck knowledge (hidden prizes); task spawned |

### Out of scope (tactical skips, not in the open set)
4 "Attack → Crushing Hammer" corrections (82523811 f15/f79, 82525101 f92/f102) + 1 snipe (f55) are `tactical`-attribution skips. A follow-up task was spawned for the energy-denial-before-attack sequencing pattern.
