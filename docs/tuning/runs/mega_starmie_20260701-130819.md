# Tuning run — mega_starmie

- **When:** 2026-07-01T13:08:19  ·  **Build:** `mega_starmie_20260701_c1efef0`  ·  **Corrections:** 4
- **Conservatism (`reg`):** 0.25  ·  see [how this works](../methodology.md)
- **Weight fit:** 4/4 ranking constraints satisfied — authored weights **kept** (the fit could not satisfy more without overfitting) (the authored seeds alone satisfy 4).

## Weights tuned
- _None._ The fit ships a weight change only when it satisfies **strictly more** corrections than the authored seeds; here it did not, so the legible priors are kept. (Lower `--reg` to let clean corrections move weights more; the ladder A/B is the real test.) The leverage this round is in the new rules below.

## New rules proposed (need a human-authored `when()`)
- _None._

## Corrections that need a rule, not a weight (unsatisfied)
_The weight fit cannot make `correct ≻ chosen` here by reweighting existing hypotheses — they are contradictory, or the distinction needs a new rule._
- _None._

## Already reviewed (excluded from fresh work)
_Assessed in an earlier round and recorded in `data/corrections/reviewed.json` — not re-surfaced here. `refuted` are also dropped from the weight fit._
- ep 83037962 f78 **covered** `Play Wally's Compassion` → `Attack with Nebula Beam` — “Resolved by game-winning-attack-first sequencing + active_can_ko heal gate (this round). The correct option (Nebula Beam) is a lethal tactical KO that the real Pilot now takes immediately instead of the energy-stripping Wally's Compassion; W-route classifies it as tactical (combat term), so record it covered by the tactical layer.”

## Authored this round

Six corrections from the episode-83037962 mirror (four CRITICAL, resolved first serially) plus two that
arrived mid-run from episode 83038055. **Common thread of the CRITICAL four: acting correctly when the
Active is DOOMED.** Every fix is behavior-verified through the real Pilot `decide()` (retest), not just
the W-route. Method math: [../methodology.md](../methodology.md).

**New rules (general baseline / doctrine):**

- **`dont-overbuild-the-doomed-wincon`** (`baseline_energy`, −45) — fixes f48 (`misattachment`, CRITICAL).
  When the Active win-condition is doomed and can ALREADY attack this turn (`not attach_target_needs`),
  cancels the `concentrate-energy-on-wincon`(+25)+`build-active-wincon`(+20) stack so the 2nd Energy
  goes to the benched successor, not toward a Nebula Beam the doomed body won't live to fire.
  Retest: `chosen [2]→[3]` (bench Staryu), Verifier passed, `fixed`.

- **`hold-successor-when-doomed`** (`doctrine_shuffle_refresh`, −35) — fixes f49 (CRITICAL) **and** f51.
  When the Active wincon is doomed + the successor Mega is in hand + a Line base is in play, refuse the
  `shuffle_hand` (Harlequin/Lillie's) — the hand copy is the next attacker, not a redundant duplicate
  (the existing holds stand down on `wincon_in_play`; the dead-hand scan is fooled post-attach). Nets the
  refresh below 0 so `_finish_turn_last` tiers it under the attack. Retest f49 `[1]→[2]`, f51 `[0]→[3]`.

- **`feed-the-firing-accelerator`** (`baseline_energy`, +35) — fixes f70 (`misattachment`, CRITICAL, peer
  mirror). Feed a doomed Active accelerator (Cinderace/Turbo Flare) its 1 Energy — it multiplies to 3 on
  the Bench — over dribbling onto one benched body. Reads the new `attach_feeds_firing_accel` signal and
  stands `dont-feed-the-doomed` down (a firing accelerator isn't a spent opener). Cleanly separated from
  the retreat-into-a-ready-attacker case by `not bench_wincon_ready` (ep83007714 f65 untouched).
  Retest `chosen [3]→[2]` (attach active Cinderace, +45 vs +35).

**Modified rules:**

- **`hold-clutch-heal`** (`baseline_heal`) — `+ not active_can_ko`. Fixes f78 (`missed_win`, CRITICAL,
  recorded `covered`): with a lethal on the board there is nothing to survive FOR — don't strip your own
  Energy to heal-and-stall.
- **`attach-before-hand-shuffle`** (`doctrine_shuffle_refresh`) — `+ energy_placeable`. Fixes f40
  (`sequencing_error`): when the held Energy has no home (Active maxed, Bench empty) the guard must not
  veto a bench-finding refresh. Retest `[3]→[0]` (Lillie's over a redundant Mega Signal).
- **`dont-tutor-the-held-wincon`** (`doctrine_fetch`) — `search_redundant_wincon` extended to the
  wincon-IN-PLAY-with-no-base case (`wincon_in_play and not wincon_base_deployable`), so a benchless agent
  stops digging a second undeployable Mega. Part of the f40 fix.

**New infra (Pilot signals, step 4b) — each with a focused test in `tests/test_blunder_20260701.py`:**

- `Board.active_can_ko` — my Active's BEST affordable attack KOs (superset of `active_cheap_attack_kos`;
  sees a loaded Active whose BIG attack, not its cheapest, reaches the KO).
- `Context.attach_feeds_firing_accel` — this ATTACH feeds an Active accelerator that needs the Energy to
  fire, with a bench recipient and no ready benched wincon.
- `Board.energy_placeable` — some in-play body can still absorb Energy productively (fail-open).
- **Pilot `_finish_turn_last`** — a GAME-WINNING KO (takes my last prize) is now tier-0 (taken
  immediately); a non-winning KO still develops-first (attack-last intact). This is what makes f78's
  lethal Nebula the chosen option rather than a deferred one behind a `+20` dig.

All gates green: Verifier `passed` per CRITICAL cluster, all 6 retests `fixed`, `pytest tests/ -q`
709 passed / 9 skipped, completion gate 4/4 W-satisfied + 0 open.

