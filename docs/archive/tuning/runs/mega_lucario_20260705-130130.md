# Tuning run — mega_lucario

- **When:** 2026-07-05T13:01:30  ·  **Build:** `mega_lucario_20260703_194dc40-dirty`  ·  **Corrections:** 8
- **Conservatism (`reg`):** 0.25  ·  see [how this works](../../../tuning/methodology.md)
- **Weight fit:** 8/8 ranking constraints satisfied — authored weights **kept** (the fit could not satisfy more without overfitting) (the authored seeds alone satisfy 8).

## Weights tuned
- _None._ The fit ships a weight change only when it satisfies **strictly more** corrections than the authored seeds; here it did not, so the legible priors are kept. (Lower `--reg` to let clean corrections move weights more; the ladder A/B is the real test.) The leverage this round is in the new rules below.

## New rules proposed (need a human-authored `when()`)
- _None._

## Corrections that need a rule, not a weight (unsatisfied)
_The weight fit cannot make `correct ≻ chosen` here by reweighting existing hypotheses — they are contradictory, or the distinction needs a new rule._
- _None._

## Already reviewed (excluded from fresh work)
_Assessed in an earlier round and recorded in `data/corrections/reviewed.json` — not re-surfaced here. `refuted` are also dropped from the weight fit._
- ep 83661652 f3 **covered** `Meowth ex` → `Meowth ex` — “FIXED on investigation (not a mislabel): the Pilot literally could not DECLINE an optional single-pick (decide() always took maxCount, ignoring minCount=0). Added (a) single-pick take-fewer — decline a score<0 pick at a minCount=0 select; (b) dont-pre-bench-the-supporter-tutor (-15) at _SETUP_BENCH. decide(f3)=[0]->[] (declines benching Meowth ex — saved for the Active Spot / an in-game bench to trigger Last-Ditch Catch). W-route can't measure it (correct==chosen==[0] tag can't encode select-nothing); gated by tests/strategy/test_setup_bench_decline.py on setup_bench_decline_f3.json. Human reversed the refuted call 2026-07-04.”
- ep 83661652 f19 **covered** `Play Boss’s Orders` → `Attach Basic {F} Energy → Lunatone (active · 110/110 · 1⚡)` — “develop-a-basic-in-setup develops Riolu over the poor Boss's Orders stall (retest [4]->[5]); W-route-blind. The poor-stall blunder is gone.”
- ep 83661652 f30 **covered** `Riolu, Makuhita` → `Lillie's Determination` — “CRITICAL 'dont discard our main line attacker': keep-line-base-at-discard covers it — real Pilot decide() at the captured f30 discards Gravity Mountain + a spare Basic {F} (Aura Jab reloads F from discard, so it is recoverable engine fuel) and KEEPS Riolu/Makuhita/Solrock. The flagged blunder (pitching Riolu) no longer occurs; retest differs from correct=[0] only in pitching a fuel-able Energy over a redundant Lillie's. Confirms f31's covered premise (Riolu no longer discarded).”
- ep 83661652 f31 **covered** `Riolu` → `Mega Lucario ex` — “Root is the f30 discard (now prevented by keep-line-base-at-discard): with Riolu no longer pitched, the discard->refetch waste never arises. On the isolated captured state (Riolu already discarded) grabbing the base is sound fetch-base-before-stranded-payoff doctrine.”
- ep 83661652 f33 **covered** `Attack with Power Gem` → `Play Riolu` — “develop-a-basic-in-setup: real Pilot develops a basic (Solrock) before the Meowth Power Gem chip (retest [3]->[1]); _finish_turn_last tiers the tier-0 develop before the tier-4 attack. First-dev-differs; W-route-blind (chip outscores the develop).”
- ep 83661652 f44 **covered** `Attack with Power Gem` → `Play Riolu` — “develop-a-basic-in-setup: real Pilot develops a basic (Makuhita) before the Meowth chip (retest [3]->[0]); same first-dev-differs as f33. The 'resisting to play basics' blunder is resolved.”


## Authored this round

**One deck-agnostic rule fixes the whole ep83661652 turn 5-7 cascade** (three CRITICAL corrections),
plus a meta-finding about the pipeline's open-set measure.

### `develop-the-wincon-base-first` (+6, `baseline/baseline_bench.py`, status=assumed)
- **Fixes cluster:** ep83661652 **f33 / f40 / f44** (all CRITICAL).
- **Rationale:** `develop-a-basic-in-setup` (+12) is INDIFFERENT among Basics, so *which* Basic got
  benched fell to `_finish_turn_last`'s option-index tiebreak — the Pilot benched whichever Basic sat
  lowest in the menu. Riolu (the wincon Line pre-evolution, Riolu → Mega Lucario ex) sat at a higher
  index than Solrock / Makuhita, so it lost on pure array position and never got down — the deck
  "played terribly" (feeding Meowth, just attacking, shuffling Riolu away). The rule gives the
  win-condition Line base (`card_is_line_preevo`) a faint edge (18 vs the other Basics' 12) so it
  leads the develop tiebreak. Deck-agnostic: `card_is_line_preevo` is `{Riolu}` here, `{Staryu}` for
  Starmie, `{Dreepy}` for Dragapult — NEVER an off-line support Basic; a NO-OP for a deck whose only
  setup Basic already is the Line base (mega_starmie).
- **Seed weight:** +6, band 0–5 "faint tiebreaker — all else equal, lean this way" (rides on top of
  the +12 develop; a real KO still dominates, KO_SCORE).
- **Retest (real `decide()`, the gate — this is a `_finish_turn_last` tier-sequencing fix, not a
  ranking margin, so the W-route Verifier cannot gate it; the retest + suite are the gate):**
  | frame | chosen_before → after | correct | fixed |
  |---|---|---|---|
  | f33 | `[3]` (Attack) → `[2]` (Play Riolu) | `[2]` | ✅ |
  | f40 | `[0]` (Lillie's — shuffles Riolu) → `[3]` (Play Riolu) | `[3]` | ✅ |
  | f44 | `[3]` (Attack) → `[2]` (Play Riolu) | `[2]` | ✅ |
  | f29 | `[3]` (Ultra Ball) → `[0]` (Play Riolu) | `[0]` | ✅ (reinforced) |
- **Verifier:** `regressed=[]` (the +6 breaks no other correction's ranking constraint;
  `newly_fixed` includes f29 in the W-route too). `cluster_satisfied=False` is EXPECTED — f33/f44's
  live-chosen is the attack, which the raw-score W-route ranks above a tier-0 develop.
- **Suite:** `1307 passed, 1 skipped`. New gate: `tests/strategy/test_blunder_20260703_develop_wincon_base.py`
  (3 replay fixtures + a negative), `tests/strategy/test_baseline_clusters.py` bench registry updated.

### Terminal-outcome ledger — all 14 corrections (real-Pilot re-measured, not W-route-assumed)
| ep-frame | CRITICAL | outcome | evidence |
|---|---|---|---|
| 83661652 f3  | ✓ | covered | single-pick decline; `test_setup_bench_decline` |
| 83661652 f19 |   | covered | plays Riolu (avoids poor Boss's Orders); reviewed |
| 83661652 f29 |   | fixed (existing + reinforced) | develop-a-basic → Riolu; retest FIXED |
| 83661652 f30 | ✓ | covered | keep-line-base-at-discard keeps Riolu (pitches fuel-able F); ledgered |
| 83661652 f31 | ✓ | covered | f30 premise holds (Riolu kept) → refetch never arises; reviewed |
| 83661652 f33 | ✓ | **fixed** | `develop-the-wincon-base-first`; retest [3]→[2] |
| 83661652 f40 | ✓ | **fixed** | `develop-the-wincon-base-first`; retest [0]→[3] |
| 83661652 f44 | ✓ | **fixed** | `develop-the-wincon-base-first`; retest [3]→[2] |
| 83966336 f9  | ✓ | fixed (prior round) | fetch-energy-when-starved; `test_blunder_20260705` |
| 83966336 f14 | ✓ | fixed (prior round) | dont-play-damage-boost-when-cant-attack |
| 83966336 f27 | ✓ | fixed (prior round) | demote-needless-search-supporter-in-setup |
| 83966336 f44 | ✓ | fixed (prior round) | refill on undeployable wincon; wincon_in_hand_undeployable |
| 83967841 f14 | ✓ | fixed (prior round) | fetch-energy-when-starved |
| 83967841 f17 | ✓ | fixed (prior round) | dont-costly-tutor-when-starved-and-developed |

**Open set empty** (completion gate): `tune.py` reports 0 proposals, 0 skipped, 0 UNSATISFIED, 8/8
W-route satisfied, 6 reviewed(excluded).

> **Deep-dive (why the pipeline said "done" while the agent blundered):** see
> [`docs/tuning/deck-agnostic-general-rules.md`](../deck-agnostic-general-rules.md).
