# Covered-disposition audit — the worklist

> **GENERATED — do not hand-edit.** `python tools/train/reviewed_audit.py --emit-report`.
> Issue #238, ADR-0114 decision 4. Regenerate after any ledger or rung change.

A `reviewed.json` closure is a claim about the shipped agent. When the rule it names is
deleted the claim expires silently, because the ledger stores its justification as opaque
prose. Every row below is an entry whose justification names a rung that **no longer
exists**. A row is *not* a finding that the frame is misplayed — it is a finding that the
**stated reason for closing it is gone**, so the closure has never been re-examined.

## How to use this

Open the frame — `python tools/train/frame_view.py <ledger key>` — and rule it on its own
merits, independent of the vanished rung (Issue #238 items 1-3, which are a human ruling and
are deliberately NOT automated). Then either re-close it against a rule that exists
(`python tools/train/review_correction.py <key> covered "<why>"`) or route it through the
current taxonomy. Once it stops being flagged, delete its line from
`data/corrections/reviewed_audit_allowlist.json`. The *what it became* column is there to
make that ruling cheap: it is the fold map's own statement of what replaced the rung.

## Tally

* ledger entries: **142**
* entries naming a retired rung: **60**
* by disposition: `covered` **50**, `deferred` **1**, `fixed` **2**, `refuted` **7**
* live rung vocabulary: **95** `Hypothesis(id=…)` in `src/` (+ **15** `SoundRule(id=…)`)
* retired rung vocabulary: **96**
* distinct rungs implicated: **25**
* tokens that resolved to NO rung (the vocabulary's blind spot): **242** distinct, **415** occurrences. Top: `attack-last` ×46, `w-route` ×18, `tier-2` ×11, `tier-0` ×10, `first-dev-differs` ×7, `forgo-ko` ×5, `tier-4` ×5, `pre-evo` ×4

The blind-spot count is reported rather than suppressed. The most frequent unresolved token
is `attack-last`, which is not a rung at all — it is the Pilot's structural resequencing
(`_finish_turn_last`). A loose `[a-z-]+` scan would have flagged every note that mentions it.

## `covered` — 50 entries

| ledger key | disposition | dead rung(s) | what it became | live rule(s) still named | the note, verbatim |
|---|---|---|---|---|---|
| `81906131-25` | covered | `dont-waste-discard-energy` | `dont-waste-discard-energy` → the BURST discipline: honest printed provision, the evaporation loss (an uncashed one-shot scores MINUS its worth, so End beats torching an Ignition on turn 1), the… | — | degenerate record (chosen == correct); the intent is already handled by dont-waste-discard-energy |
| `82224509-67` | covered | `play-energy-denial` | `play-energy-denial` → RETIRED 2026-07-14 (ADR-0062): `play-energy-denial` (+20 flat). | `dig-before-commit` | Deferral reason ('needs reactive-disruption/Posture') is now obsolete — the new play-energy-denial Hypothesis supplies it and FIRES (+20 on Crushing Hammer cid 1120, gated by opp_has_energy_in_play). Single-frame retest reports fixed=False (chosen [7]Attack->[0]) ONLY because dig-before-commit ties play-energy-denial at +20 and the tie-break picks the dig as the literal first action; the Hammer (idx 5) is tier-0 endorsed and both attacks (tac 119.9/209.7 < 1000=KO_SCORE, neither a KO) are tier-2 deferred, so attack-last plays the Hammer before the attack this turn — the human's intent ('use crushing hammer'). First-dev-differs (cf 81785223-32/44, also covered at fixed=False). Covered by play-energy-denial + attack-last sequencing. |
| `82226759-64` | covered | `play-harlequin-vs-hand-size` | `play-harlequin-vs-hand-size` → RETIRED 2026-08-02 (ADR-0102, Issue #261 item 2c): `play-harlequin-vs-hand-size` (+25) and `disrupt-when-unfavored` (+18). | — | play-harlequin-vs-hand-size endorses Harlequin (45, opp Alakazam line) and the agent plays it this turn; the free Crushing Hammer sequences first (tier-0), then Harlequin (tier-3), then the KO (attack-last) |
| `82224509-41` | covered | `retreat-to-ready-attacker` | `retreat-to-ready-attacker` → EMERGENT from destination value minus retreat cost: a ready benched attacker simply scores more `my_yield` than the Active's own option, and a transient-locked attack… | — | retreat-to-ready-attacker fires; attack-last develops first then retreats to the powered bench Mega (same turn) — blunder fixed, first action differs |
| `82226116-48` | covered | `retreat-to-ready-attacker` | `retreat-to-ready-attacker` → EMERGENT from destination value minus retreat cost: a ready benched attacker simply scores more `my_yield` than the Active's own option, and a transient-locked attack… | — | retreat-to-ready-attacker fires; agent evolves/develops then retreats to the powered Mega — blunder fixed (no more chipping with spent Cinderace) |
| `82228640-25` | covered | `concentrate-energy-on-wincon` `prefer-reusable-over-burst` | `concentrate-energy-on-wincon` → the ATTACK AXIS (`max(this_turn, build, accel_value)`): tonight's counterfactual under the full Attach Budget, typed convex build toward the line payoff, and the forward…<br>`prefer-reusable-over-burst` → the BURST discipline: honest printed provision, the evaporation loss (an uncashed one-shot scores MINUS its worth, so End beats torching an Ignition on turn 1), the… | — | concentrate-energy-on-wincon + prefer-reusable-over-burst + conserve-ignition: agent attaches a Basic to the active Mega (board-identical to recorded option); Ignition->active now dominated (989<1031) |
| `81785223-38` | covered | `evolve-into-wincon` | `evolve-into-wincon` → The four rungs that PRICED an evolve are DELETED, not suppressed: `evolve-into-wincon` (+40) and `advance-the-evolution-line` (+15) are the decider's deploy term, and… | — | attack-last defers the Attack; real Pilot evolves the wincon first (evolve-into-wincon), Pokegear-dig endorsed - same turn |
| `81903490-27` | covered | `dont-waste-discard-energy` | `dont-waste-discard-energy` → the BURST discipline: honest printed provision, the evaporation loss (an uncashed one-shot scores MINUS its worth, so End beats torching an Ignition on turn 1), the… | — | dont-waste-discard-energy sinks Ignition->Staryu to -20 (below End); Basic->Staryu +10 - real Pilot digs first then attaches Basic, never Ignition |
| `81903490-49` | covered | `dont-waste-discard-energy` | `dont-waste-discard-energy` → the BURST discipline: honest printed provision, the evaporation loss (an uncashed one-shot scores MINUS its worth, so End beats torching an Ignition on turn 1), the… | — | dont-waste-discard-energy sinks Ignition->Cinderace to -20; Basic->Cinderace +10 - attaches Basic not Ignition |
| `81903490-74` | covered | `evolve-into-wincon` | `evolve-into-wincon` → The four rungs that PRICED an evolve are DELETED, not suppressed: `evolve-into-wincon` (+40) and `advance-the-evolution-line` (+15) are the decider's deploy term, and… | — | evolve-into-wincon (40) outranks the deferred Attack (39.9); real Pilot plays a rush-evolve tutor then evolves Mega Starmie ex - same turn |
| `81904064-29` | covered | `evolve-into-wincon` | `evolve-into-wincon` → The four rungs that PRICED an evolve are DELETED, not suppressed: `evolve-into-wincon` (+40) and `advance-the-evolution-line` (+15) are the decider's deploy term, and… | `hold-clutch-heal` | hold-clutch-heal rewards Wally's Compassion ONLY when active_doomed; here not doomed -> Wally's scores 0, dominated by evolve-into-wincon + digs incl the human's Lillie's. Rule encodes the 'save it for clutch' correction exactly |
| `81904451-6` | covered | `dont-waste-discard-energy` | `dont-waste-discard-energy` → the BURST discipline: honest printed provision, the evaporation loss (an uncashed one-shot scores MINUS its worth, so End beats torching an Ignition on turn 1), the… | — | dont-waste-discard-energy sinks Ignition->Cinderace to -20; Basic->Cinderace +10 - develops then attaches Basic |
| `81904451-50` | covered | `dont-waste-discard-energy` `tutor-the-wincon` | `dont-waste-discard-energy` → the BURST discipline: honest printed provision, the evaporation loss (an uncashed one-shot scores MINUS its worth, so End beats torching an Ignition on turn 1), the…<br>`tutor-the-wincon` → play-a-tutor-for-the-unfound-wincon doctrine_fetch never-fetch-cinderace -> dont-fetch-the-setup-only-opener doctrine_fetch (opener tag + stranded-evolution guard: no… | — | dont-waste-discard-energy sinks Ignition->Cinderace to -35 (dead last); Hilda endorsed via tutor-the-wincon (45) and played |
| `81905522-47` | covered | `power-up-attacker` | `power-up-attacker` → the ATTACK AXIS (`max(this_turn, build, accel_value)`): tonight's counterfactual under the full Attach Budget, typed convex build toward the line payoff, and the forward… | — | attack-last defers the Attack; real Pilot evolves the wincon first; Basic-attach (power-up-attacker 15) outranks the Ignition attaches (-45/-60) |
| `82523811-95` | covered | `build-active-wincon` | `build-active-wincon` → the ATTACK AXIS (`max(this_turn, build, accel_value)`): tonight's counterfactual under the full Attach Budget, typed convex build toward the line payoff, and the forward… | — | Real Pilot decide() now picks the attach (build-active-wincon fires +20 on the active win-condition under its max attack cost; retest chosen [1]->[0]=correct). W-route flags it unsatisfiable only because the alternative is a KO attack (tac 1000.9) that dominates by score — but _finish_turn_last sequences the tier-1 attach BEFORE the tier-2 KO, so the energy is attached this turn and the KO still lands next frame (attack-last). Covered by build-active-wincon + attack-last sequencing. |
| `82524455-27` | covered | `build-active-wincon` | `build-active-wincon` → the ATTACK AXIS (`max(this_turn, build, accel_value)`): tonight's counterfactual under the full Attach Budget, typed convex build toward the line payoff, and the forward… | — | Real Pilot decide() now picks the attach (build-active-wincon fires +20; retest chosen [3]->[2]=correct). W-route unsatisfiable because the alternative is a KO attack dominating by score; _finish_turn_last sequences the tier-1 attach before the tier-2 attack so the energy is attached before the turn ends. Covered by build-active-wincon + attack-last sequencing. |
| `82523811-15` | covered | `play-energy-denial` | `play-energy-denial` → RETIRED 2026-07-14 (ADR-0062): `play-energy-denial` (+20 flat). | — | Real Pilot decide() now plays Crushing Hammer before attacking (new play-energy-denial fires +20: energy_denial tag + opponent's Active carries Energy; retest chosen [4]->[3]=correct). SETUP-phase strip of a developing Riolu before it becomes Mega Lucario ex. W-route satisfiable here (the competing option is only a 20-dmg chip, < the +20 rule). Covered by play-energy-denial + attack-last sequencing. |
| `82523811-79` | covered | `play-energy-denial` | `play-energy-denial` → RETIRED 2026-07-14 (ADR-0062): `play-energy-denial` (+20 flat). | — | Real Pilot decide() now plays Crushing Hammer before attacking (play-energy-denial fires +20; retest chosen [1]->[0]=correct). W-route flags it unsatisfiable only because the alternative is a 120-dmg attack (tac 119.9) that dominates by score — but _finish_turn_last sequences the tier-0 Item PLAY BEFORE the tier-2 attack, so the strip lands this turn and the attack still follows (attack-last). Covered by play-energy-denial + attack-last sequencing. |
| `82525101-92` | covered | `play-energy-denial` | `play-energy-denial` → RETIRED 2026-07-14 (ADR-0062): `play-energy-denial` (+20 flat). | — | Real Pilot decide() now plays Crushing Hammer before attacking (play-energy-denial fires +20; retest chosen [3]->[0]=correct). W-route unsatisfiable because the alternative is a 120-dmg attack dominating by score; _finish_turn_last sequences the tier-0 Item PLAY before the tier-2 attack (both Crushing Hammers then the attack — same turn). Covered by play-energy-denial + attack-last sequencing. |
| `82525101-102` | covered | `play-energy-denial` | `play-energy-denial` → RETIRED 2026-07-14 (ADR-0062): `play-energy-denial` (+20 flat). | — | Real Pilot decide() now plays Crushing Hammer before attacking (play-energy-denial fires +20; retest chosen [2]->[0]=correct). W-route unsatisfiable because the alternative is a 120-dmg attack dominating by score; _finish_turn_last sequences the tier-0 Item PLAY before the tier-2 attack. Covered by play-energy-denial + attack-last sequencing. |
| `81906755-77` | covered | `play-energy-denial` | `play-energy-denial` → RETIRED 2026-07-14 (ADR-0062): `play-energy-denial` (+20 flat). | — | attack-last: real Pilot develops first (Crushing Hammer play-energy-denial, then Salvatore), attack deferred — retest [6]->[1], first-dev-differs (human wanted Salvatore; both tier-0 same turn) |
| `82224509-29` | covered | `evolve-into-wincon` | `evolve-into-wincon` → The four rungs that PRICED an evolve are DELETED, not suppressed: `evolve-into-wincon` (+40) and `advance-the-evolution-line` (+15) are the decider's deploy term, and… | — | attack-last: real Pilot evolves bench Staryu->Mega first (evolve-into-wincon), KO deferred — retest [6]->[4]=correct |
| `82226116-70` | covered | `evolve-into-wincon` | `evolve-into-wincon` → The four rungs that PRICED an evolve are DELETED, not suppressed: `evolve-into-wincon` (+40) and `advance-the-evolution-line` (+15) are the decider's deploy term, and… | — | attack-last: real Pilot evolves bench Staryu->Mega first (evolve-into-wincon), KO deferred — retest [14]->[11]=correct |
| `82226759-16` | covered | `evolve-into-wincon` | `evolve-into-wincon` → The four rungs that PRICED an evolve are DELETED, not suppressed: `evolve-into-wincon` (+40) and `advance-the-evolution-line` (+15) are the decider's deploy term, and… | — | attack-last: real Pilot evolves bench Staryu->Mega first (evolve-into-wincon) — retest [2]->[0]=correct |
| `82226759-29` | covered | `evolve-into-wincon` | `evolve-into-wincon` → The four rungs that PRICED an evolve are DELETED, not suppressed: `evolve-into-wincon` (+40) and `advance-the-evolution-line` (+15) are the decider's deploy term, and… | — | attack-last: real Pilot evolves bench Staryu->Mega first (evolve-into-wincon) — retest [4]->[0]=correct |
| `82748522-15` | covered | `hold-wincon-dont-shuffle` | `hold-wincon-dont-shuffle` → `hold-wincon-dont-shuffle` (−25), `hold-line-piece-dont-shuffle` (−25) and `hold-wincon-with-base-dont-shuffle` (−15) RETIRED 2026-07-18 (ADR-0065): they were the flat… | `attach-before-hand-shuffle` | hold-wincon-dont-shuffle + attach-before-hand-shuffle soundly suppress Lillie's (2 undeployed Megas + unattached energy in hand); agent tutors with Hilda — don't shuffle away your only wincons |
| `82750161-59` | covered | `concentrate-energy-on-wincon` `dont-waste-discard-energy` | `concentrate-energy-on-wincon` → the ATTACK AXIS (`max(this_turn, build, accel_value)`): tonight's counterfactual under the full Attach Budget, typed convex build toward the line payoff, and the forward…<br>`dont-waste-discard-energy` → the BURST discipline: honest printed provision, the evaporation loss (an uncashed one-shot scores MINUS its worth, so End beats torching an Ignition on turn 1), the… | — | concentrate-energy-on-wincon: agent attaches a reusable Basic to the 1-energy benched Mega (priority wincon); recorded Ignition->bench correctly avoided (dont-waste-discard-energy). Same target, better energy |
| `82752045-97` | covered | `concentrate-energy-on-wincon` | `concentrate-energy-on-wincon` → the ATTACK AXIS (`max(this_turn, build, accel_value)`): tonight's counterfactual under the full Attach Budget, typed convex build toward the line payoff, and the forward… | — | concentrate-energy-on-wincon: agent attaches Basic to the most-built benched Mega over the bare one; recorded Ignition->that-Mega avoided. Concentrate-don't-spread realized |
| `82753102-37` | covered | `hold-wincon-dont-shuffle` `play-energy-denial` | `hold-wincon-dont-shuffle` → `hold-wincon-dont-shuffle` (−25), `hold-line-piece-dont-shuffle` (−25) and `hold-wincon-with-base-dont-shuffle` (−15) RETIRED 2026-07-18 (ADR-0065): they were the flat…<br>`play-energy-denial` → RETIRED 2026-07-14 (ADR-0062): `play-energy-denial` (+20 flat). | — | CRITICAL fixed: play-energy-denial gates on opp_active_has_energy (Kadabra active 0 energy) so Crushing Hammer no longer played; Harlequin soundly suppressed by hold-wincon-dont-shuffle (2 undeployed Megas) — agent develops instead |
| `82754875-8` | covered | `tutor-the-wincon` | `tutor-the-wincon` → play-a-tutor-for-the-unfound-wincon doctrine_fetch never-fetch-cinderace -> dont-fetch-the-setup-only-opener doctrine_fetch (opener tag + stranded-evolution guard: no… | `fetch-when-it-fills-a-need` | agent tutors the win-condition via Mega Signal (53: fetch-when-it-fills-a-need + tutor-the-wincon); bulk-draw-to-develop (Lillie's) is a supporter-priority judgment for the value model |
| `82748422-26` | covered | `play-energy-denial` | `play-energy-denial` → RETIRED 2026-07-14 (ADR-0062): `play-energy-denial` (+20 flat). | — | play-energy-denial stands down when active_cheap_attack_kos (Jetting Blow KOs the Active): the strip is moot, Crushing Hammer no longer played; agent develops then takes the KO (attack-last) |
| `82749168-21` | covered | `play-energy-denial` | `play-energy-denial` → RETIRED 2026-07-14 (ADR-0062): `play-energy-denial` (+20 flat). | — | play-energy-denial gates on opp_active_has_energy: opp Active energyless so no Crushing Hammer; Pilot attacks (retest [1]->[7]=correct) |
| `82752045-80` | covered | `concentrate-energy-on-wincon` | `concentrate-energy-on-wincon` → the ATTACK AXIS (`max(this_turn, build, accel_value)`): tonight's counterfactual under the full Attach Budget, typed convex build toward the line payoff, and the forward… | — | develop-before-KO already covered by concentrate-energy-on-wincon for evolved benched wincons; here the bench body is a pre-evo and the dev is a marginal Night Stretcher recycle ('might as well') - Pilot takes the prize; value-model judgment, no rule |
| `82752604-61` | covered | `concentrate-energy-on-wincon` | `concentrate-energy-on-wincon` → the ATTACK AXIS (`max(this_turn, build, accel_value)`): tonight's counterfactual under the full Attach Budget, typed convex build toward the line payoff, and the forward… | — | same as f80: concentrate-energy-on-wincon covers the real develop-before-KO case; attaching to a bench pre-evo before fetching its evolution is speculative spread vs the concentrate doctrine - Pilot takes the prize; value-model judgment |
| `82754241-41` | covered | `play-energy-denial` | `play-energy-denial` → RETIRED 2026-07-14 (ADR-0062): `play-energy-denial` (+20 flat). | — | play-energy-denial: wasted-Crushing-Hammer blunder resolved - Pilot no longer plays the Hammer, takes a KO attack instead (retest [0]->[2], a KO) |
| `82866415-43` | covered | `hold-irreplaceable-tool-dont-shuffle` | `hold-irreplaceable-tool-dont-shuffle` → `hold-irreplaceable-tool-dont-shuffle` (−30) RETIRED 2026-07-18 (ADR-0065): the graded refresh SHED (`pilot._refresh_shed_keepcost`) now prices a held ACE SPEC Tool at… | `deploy-hp-tool` | deploy-hp-tool + hold-irreplaceable-tool-dont-shuffle deploy the Cape (tier-2) and suppress Lillie's (-30); agent digs (Mega Signal) then attaches the Cape — the ACE SPEC is no longer shuffled away (ADR-0028, first-dev-differs) |
| `82867148-34` | covered | `develop-turbo-flare-recipient` | `develop-turbo-flare-recipient` → develop-the-accel-recipient baseline_bench tutor-the-wincon -> play-a-tutor-for-the-unfound-wincon doctrine_fetch never-fetch-cinderace ->… | `fetch-base-before-stranded-payoff` | degenerate (chosen==correct==Attack); the play-Ultra-Ball-to-develop-a-recipient intent is carried by develop-turbo-flare-recipient + fetch-base-before-stranded-payoff |
| `83007714-7` | covered | `dont-waste-discard-energy` | `dont-waste-discard-energy` → the BURST discipline: honest printed provision, the evaporation loss (an uncashed one-shot scores MINUS its worth, so End beats torching an Ignition on turn 1), the… | `prefer-active-attach-in-setup` | FIXED in the real Pilot by the new prefer-active-attach-in-setup (+8): energy now goes to the ACTIVE Cinderace, not the benched Staryu (the regression is gone; retest chosen [4]->[3]). W-route stays UNSATISFIED only because the literal correct labels Ignition->Cinderace, which is UNSOUND turn-1-going-first (Ignition is discard_eot -> discarded EOT unused; dont-waste-discard-energy sinks it -60), so the Pilot soundly attaches the reusable Basic->Cinderace [3] instead. Both put the energy on Cinderace = the human intent. Mirrors covered precedent 81903490-49. |
| `83007714-22` | covered | `spread-attach-to-the-needy` | `spread-attach-to-the-needy` → the ATTACK AXIS (`max(this_turn, build, accel_value)`): tonight's counterfactual under the full Attach Budget, typed convex build toward the line payoff, and the forward… | `prefer-active-attach-in-setup` | Degenerate record (chosen==correct==[1]); the real Pilot already picks the Mega Starmie ex over the Staryu at this Turbo-Flare AttachFrom via spread-attach-to-the-needy (+15, retest FIXED=True). The rationale's '3 energy on Staryu' complaint is the frame-7 manual misattach, whose root is fixed by prefer-active-attach-in-setup. |
| `83007714-135` | covered | `play-energy-denial` | `play-energy-denial` → RETIRED 2026-07-14 (ADR-0062): `play-energy-denial` (+20 flat). | — | attack-last (attack-is-turn-ender-develop-first): the match-winning Nebula Beam KO (tac 1002.7, 2 prizes = last 2) is DEFERRED behind play-energy-denial's Crushing Hammer but NEVER forfeited — _finish_turn_last tiers the tier-0 Item PLAY before the tier-4 attack, so the KO is taken same turn and the win lands. The 'take it immediately' is satisfied in outcome. Matches many ledger precedents (e.g. 82525101 f92/f102). |
| `83116501-89` | covered | `concentrate-energy-on-wincon` | `concentrate-energy-on-wincon` → the ATTACK AXIS (`max(this_turn, build, accel_value)`): tonight's counterfactual under the full Attach Budget, typed convex build toward the line payoff, and the forward… | — | Fixed this round via the _priority_wincon_slot doomed-active skip: with the Active doomed (70HP Mega, 1e), the priority wincon slot now targets the HEALTHY benched Mega (bench0, 1e) instead of the doomed Active, so concentrate-energy-on-wincon (+25) fires there and the real Pilot attaches to bench0 (decide [6]->[5], concentrating) instead of spreading to the bare bench1. W-route stays UNSATISFIED only because the literal correct labels Ignition->bench0, which is UNSOUND (Ignition discard_eot wasted on a benched body); the Pilot soundly uses reusable Basic {W} to the same target = the human's concentrate intent. Mirrors precedent 82750161-59. Before/after diff: this change touches EXACTLY f89, zero other frames. |
| `83116081-17` | covered | `play-harlequin-vs-hand-size` | `play-harlequin-vs-hand-size` → RETIRED 2026-08-02 (ADR-0102, Issue #261 item 2c): `play-harlequin-vs-hand-size` (+25) and `disrupt-when-unfavored` (+18). | — | Value-model judgment (contradictory pair): correct wants Lillie's over Harlequin (both score 28, tie), but the covered ep83117367-34 wants Harlequin OVER Lillie's — the human's refresh-supporter preference reverses by matchup, so no single weight/rule satisfies both. play-harlequin-vs-hand-size already fires Harlequin vs hand-size decks; the residual Lillie's-vs-Harlequin against a non-hand-size opp is a Base-Value-Model call (ADR-0007), not a weight gap. Both are sound turn-2 hand refreshes. |
| `82756664-74` | covered | `conserve-burst-when-no-ko` | `conserve-burst-when-no-ko` → the BURST discipline: honest printed provision, the evaporation loss (an uncashed one-shot scores MINUS its worth, so End beats torching an Ignition on turn 1), the… | `dig-before-commit` | Value-model / dig-before-commit: real Pilot plays Hilda (informative tutor +20) then attaches same turn (attaches are tier-2, sequenced after the supporter) — energy IS attached this turn. Whether flexible Hilda is 'pointless' given energy-in-hand + a prized Mega is a hand-quality read for the Base Value Model (Hilda finds Staryu/energy/any Pokemon, not a wincon-only tutor, so search_targets_exhausted exempts it). The literal correct [3] labels Ignition->active on a 30hp doomed active that can't KO even maxed; conserve-burst-when-no-ko soundly de-prioritizes that wasteful burst toward a reusable Basic — the W-route 'unsatisfied' is the sound rule declining the burst label, NOT a behavior regression (decide unchanged [1] before/after; corpus diff touched only f70). |
| `83661652-31` | covered | `keep-line-base-at-discard` | `keep-line-base-at-discard` → — | `fetch-base-before-stranded-payoff` | Root is the f30 discard (now prevented by keep-line-base-at-discard): with Riolu no longer pitched, the discard->refetch waste never arises. On the isolated captured state (Riolu already discarded) grabbing the base is sound fetch-base-before-stranded-payoff doctrine. |
| `83661652-33` | covered | `develop-the-wincon-base-first` | `develop-the-wincon-base-first` → It is pinned so the equation reproduces the incumbent range of the rungs it replaces — `develop-a-basic-in-setup` +12, `develop-the-wincon-base-first` +6 riding on it,… | — | CRITICAL 'dont feed Meowth ex, should have played solrock/riolu': NOW RULE-FIXED by develop-the-wincon-base-first (+6). The tier-0 develop already beat the tier-4 chip (_finish_turn_last); the new rule breaks the develop tie toward the wincon Line base, so real Pilot decide() picks Riolu (retest [3]->[2]=correct, FIXED). W-route-blind (chip outscores the develop in raw score). Gated by tests/strategy/test_blunder_20260703_develop_wincon_base.py. |
| `83661652-44` | covered | `develop-the-wincon-base-first` | `develop-the-wincon-base-first` → It is pinned so the equation reproduces the incumbent range of the rungs it replaces — `develop-a-basic-in-setup` +12, `develop-the-wincon-base-first` +6 riding on it,… | — | CRITICAL 'resisting to play basics, wanting to feed meowth and just attack': NOW RULE-FIXED by develop-the-wincon-base-first (+6). Same develop-tiebreak as f33 — real Pilot decide() benches Riolu over Makuhita (retest [3]->[2]=correct, FIXED). W-route-blind. Gated by the ml0703 develop_wincon replay test. |
| `83661652-3` | covered | `dont-pre-bench-the-supporter-tutor` | `dont-pre-bench-the-supporter-tutor` → `bench-the-supporter-tutor` (+25), `dont-pre-bench-the-supporter-tutor` (−15) and `dont-pre-bench-a-redundant-utility` (−15) stood here until ADR-0086. | — | FIXED on investigation (not a mislabel): the Pilot literally could not DECLINE an optional single-pick (decide() always took maxCount, ignoring minCount=0). Added (a) single-pick take-fewer — decline a score<0 pick at a minCount=0 select; (b) dont-pre-bench-the-supporter-tutor (-15) at _SETUP_BENCH. decide(f3)=[0]->[] (declines benching Meowth ex — saved for the Active Spot / an in-game bench to trigger Last-Ditch Catch). W-route can't measure it (correct==chosen==[0] tag can't encode select-nothing); gated by tests/strategy/test_setup_bench_decline.py on setup_bench_decline_f3.json. Human reversed the refuted call 2026-07-04. |
| `83661652-30` | covered | `keep-line-base-at-discard` | `keep-line-base-at-discard` → — | — | CRITICAL 'dont discard our main line attacker': keep-line-base-at-discard covers it — real Pilot decide() at the captured f30 discards Gravity Mountain + a spare Basic {F} (Aura Jab reloads F from discard, so it is recoverable engine fuel) and KEEPS Riolu/Makuhita/Solrock. The flagged blunder (pitching Riolu) no longer occurs; retest differs from correct=[0] only in pitching a fuel-able Energy over a redundant Lillie's. Confirms f31's covered premise (Riolu no longer discarded). |
| `85785067-14` | covered | `dont-play-switch-for-no-gain` `hold-position-in-setup` | `dont-play-switch-for-no-gain` → EMERGENT: a switch that gains nothing has a destination value no better than staying, and now pays the card's Worth as `retreat_cost`, so it nets below End without a…<br>`hold-position-in-setup` → DELETED on an emergence argument: a setup retreat pays real `retreat_cost` for a destination that is not yet worth promoting, so the equation should decline it on its… | — | CRITICAL 'dont switch out of a Solrock-active+Lunatone-benched setup': COVERED on the real shipped Pilot. retest_one decide()=[2]=End turn=correct (FIXED=True, margin 0->8). dont-play-switch-for-no-gain (-8) demotes Play Switch [0] + hold-position-in-setup (-25) demotes [1], so keeping Solrock Active wins. Stale on-disk open[] predates these rules (ledger last written 2026-07-13). |
| `85785606-1` | covered | `arm-the-doomed-active` `start-solrock-over-lunatone` | `arm-the-doomed-active` → the ATTACK AXIS (`max(this_turn, build, accel_value)`): tonight's counterfactual under the full Attach Budget, typed convex build toward the line payoff, and the forward…<br>`start-solrock-over-lunatone` → **The Set-Up ACTIVE pick is ONE rule reading ONE deck declaration (ADR-0079).** It used to be five rungs across two layers — `open-the-accelerator` (+40, `accel_source`… | `attach-solrock-over-line-base` | CRITICAL 'start attacking with Solrock, dont energy Riolu': COVERED (degenerate, chosen==correct==[0]) on the real Pilot. retest_one at the captured SetupActivePokemon frame decide()=[0]=Solrock=correct (FIXED=True) via start-solrock-over-lunatone (+12). The 'dont energy Riolu' intent is a separate attach decision (not this frame) already handled by attach-solrock-over-line-base + the general Active-arm rungs (arm-the-doomed-active on a live Cosmic Beam). |

## `deferred` — 1 entry

| ledger key | disposition | dead rung(s) | what it became | live rule(s) still named | the note, verbatim |
|---|---|---|---|---|---|
| `83667237-107` | deferred | `snipe-the-evolving-threat` | `snipe-the-evolving-threat` → `snipe-for-the-ko` (60), `snipe-the-evolving-threat` (45), `snipe-the-forced-promotion` (40), `snipe-the-top-threat` (30), `snipe-the-threat` (20) and… | — | discard-fuel snipe residual (grill 2026-07-22): bot snipes Lunatone on-path, human wants Makuhita (pre-evo of a discard-fueled Wild-Press line). snipe-the-evolving-threat stands down via target_forward_form_in_play (Hariyama already in play, ADR-0044) — but under 5 {F} in opp discard + Mega Lucario Aura Jab the 2nd line is live, so the pre-evo is NOT redundant. DoD: build opponent-side discard-fuel read (coverage item #2) then override the stand-down. Handoff: docs/plans/snipe-discard-fuel-grill-handoff.md. Open: strict-vs-broad trigger threshold. To be done in a separate session. |

## `fixed` — 2 entries

| ledger key | disposition | dead rung(s) | what it became | live rule(s) still named | the note, verbatim |
|---|---|---|---|---|---|
| `82225643-12` | fixed | `play-energy-denial` | `play-energy-denial` → RETIRED 2026-07-14 (ADR-0062): `play-energy-denial` (+20 flat). | `dig-before-commit`, `information-before-commitment` | USER-RULED ACCEPT 2026-08-02 (Issue #261 wave-2, item 2f). NON-VOIDING: the ruling stands and still grades. The agent now plays Pokegear 3.0 [0] where this record names Crushing Hammer [1], and that is the ADR-0095 decision-1 information-before-commitment boundary doing its job across TWO actions of ONE turn rather than contradicting the ruling. Three reasons, all measured: (1) the adjacent f11 ruling on the same turn is explicit -- 'This goes into the box of Collect information before committing. Do PokeGear first. Then, most likely, you'll also play Hammer and Ignition Energy in this same turn' -- so Pokegear-then-Hammer IS the ordering asked for; (2) this record's own live 'chosen' was [6] ATTACK, which ends the turn, so its rationale ('Rioulu would not have died from this attack ... playing the crushing hammers could have reduced its threat') adjudicates Hammer-vs-attack, not Hammer-vs-dig, and the Hammer is DEFERRED here (still endorsed at +22.50) rather than declined; (3) the Discrimination Gate independently reports this same frame IMPROVED MISS -> OK -- the leaf now ranks the human's Hammer TOP -- so the VALUATION agrees with the ruling and only the SEQUENCER re-orders, which is what a tier is for. data/decider_lab/baseline.json re-captured against this ruling; the leaf baseline deliberately NOT re-captured (that gate passes, and re-capturing a passing gate absorbs movement nobody ruled).  SUPERSEDES a 'covered' entry of 2026-06-29, preserved here verbatim rather than overwritten -- and it CORROBORATES the ruling above, having reached the same reading six weeks earlier from the opposite direction (it treated the dig-first pick as correct behaviour the retest merely mislabelled): "Deferral reason ('needs reactive-disruption/Posture') is now obsolete - play-energy-denial supplies it and FIRES (+20 on both Crushing Hammer copies idx 1/3: energy_denial tag + opponent's energized Riolu - strip it before it becomes Mega Lucario ex). Single-frame retest reports fixed=False (chosen [6]Attack->[0]) ONLY because dig-before-commit ties play-energy-denial at +20 and the tie-break picks the dig first; the Hammer is tier-0 endorsed and the 49.9-dmg attack (< 1000=KO_SCORE) is tier-2 deferred, so attack-last plays the Hammer before the attack this turn - the human's intent (slow the Mega Lucario line). First-dev-differs (cf 81785223-32/44). Covered by play-energy-denial + attack-last sequencing." Two of its details have since expired and are corrected rather than left to mislead: `play-energy-denial` (+20) was RETIRED by ADR-0062, and the tie it describes was a TIE broken by menu position, where the boundary now orders the two on a board fact. |
| `85046350-20` | fixed | `hold-position-in-setup` | `hold-position-in-setup` → DELETED on an emergence argument: a setup retreat pays real `retreat_cost` for a destination that is not yet worth promoting, so the equation should decline it on its… | `feed-the-line-for-disruptor-lock`, `retreat-to-wall-the-line` | BUILT 2026-07-13 (disruptor_lock_maneuver, default ON, kill-switched). Diagnosed at build time: the shipped retreat rung's premise (can_wall_line_with_disruptor) requires the opp Active to threaten damage, which is FALSE here (0-Energy Gible) -- so f20 is OFFENSIVE item-lock disruption (deny the opp's T2 Item turn), not defensive walling. New signal can_lock_line_with_disruptor (early game + fragile line-preevo Active + benched item_lock opener + reachable cheap retreat + Active can't KO); new rung feed-the-line-for-disruptor-lock (+20) endorses the active-Dreepy attach so it can retreat into Budew; retreat-to-wall-the-line + hold-position-in-setup broadened to the offensive signal so the follow-up retreat completes the maneuver. decide()=[0] (active), was [1] (bench). SHIP-AND-REFINE per user decision 2026-07-13: value is matchup-dependent (a fragile 30-HP Budew may concede a prize) -- kill-switch is the lever. Zero dragapult-corpus regression. Tests in test_blunder_20260710_split_fixes.py. |

## `refuted` — 7: flagged, but NOT blockers (ADR-0114 decision 6)

A refuted ruling owes no fix either way, so a dead rung in its refutation note costs
nothing operationally. They are listed because the refutation rests on the same
vanished premise, and Issue #238 asked for them to be re-read on that basis.

| ledger key | disposition | dead rung(s) | what it became | live rule(s) still named | the note, verbatim |
|---|---|---|---|---|---|
| `81785223-44` | refuted | `evolve-into-wincon` | `evolve-into-wincon` → The four rungs that PRICED an evolve are DELETED, not suppressed: `evolve-into-wincon` (+40) and `advance-the-evolution-line` (+15) are the decider's deploy term, and… | — | Wave-3 ruling (Issue #262): same board and same call as 81785223-32 - Pokegear is not the play, the evolve-into-wincon line is. Label disowned; supersedes the covered 'Pokegear-dig endorsed' ruling. [same snipe-target correction as 81785223-32.] |
| `82523811-59` | refuted | `build-active-wincon` | `build-active-wincon` → the ATTACK AXIS (`max(this_turn, build, accel_value)`): tonight's counterfactual under the full Attach Budget, typed convex build toward the line payoff, and the forward… | — | Mis-tagged: chosen [2] and correct [6] both attach a Basic {W} Energy to the SAME benched Mega Starmie (bench0) — board-identical, no real blunder as encoded (different identical W cards). The rationale's true intent (attach a 2nd energy to the ACTIVE toward Nebula Beam) is now realized by the new build-active-wincon Hypothesis, which makes the agent attach to the active win-condition here. |
| `82525741-81` | refuted | `build-active-wincon` | `build-active-wincon` → the ATTACK AXIS (`max(this_turn, build, accel_value)`): tonight's counterfactual under the full Attach Budget, typed convex build toward the line payoff, and the forward… | — | Mis-tagged: correct [2] == chosen [2] (both attach {W} to bench0) — structurally unsatisfiable (no ranking constraint). Rationale's intent (2nd energy to the ACTIVE toward Nebula Beam) is now realized by build-active-wincon, which makes the agent attach to the active win-condition here. |
| `82224509-31` | refuted | `spread-attach-to-the-needy` | `spread-attach-to-the-needy` → the ATTACK AXIS (`max(this_turn, build, accel_value)`): tonight's counterfactual under the full Attach Budget, typed convex build toward the line payoff, and the forward… | — | mis-tagged: chosen==correct==[0] (structurally unsatisfiable); correct [0] is the over-attach (3-energy Mega). Rationale's intent (attach to the bare Staryu) now realized by the new spread-attach-to-the-needy rule — retest chosen [0]->[1]=bare Staryu |
| `82867148-87` | refuted | `dont-waste-discard-energy` | `dont-waste-discard-energy` → the BURST discipline: honest printed provision, the evaporation loss (an uncashed one-shot scores MINUS its worth, so End beats torching an Ignition on turn 1), the… | — | literal Ignition->Staryu dominated by reusable Water->Staryu (dont-waste-discard-energy soundly avoids wasting the one-shot Ignition on a non-attacking bench body); the intent (energy to the Staryu line base over off-line Cinderace) is realized by the W-route-invisible attach_to_needy_line tie-break, retest [7]->[8]=Water->Staryu |
| `85058574-114` | refuted | `attach-energy-last` | `attach-energy-last` → a decide()-only ORDERING deferral. | — | CRITICAL, human-acknowledged 2026-07-10. Spec does not survive the board: the Bench IS full (5), but Poke Pad can still fetch a 2nd Hariyama that evolves the benched Makuhita, so 'a tutor with nothing to find' is FALSE here. Reproducing the tagged correct=[1] (the Energy attach) would additionally require suppressing a perfectly reasonable Fighting Gong, and the attach is deferred by attach-energy-last (reordered=True) by design anyway. The human's real reason -- 'Poke Pad is Ultra Ball fodder' -- is a value judgment about card economy, not a nothing-to-find test. Needs a retag, not code. |
| `82749168-38` | refuted | `dont-snipe-a-benched-tera` | `dont-snipe-a-benched-tera` → Backs the STRUCTURAL `_snipe_tera_veto` (KO_SCORE-class, Tactical) — it superseded the tuner-mutable `dont-snipe-a-benched-tera` (−60), which a positional stack could… | — | correct=[0] snipes the benched Dragapult ex, a [Tera] ex that takes NO attack damage while Benched (data/EN_Card_Data.csv id121; docs/rulebook.txt Tera rule) -- the 50 Jetting Blow bench rider does literally 0 there. dont-snipe-a-benched-tera (-60) already ranks it below the real 50-dmg Hoothoot snipe; retest 82749168-38: [0]=-18 vs chosen [1]=12. The correction forgoes the attack's entire snipe effect on a damage-immune body. |

## Reconciliation against Issue #238's own lists

Acceptance criterion 5. Every count below is derived from this run, not transcribed.

* **body, the 13** — flagged 13/13.
* **body, the 3 `refuted` re-reads** — flagged 3/3.
* **comment, the 14 (`<ep>|<seat>|decision|<frame>` → `<ep>-<frame>`)** — flagged 4/14.
  Not flagged: `82224509-40`, `82224509-71`, `82225643-11`, `82225643-57`, `82227388-22`, `82227388-30`, `82227388-43`, `82228640-48`, `82228640-53`, `82229122-33`.

The 10 unflagged entries from the comment's 14 are correct behaviour, not a miss —
**10 of 10** close on `attack-last`, which names no rung, live or dead. It is the
Pilot's structural resequencing, so *"the agent does the right thing, just in a different
order within the same turn"* is a different question (*is same-turn ordering a blunder at
all?*) — and the comment filing them says exactly that. Nothing about them expired; there
is no dead rule for them to have expired against.

Entries this audit surfaces that Issue #238 never named: **40**.

## Provenance

* rung vocabulary captured at `d38270208cba`
* rungs deleted since that capture, folded in without git: none
* positive control — the four decider sweeps' `RETIRED` lists: **45** names, every one present in the historical harvest

