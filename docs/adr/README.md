# ADR index

The authoritative map of **number → file → status**. Generated 2026-07-14 during the audit-remediation
pass, which found 31 ADRs with no status line at all and 5 whose status was demonstrably false — a
status that lies is worse than none, and without this index auditing them meant reading 62 files.

## Numbering

Three numbers were used twice. Each collision was one **strategy** ADR and one **tooling** ADR, and the
bulk of inbound references meant the strategy one — so the tooling ADR moved and the strategy ADR kept
its number (2026-07-14):

| was | is now | doc |
|---|---|---|
| 0022 | **0057** | the self-play corpus (0022 stays the **Gust** doctrine) |
| 0033 | **0058** | the Arena (0033 stays the **transient-effect tracker**) |
| 0050 | **0059** | the cgpy engine twin (0050 stays **multi-step lethal verification**) |

`0050-glossary.md` is *not* an ADR — it is the companion vocabulary doc for ADR-0050 (lethal
verification), and its filename prefix is correct.

*(**0106 was SKIPPED deliberately, not lost, and then landed anyway — as the OTHER branch's ADR.**
Issue #313 item 2g's ADR took **0107** on 2026-08-02: a disk scan of the rebased branch said 0106
was free, but open **PR #316** (Issue #294) already claimed it, and this file's own rule is that a
number claimed by an open PR is taken even though `main` cannot see it. PR #316 merged first and its
ADR is exactly **0106** below — so both numbers are now on disk, the pointer moves past both, and
checking the open PRs before settling is the step that turned what would have been a third collision
into a non-event.)*  
*(**Collided, THIRD time — and this one REACHED `main` unresolved.** Issue #406's ADR (the grab equation) and Issue #409's (the HEAL target) both took **0122** on 2026-08-06 from branches that were open at once. Both merged — PR #413 at `78bb5943`, then PR #415 at `0926eba7` — so unlike the first two collisions neither was caught at an `/open-pr` rebase, and `tests/test_adr_index.py` sat RED on `main` for hours with THREE assertions failing, not one: the duplicate number, two missing Index rows (0121, 0122), and a "Next free number" pointer that PR #415's merge had deleted outright. Both ADR-0072 gates were green throughout, which is why nobody noticed — the gates and the suite are different signals, and a green gate is not a green suite. Resolved on `main` at `a8bcdba` by the usual rule (grab merged first and KEEPS 0122; heal renumbered to **0123**); Issue #417's branch had independently made the identical repair and dropped it at rebase as redundant, keeping only its own **0124**. The lesson is the one the numbering convention cannot enforce on its own: the collision check belongs at MERGE, not only at `/open-pr`, because two branches can each be correct at their own rebase and still collide.)*  
*(**Collided, second time.** Issue #261 POC-T2 ran items 2c and 2e/2h on two branches at once, and both authored an ADR. Item 2c's merged first as **0102** (PR for Issue #261 item 2c), so per this log's convention it keeps the number and item 2e's renumbered to **0103** at its `/open-pr` rebase. Both branches picked 0102 from a disk scan that was correct when each ran it — which is the residual race the temp-naming convention narrows and cannot close, now fired for the second time. Cost: one mechanical commit, and the `ADR-0102` references already written into eight shipped source files had to be re-pointed WITHOUT touching item 2c's own — same string, two referents, on one tree.)*
*(**0100** is on disk as of the 0073 collision resolution below, and **0094-0099** landed with PR #266 (Issue #259, POC-T0). Both were in flight at once, which is why PR #267 numbered past them rather than taking 0094: a number claimed by an OPEN PR is taken even though a disk scan of `main` cannot see it. `tests/test_adr_index.py` now fails if this pointer is not actually free.)*  
*(The pointer read `0092` while `0092` was already taken on disk — Issue #228's ADR scanned `docs/adr/` rather than trusting it, as this file's own instruction says to, and took **0093**. Second time the disk scan has caught a stale pointer; the instruction is earning its keep.)*  
*(**Collided.** Issue #250's ADR was `ADR-TEMP-250` and took **0089** at its `/open-pr` rebase, when both the pointer and a disk scan said `0089` was free. Issue #243's PR #252 then merged first with its own **0089**. Per this log's convention the first-merged keeps the number, so Issue #250 renumbered to **0090** on its next rebase. This is the residual race the temp-naming convention narrows but cannot close — and the first time it has actually fired. It cost one mechanical commit, not a scramble across a long-lived branch, which is the convention working as designed.)*  
*(The pointer read `0086` while `0086` was already taken on disk. Issue #241's ADR scanned `docs/adr/` rather than trusting it — as this file's own instruction says to — and took **0087**. Caught before it collided, which is the first time in this log. Issue #239's ADR-0088 then found the pointer and the disk scan AGREEING, which is what the temp-naming convention is for: the number was claimed once, at `/open-pr` time, instead of at grill time.)*

⚠️ **Check the open specs as well as this file before claiming a number — a claim can exist in an
issue comment before it exists as a file.** Issue #217's ADR skipped 0083 on 2026-07-30 believing it
was spoken for by Issue #188's S4-snipe ADR, and took **0084**. The skip was correct but the reason
was wrong: 0083 went to **Issue #213**'s scaler ADR, which merged first (PR #218). **Issue #188's ADR
must therefore renumber a second time** — it has already moved 0082 → 0083, and 0083 is now taken, so
it lands on **0085** and this pointer moves to 0086 when it does. That is the seventh collision in six
days.

⚠️ The index below was **incomplete**: **0067** exists on disk but has never been given a row (it
landed on the #137 / 0a branch). 0069 and 0070 were backfilled 2026-07-26. Re-indexing 0067 is
unowned work — see #167's grill notes.

**0090, 0092 and 0100 were also missing, and were backfilled 2026-08-01.** The gap was found by
`tests/test_adr_index.py`, added the same day — the index had no check of any kind, so an ADR
could merge without a row and nothing said so. That test now fails on a new gap, on a duplicate
number prefix, on a row whose link 404s or points at a different number, and on a "next free"
pointer that is not actually free. **0067 is its one declared exemption**, carried as data with
its reason rather than as a silence.

**0071 was claimed twice and resolved 2026-07-26.** #163's *bench survival is a shared-budget
Harvest* merged to `main` first and KEEPS 0071; #167's *mid-build swaps are gated by deterministic
instruments* renumbered to **0072** on rebase. Both are strategy ADRs, so the tooling-moves-first rule
above did not apply — first-merged kept the number.

**0073 was claimed twice and resolved 2026-08-01 — five days LATENT, and the only one this log ever
missed.** Issue #164's *fetch reach and fetch deadness are opposite readings of one clause* merged
first (PR #171, 2026-07-26 21:47Z) and KEEPS 0073; Issue #141's *the promote/retreat equation is the
sub-lethal residual* (PR #173, 2026-07-27 09:16Z) renumbered to **0100** on 2026-08-01. Same
first-merged rule; both are strategy ADRs.

What makes this one different is that **nothing caught it at the time**. The second PR merged with a
duplicate filename prefix and the collision was simply never noticed: no rename, no log entry, and —
the part that actually cost something — **no Index row at all** for the promote/retreat ADR, which is
why five days of `git grep ADR-0073` returned two unrelated ADRs and why a reader following the index
would conclude the promote/retreat ADR did not exist. 127 references across 49 files pointed at the
ambiguous number; 27 of them meant the fetch ADR and stayed. Found by audit while numbering an
unrelated branch, not by a failing rebase — which is the lesson: **a duplicate prefix is silent.**
Nothing in the toolchain looks for one, and the temp-naming convention below prevents a collision
being *created* at grill time without detecting one that already exists on disk.

It renumbered to 0100 rather than 0094 deliberately: PR #266 (Issue #259) was in flight claiming
0094–0099, so 0100 is the first number that is correct whichever of the two merges first.

**0074 was claimed three times and resolved 2026-07-27.** Three branches were open at once and each
authored an ADR numbered 0074. Resolved by the same first-merged-keeps-it rule: #175's *a probability
may weight a ranked value* merged first and KEEPS 0074; #177's *the KO oracle prices attachments as a
typed Budget* renumbered to **0075**; #186's *the opponent-target slot family splits by instrument
shape* renumbered to **0076** on rebase. All three are strategy ADRs, so the tooling-moves-first rule
did not apply to any of them. ⚠️ The lesson is now recurring (0071, then 0074 ×3): the number is
claimed at grill time but only settled at merge time, so a long-lived branch should expect to
renumber and should keep its ADR references greppable for exactly that reason.

**0076 was claimed twice and resolved 2026-07-29.** #186's *the opponent-target slot family*
(itself already renumbered 0074 → 0076 above) merged first and KEEPS 0076; Issue #172's *a ranked
count consumer reads `expected`* renumbered to **0077** on rebase. Same first-merged rule, and the
third instance of the recurring lesson in three days — the grill-time number is a claim, not a
reservation.

**0077 was claimed twice and resolved 2026-07-29.** Issue #172's *a ranked count consumer reads
`expected`* (itself already renumbered 0076 → 0077 above) merged first and KEEPS 0077; Issue #187's
*the value currencies are three scales bridged by derived rates* renumbered to **0078** on rebase.
Fourth collision in four days, and the second time a single ADR has been renumbered twice in its own
life. Treat the number as a rebase artifact rather than an identifier: **cite the issue alongside it**
("ADR-0078, Issue #187") so a rename can be applied to one branch's references without corrupting
another's.

**0079 was claimed twice and resolved 2026-07-29.** Issue #161's *the Set-Up Active pick is one deck
declaration* (itself already renumbered 0075→0077→0078→0079 across three rebases) merged first and
KEEPS 0079; Issue #199's *deny is a categorical relevance instrument* renumbered to **0080** on
rebase. **Fifth collision in four days**, and the second ADR in the series to be renumbered more than
once in its own life. The lesson is no longer "expect to renumber" but "the number is not the
identifier": cite the issue alongside it ("ADR-0080, Issue #199").

**0080 was claimed twice and resolved 2026-07-29.** Issue #199's *deny is a categorical relevance
instrument* (itself already renumbered 0079 → 0080 above) merged first and KEEPS 0080; Issue #203's
*the opener is hand-conditional* renumbered to **0081** on rebase. **Sixth collision in four days**,
and the third ADR in the series renumbered more than once in its own life. Issue #203's file carried
a grill-time warning that it expected to renumber, and it did — within the day. Both are strategy
ADRs, so the tooling-moves-first rule did not apply. At this point the collision is the norm rather
than the exception: **claim nothing, cite the issue, and renumber at rebase**.

**0083 claimed 2026-07-30** by Issue #213 (*a visible-state scaler's variable is named by
measurement*) — and it SURVIVED its rebase, the first in this streak to do so. It skipped 0082
because Issue #188's grill had claimed that number on an unmerged branch; by the time #213 rebased,
0082 had gone to Issue #211 instead and 0083 was genuinely free. Skipping a contested number turns
out to be cheaper than renumbering after the fact, but it is luck rather than a rule — cite it as
**ADR-0083, Issue #213** regardless.

## Temp naming (2026-07-30) — the fix for the numbering saga above

Eight collisions in eight days, several branches renumbered more than once, all because a number was
claimed at grill time and only settled at merge time. As of 2026-07-30, `/grill-with-docs` no longer
claims a number at grill time at all:

- A new ADR is authored as `docs/adr/temp-issue<N>-<slug>.md` (`<N>` = the originating issue number),
  tagged `ADR-TEMP-<N>` in its title and in any same-session cross-reference. Issue numbers are
  already unique, so two branches' temp ADRs can never collide.
- `/open-pr` assigns the real number exactly once, late — immediately after it rebases the branch
  onto `main` and immediately before it pushes — by scanning disk for the true highest `NNNN-*.md`
  and incrementing. It renames the file (prefix only; the slug is untouched), rewrites every
  `ADR-TEMP-<N>` reference to the real `ADR-NNNN`, and updates the Index and the "Next free number"
  pointer below in one mechanical commit.

This shrinks the collision window from "however long the branch lives" down to the few seconds
between one branch's rebase and its push — rare rather than routine. If that residual race still
fires, the first-merged-keeps-it convention established above still applies: refetch `main`,
recompute, rename again. See `.claude/skills/grill-with-docs/SKILL.md` and
`.claude/skills/open-pr/SKILL.md` for the full mechanics.

**Next free number: 0155.**

## Index

| # | Title | Status |
|---|---|---|
| 0001 | Source the deck Meta from Simulation-competition replays | accepted |
| 0002 | Extracts-only retention — discard raw replays | accepted |
| 0003 | Scouting knowledge is an offline-compiled, shipped artifact | Accepted and BUILT — `tools/build_scouting_artifact.py` compiles the committed |
| 0004 | Shared runtime packages, assembled into a self-contained submission at package time | Accepted and BUILT — bundles ship `src/common/` and native `src/cg/`; `src/cgpy/` is offline-only and artifact-forbidden. |
| 0005 | deck_stealer copies decks from a replay file, not the leaderboard or meta store | Accepted and BUILT — `tools/deck_stealer.py` is the shipped tool and sources decks from |
| 0006 | Function Tags are the single source of structural card facts; roles are tags-in-context | Accepted, then **partially reversed 2026-06-24** (the revision note below): Depth 1 (the |
| 0007 | Learning enters as one offline, replay-trained general value model gated by the Read | Accepted — the one-learned-seam principle holds and the seam was built as |
| 0008 | The Pilot is a layered rules pipeline; decks plug in a declarative Strategy | Accepted and BUILT — `src/common/pilot.py` is the shipped Sense→Plan→Score→Act pipeline |
| 0009 | Training methodology — three jobs, dense signals tune, the ladder gates | Accepted and BUILT — Job A (weight tuning from Corrections) ships as `tools/train/tune.py` |
| 0010 | Local agent verification runs on the real cabt env (kaggle-environments), not the raw `cg` loop | Accepted and BUILT — `tools/sim/check_agent.py` verifies agents on the real `cabt` env, |
| 0011 | Fetch from the daily top-episode dataset; drop the low band | accepted (supersedes the acquisition mechanism of ADR-0001; |
| 0012 | Optimize for the Strategy Category — legible reasoning over leaderboard rank | Accepted — the standing goal of the whole repo. It is policy, not code: every later ADR is |
| 0013 | Convert Limitless decklists by resolving card *names*, not (set, number) | Accepted and BUILT — `tools/deck_convert.py` is the shipped converter: name-keyed, |
| 0014 | Blunder inspector reuses the official cabt visualizer, embedded in a tagging shell | Accepted and BUILT — the vendored viewer + tagging shell ship in `tools/train/blunder/` |
| 0015 | The Correction schema — atomic, two-axis, self-contained | Accepted and BUILT — the shipped Correction schema (`tools/train/blunder/correction.py`, |
| 0016 | Energy attachment is a layered, override-able procedure | Accepted and BUILT — the universal energy reflexes live in |
| 0017 | Corrections compile to Hypotheses via the Tuner — attribution derived, Tier-0 now, fan-out | Accepted and BUILT — the Tuner (`tools/train/tuner/`) derives `attribution` by replaying |
| 0018 | Applying Tuner output — weights auto-load; Hypotheses are LLM-authored behind a Verifier | Accepted and BUILT — `tuned.json` auto-loads as the Pilot's `overrides` (now through |
| 0019 | Submissions are traceable, self-describing, and tracked against performance | accepted |
| 0020 | Forward-evolution knowledge is a provider primitive, distinct from the Read's EvoPath | accepted (2026-06-28) |
| 0021 | The self-play pre-filter balances seats; reproducibility is statistical, not seeded | Accepted and BUILT — `tools/sim/battle.py` seat-balances every run and appends a Battle |
| 0022 | Gust decisions are a closed-form lethal-lookahead over hypothetical defenders (board-only, Read-deferred) | Accepted (grilled 2026-06-29); **implemented 2026-06-29** — whether-to-play |
| 0023 | Fetch decisions are one shared closed-form value comparator (importance × gap × availability), board-only, Read-deferred | Accepted (grilled 2026-06-29); **core implemented 2026-06-29** test-first |
| 0024 | Shuffle-Refresh is the Fetch comparator's decision (A) only — a dead-hand fallback over keep-value, with a deferred stochastic pull-EV | Accepted (grilled 2026-06-29); **Layer-A premise PARTLY REVERSED 2026-06-30** — the |
| 0025 | Baseline rules cluster by decision-context; `doctrine_` vs `baseline_` | Accepted and BUILT (behavior-neutral) — `common/strategy/baseline/baseline_*.py` holds the |
| 0026 | M2 Posture's generic core is the Read's net-new levers, not generic seek/avoid | Accepted and BUILT — `posture` is `PROFILE=True` (default ON): levers A (favorability) and |
| 0027 | Per-archetype counterplay is a hand-authored Matchup Brief, distinct from the auto-Dossier | Accepted and BUILT — eight Briefs ship at `src/common/scouting/briefs/<slug>.json` with |
| 0028 | +HP Tool deploy is survival-turns board-math — proactive default, not hold-for-breakpoint | Accepted (grilled 2026-06-30, `/grill-with-docs`) and **BUILT** test-first (`/tdd`, |
| 0029 | Own-deck content is a SOUND oracle PLUS a PROBABILISTIC estimate — two epistemics, never contradictory | Accepted & **implemented** test-first 2026-06-30 (`tests/strategy/test_deck_odds.py`, |
| 0030 | Winning this turn is an eager, engine-verified Lethal Solver (sound, shortest-line, execute-only) | Accepted (grilled 2026-07-01, `/grill-with-docs`). **Superseded in layout by |
| 0031 | The Turn Planner is a goal-directed, engine-simulated whole-turn optimizer (Tier-1 Search) | Accepted (grilled 2026-07-01, `/grill-with-docs`). **Extended by |
| 0032 | Card knowledge is an engine-audited effect compendium (three tables + one damage oracle) | Accepted (grilled 2026-07-02, `/grill-with-docs`). **Built 2026-07-02** (TDD, all |
| 0033 | Transient attack effects are a log-inferred, match-scoped tracker | Accepted & built 2026-07-02 (TDD; the completion plan's P2 — |
| 0034 | Deck rules fold into the General Strategy when their vocabulary is general | Accepted and BUILT (2026-07-02) — the first fold round landed (mega_starmie ships |
| 0035 | Per-deck specialization of General-Strategy weights is a two-layer override — authored seeds under learned deltas | Accepted (grilled 2026-07-02, `/grill-with-docs`) and **BUILT** — `/deck-align` is shipped |
| 0036 | Deck strategies are recurringly re-aligned against the evolving General Strategy — /deck-align, ledger-diffed and score_diff-gated | Accepted (grilled 2026-07-02, `/grill-with-docs`) and **BUILT** — `/deck-align` ships at |
| 0037 | The Lethal Solver is the Turn Planner's sound top rung (one entry point, one generator family, verified locks replay) | Accepted (grilled 2026-07-03, `/grill-with-docs`). **Built 2026-07-03 (`/tdd`), all three |
| 0038 | Brief consumption sharpens the owning Tactical signal (γ-scaled), not parallel Hypotheses | Accepted (2026-07-04) and built — then **SUPERSEDED by ADR-0051 (2026-07-12)**: the γ-gated Brief levers (`brief_preevo` / `brief_engine`) are **RE… |
| 0039 | Gamble Lines are closed-form expectimax over Outcome Classes | Accepted (grilled 2026-07-05, `/grill-with-docs`). **Built 2026-07-05 (`/tdd`)**: the |
| 0040 | Match-level judgment is per-turn closed-form objectives (Prize Path × KO Race → derived phases) | Accepted (grilled 2026-07-05, `/grill-with-docs`). **Built 2026-07-05 (`/tdd`)**: KO |
| 0041 | Posture is observable in Decision Telemetry (matchup misplays route to a Brief, not a weight) | Accepted and BUILT (2026-07-05) — the Pilot stamps `_posture_record` on every Decision and |
| 0042 | The Automatic Value Model is a dependency-free logistic over the objective features | Accepted + **Built 2026-07-05** (`/tdd`, Tier 5). The single learned seam (ADR-0007), |
| 0043 | Escalation Search is a budgeted depth-2 tree on a close attack tie | **Deprecated & REMOVED (2026-07-17**, ADR-0064 Decision 6); corpus re-check clean; code + tests physically removed, `search_budget` kept inert |
| 0044 | The deferred opponent-choice residue is narrow closed-form reads, not revived escalation search | Accepted (grilled 2026-07-06, `/grill-with-docs`) + **Built 2026-07-06** (`/tdd`) + |
| 0045 | Match-scale planning is a closed-form directive Game Plan atop the Turn Planner | Accepted (grilled 2026-07-06/07, `/grill-with-docs`) + **BUILT 2026-07-07 (`/tdd`, all four |
| 0046 | Strategy authoring splits — analysis skills propose, one skill applies | Accepted (2026-07-09). `update-strategy` built + proven; **all five producers trimmed to |
| 0047 | Opponent awareness is one facade over knowledge subsystems, not scattered fields | Accepted (grilled 2026-07-09, `/grill-with-docs`) + **Foundation built 2026-07-09**; the |
| 0048 | Prize-economy fetch broadens the Line concept behind a role-gated win-condition set | Accepted (grilled 2026-07-10) and BUILT — `prize_economy_fetch` is `PROFILE=True` (default |
| 0049 | A Correction carries a Scope — decision, turn, or match | Accepted and BUILT — `scope` (`decision` | `turn` | `match`) ships through the whole blunder |
| 0050 | Ubiquitous Language — Lethal verification & engine seeding (ADR-0050) | —  *(companion vocabulary doc for ADR-0050, not an ADR)* |
| 0050 | The Lethal Solver's engine verify seeds the EXACT deck; the multi-step verification tool is that seeding + a fixture backfill + a pytest helper | Accepted + **Phases 1, 2 AND 3 built** — Phase 3's follow-up hooks shipped 2026-07-13 and |
| 0051 | ADR-0051 — Matchup Target Priority is one spine the targeting decisions read | Accepted (Phases 1–3b built 2026-07-12/13) |
| 0052 | Combat math is one KO-oracle module with explicit dependencies | Accepted (2026-07-13) and **BUILT** — `CombatMath` ships as `src/common/strategy/combat.py` |
| 0053 | ML Training Pipeline — Build Plan | Accepted (2026-07-13); an accepted BUILD PLAN — no work package has started (the S1/WP0 row |
| 0054 | The stat provider splits into parser battery, indexes, and records | Accepted (2026-07-13) and **BUILT** — `scouting/card_text.py` (parser battery) and |
| 0055 | One agent runtime owns the deployment profile | Accepted (2026-07-13) and **BUILT** — `src/common/runtime.py` is merged to main: `PROFILE` |
| 0056 | The Stat Provider is the one card-knowledge seam; records answer single-card questions | Accepted (2026-07-13) and **BUILT** — merged to main: the Stat Provider is the one |
| 0057 | The self-play corpus uses the cabt-env path (for Tuner-usable obs), not the A/B harness | Accepted and BUILT — `tools/sim/selfplay.py` generates the own-game corpus on the cabt-env |
| 0058 | Arena captures PvC Matches on the cabt-env path (human bridged as an env agent) | Accepted and BUILT (2026-07-02) — `tools/arena/` hosts PvC Tables on the cabt env with the |
| 0059 | cgpy is a trace-verified pure-Python twin of the native engine | accepted (2026-07-10) |
| 0060 | Hand-refresh value is a closed-form card swing, not a hand-size threshold | Accepted (grilled 2026-07-14, `/grill-with-docs`) and **BUILT 2026-07-14 (`/tdd`)**, default |
| 0061 | A locking attack's value includes its forced follow-up (Horizon-2) | Accepted (grilled 2026-07-14, `/grill-with-docs`) and **BUILT 2026-07-14 (`/tdd`)**, default |
| 0062 | Energy denial is what the strip actually takes away, not whether Energy is present | Accepted (grilled 2026-07-14, `/grill-with-docs`) and **BUILT 2026-07-14 (`/tdd`)**, default |
| 0063 | A booster scales the oracle; a doomed body denies nothing; banked Energy is worth what it will pay for | Accepted and **BUILT 2026-07-14 (`/tdd`)**, default ON — amends ADR-0062 |
| 0064 | Incoming counts the opponent's next development step, budgeted by the Read | Accepted (grilled 2026-07-16) and **BUILT 2026-07-16/17** — all six decisions, suite-green; deprecates+removes ADR-0043, amends the `incoming-affordability` WON'T-FIX |
| 0065 | Card worth is one marginal oracle with a closure-graph backend | Accepted (grilled 2026-07-17, Rounds 7–9) and **BUILT 2026-07-18** — the module seam (`fetch_closure.py` + `card_worth.py`); the four-shadow value convergence STAGED; amends ADR-0060 |
| 0065 | Ubiquitous Language — Worth / Gates / Odds / Closure (ADR-0065) | —  *(companion vocabulary doc for ADR-0065, not an ADR)* |
| 0066 | The gust baseline is rider-aware, and gust denial is marginal | Accepted and **BUILT 2026-07-19** — the gusting Round-0 build (3 targeted fixes: rider-aware baseline + threat-forfeit premium, marginal famine stall, loaded-equal-KO tie-break); amends ADR-0022 |
| 0068 | The StateModel is a lazy, pure snapshot; reuse is by side, never by patch | Accepted (grilled 2026-07-24, `/grill-with-docs` on #138 — seven locked decisions); build = #138 (Phase 0b). `apply(action)` rejected; Carried State channel declared; Count Triple deck counts; Leaf Profile CI-pinned. *(0067 = the ADR-0067 epistemic split, authored on the #137/0a branch.)* |
| 0069 | The attach marginal is an axes-sum, and the decider may say no | Accepted (grilled 2026-07-25, `/grill-with-docs` on #139 — thirteen locked decisions) and **BUILT** — Phase 1a, the FIRST no-shadow decider swap; its shape (fold → delete → retune → corpus re-rule → paired A/B) is the pattern 1b–1e follow *(index row backfilled 2026-07-26)* |
| 0070 | The evolve marginal is a body-substituted delta, and its constants are odds | Accepted (grilled 2026-07-25, `/grill-with-docs` on #140) and **BUILT/MERGED 2026-07-26** — Phase 1b, the evolve decider ships ON; amendments A–I. §9's bench branch is corrected by ADR-0071, and amendment C's f82 planner-scope premise is retired by it *(index row backfilled 2026-07-26)* |
| 0071 | Bench survival is a shared-budget Harvest, and the clock accumulates | Accepted (grilled 2026-07-26, `/grill-with-docs` on #163 — twelve locked decisions); build = #163, branching after #166 merges. Amends ADR-0070 §9 (amendment D) and the shipped `turns_to_ko_me`; corrects the CONTEXT.md Threat Clock promotion surcharge. *(0070 = the evolve marginal, authored on the #140/1b branch.)* |
| 0072 | A mid-build decider swap is gated by deterministic instruments; the paired A/B becomes a crash-and-catastrophe tripwire | Accepted (grilled 2026-07-26, `/grill-with-docs` on #167 — five locked decisions); build = #167. Amends #136 directive 6 and promotes ADR-0069 §8's sweep to a gate |
| 0073 | Fetch reach and fetch deadness are opposite readings of one clause | Accepted (grilled 2026-07-26, `/grill-with-docs` on #164); build = #164. Extends ADR-0065 / ADR-0032 / ADR-0029, companion to ADR-0068 |
| 0074 | A probability may WEIGHT a ranked value, never GATE a lock | Accepted (grilled 2026-07-27, `/grill-with-docs` on #175) and **IMPLEMENTED 2026-07-27**. Extends ADR-0067, amends ADR-0031 decision 3; leaves ADR-0030/0037 untouched |
| 0075 | The KO oracle prices attachments as a typed Budget, once, for every line | Accepted (grilled 2026-07-27, `/grill-with-docs` on #177 — five locked decisions); build = #177. Renumbered from 0074 on rebase (#175 merged first). Extends ADR-0067 and ADR-0074 into the five `_play_accel_extra` lines; gated by ADR-0072 |
| 0076 | The opponent-target slot family splits by instrument shape: held-card keep pricing extends the Needs assignment; target-ranking reads the marginal directly | Accepted (grilled 2026-07-27, `/grill-with-docs` on #186 — three locked decisions); build = #186, consumed by #187/#188/#189/#190. Renumbered from 0074 on rebase (#175 merged first, #177 took 0075). Extends ADR-0065's Needs/`keep_v2`; gated by ADR-0072. Amendments A–F |
| 0077 | A ranked consumer asking HOW MANY reads `expected`, not the Probability Leg | Accepted (grilled 2026-07-28, `/grill-with-docs` on Issue #172 — four locked decisions); build = Issue #172. **Amends ADR-0074 decision 1** (the ranked branch splits by the question asked) and resolves its decision-6 untyped-union objection for `expected`; retires ADR-0061's hand-rolled `_deck_basic_energy_fuel` floor; gated by ADR-0072 |
| 0078 | The value currencies are three scales bridged by DERIVED rates, and building the bridge is a shared-layer prerequisite, not an instrument swap | Accepted (grilled 2026-07-28, `/grill-with-docs` on Issue #187 — six locked decisions); build = Issue #199 (S3c), consumed by Issue #187/#188/#189. Renumbered from 0077 on rebase (Issue #172 merged first). Hoists ADR-0100's `PRIZE_DAMAGE_RATE` to `common/currency.py` and names the still-underived Worth Damage Rate; supersedes ADR-0076 Amendment E's hand-off of the currency debt to Issue #189; overturns the WP-N8 play-side ruling. Amendments A–C record gate 1 failing, the user's instantaneous-deny ruling, and its passing |
| 0079 | The Set-Up Active pick is one deck declaration, not a pile of derived rungs | Accepted (grilled 2026-07-28, `/grill-with-docs` on #161 — nine locked decisions); build = #161. Completes ADR-0070 §4 and overturns the 2026-07-15 evolve grill's Ruling 5; applies ADR-0034 / ADR-0046. *(authored as 0075, renumbered 0075→0077→0078→0079 across three rebases — each of 0075/0076/0077/0078 merged first)*. Bench sibling split to #197 |
| 0080 | Deny is a CATEGORICAL RELEVANCE instrument, not a magnitude one: the Worth Damage Rate is not needed, it is MOOT | Accepted (grilled 2026-07-29, `/grill-with-docs` on Issue #199 — five locked decisions); builds in Issue #187 (rechartered), Issue #199 closes as the shared layer reduced, Issue #188 unblocked, Issue #189 re-inherits the gust currency debt. Supersedes ADR-0078 decision 1 for deny and withdraws its one-backend claim for deny; answers gate 2 MOOT (the DISCARD sweep found the sole anchor degenerate — it prices 0.000 under both instruments, so the rate divides out) *(authored as 0079, renumbered 0079→0080 on rebase — Issue #161's Set-Up Active pick merged first)* |
| 0081 | The opener is hand-conditional: a turn-0 evolve marginal REORDERS the declaration, and pins hold their slots | Accepted (grilled 2026-07-29, `/grill-with-docs` on Issue #203 — five locked decisions plus Amendment A, which narrows decision 4 and defers decision 2); build = Issue #203. **Extends** ADR-0079 rather than overturning it (one rule, one boolean, `+40` all unchanged — the *resolution* of `top_starter_id` gains hand-awareness); collects the placement work ADR-0070 §4 parked. Applies ADR-0034 / ADR-0046 / ADR-0065 *(authored as 0080, renumbered 0080→0081 on rebase — Issue #199's deny instrument merged first)* |
| 0082 | A Correction's ruling lives in its **Claim**, and a Claim must AGREE with its record | Accepted (grilled 2026-07-29, `/grill-with-docs` during the Issue #187 review — five locked decisions, one a mid-grill withdrawal); build = Issue #211. **Extends ADR-0072 decisions 3-4** (the `claims` block and the Held-out Ledger) to the 34 fixtures that predate them, and adds the Claim Agreement invariant. Supersedes nothing; knock-on = ADR-0062 Amendment A. Records that `82749168-29` was re-ruled in Issue #177 and the corpus was right all along — the stale value was the fixture's |
| 0083 | A visible-state scaler's VARIABLE is named by measurement, and a fit may only claim a variable the sweep controls | Accepted and BUILT — Issue #213 **Amendment A** (2026-08-02, Issue #224 follow-up): decision 2's rule - a fit may only claim what the harness CONTROLS - is applied one field over, to the coin BOUND. `_coin_bounds` keyed its fork pair on `coin` alone, so `--sweep` left several vanilla `coin="max"` records and the shipped `damageMin`/`damageMax` was an arbitrary survivor. Bounds are now scoped to the PHYSICAL board (sweep/step excluded - they are labels, and the panel point and the `atk_bench` step-1 point are the same board): one board or several AGREEING boards emit, disagreeing boards emit nothing, one board answering twice emits nothing. Corroboration is kept rather than refused, per §3's flat-axis argument. And a bound never ships for an attack that HAS a scaler, parser-named or fitted, because `compute_active_damage` REPLACES the base with the bound and then adds the scaling term - shipping both double-counts it, an over-prediction. Value-preserving: all 99 bound entries are `unaudited` (ADR-0108) and no measurement speaks to them. |
| 0084 | Deny's derived clock is a **tiebreak**, not a deadline, and never a **gate** | Accepted (grilled 2026-07-30, `/grill-with-docs` on Issue #217 — eight locked decisions, one a mid-grill **reversal of an already-accepted decision**); build = Issue #217. **Both go/no-go gates PASSED** (`tools/train/probes/deny_gate217.py`, commit `809c6d5`) — gate 1 the opposite way to the charter's prediction, which rested on a false board fact. Answers the charter's central proposal **NO**: `/2**t` survives on a type argument (a clock DELTA is not a clock READING), so nothing collapses; `_DENIAL_BENCH` retires from the ARMED fire rung only, in favour of ADR-0071 decision 6's promotion gate, gated on a `decide()` retest because gate 1 proved sign not rank. The keep-price bite gate was accepted then **REVERSED** on measurement (would suppress 128/218 relevance-positive rows; the clock is blind to Ability mutes, sub-turn setbacks, and plan damage). `deny_strip_delta` revived with exactly ONE consumer — a lexicographic target-pick tiebreak. Arms `deny_relevance` per tracker build rules 1 and 11. Three reusable NEGATIVE results recorded with sample sizes. **Amendment A**: the armed read never applied ADR-0080's mandated `_DENIAL_FORWARD` discount — an Issue #187 defect found here, priced forward threats at DOUBLE the OFF path, now fixed with a required keyword-only `forward_discount`. **Amendment B**: decision 6 (arming) NOT taken — the Discrimination Gate blocked it and the pre-registered ship-dark fallback was used, so every decision here ships INERT behind `deny_relevance`; arming is owed by **Issue #228**. As shipped this work adds ZERO gate regressions (the one remaining flip reproduces on a clean tree, from Issue #213 against a stale baseline). Supersedes nothing; ADR-0062's `/2**t` and its `_DENIAL_BENCH` derivation both survive (the latter on the OFF path) *(skipped 0083 believing Issue #188 held it; 0083 in fact went to Issue #213, which merged first — the skip was right, the reason wrong. See Numbering above)* |
| 0085 | Snipe is a CATEGORICAL RELEVANCE instrument too: the fold collapses the additive rung stack into one [0,1] scalar under hard gates, not onto the prize marginal | Accepted and BUILT, armed ON, rungs DELETED — Issue #188. Grilled 2026-07-29 (thirteen locked decisions, one taken then reverted on measurement). **Rechartered** from the chartered fold, which measured 7/19 against the shipped rungs' 17/19; two magnitude-shaped successors also refuted (11/19, 10/19). Extends ADR-0080's relevance shape to a second instrument and reaches ADR-0062's "no monotone pricing of magnitude" wall again. **Amendment C** arms it (Tripwire -1.25 pp, CI [-4.79, +2.29], 0 crashes/2400 games; Discrimination Gate PASS run ARMED; Decision Gate 19/19); **Amendment D** rebases onto Issue #213 and catches the instrument reading PRINTED damage because `context=` was never passed; **Amendment E** is the deletion pass. *(authored as 0082, renumbered 0082->0083->0084->0085 across THREE rebases in one day — Issues #211, #213 and #217 each merged first; the ninth collision and the record holder)* |
| 0086 | The deploy marginal prices a bench slot and what fills it | Accepted (grilled 2026-07-29, `/grill-with-docs` on Issue #197 — eight locked decisions); build = a new Value-System phase. Amendments A-C: Issue #199 merged without deriving the Worth Damage Rate (ADR-0080 ruled it moot for deny) and `deploy_anchor_sweep.py` showed this seam cannot anchor it either — structurally, a deploy is never exclusive with a damage-denominated option. Decision 4 was RE-RULED: the Worth legs are dimensionless RATIOS over the Needs assignment, scaled by a preservation-pinned band, so no `WORTH_DAMAGE_RATE` is referenced and the currency guard stands. **UNBLOCKED**; decisions 1-3 and 5-8 stand. The fourth per-seam value equation beside ADR-0069/0070/0073; extends ADR-0065's Needs assignment and consumes ADR-0071's Bench Harvest. Replaces nine of the ten bench rules; `keep-a-bench` is PROMOTED to a post-setup sound rung, not deleted. Closes Issue #197 as the grill record (its items 3-4 superseded, item 2 dissolved). Number claimed, not settled — renumbered 0079 -> 0081 on rebase (Issue #161 kept 0079, Issue #199 took 0080). **Decision 9 (2026-07-30)**: we never bench during Set Up — a rule read off the rulebook (optional placement; my first turn precedes the first legal attack in either seat; no Basic carries a damage Ability), which subsumes three pregame special cases. Amendment F was proposed and WITHDRAWN the same day. Both ADR-0072 gates PASS *(authored as 0079, renumbered 0079 -> 0081 -> 0083 -> 0084 -> 0086 -> 0084 -> 0086 across two rebases — Issue #161 kept 0079, Issue #199 took 0080, and Issue #203 / Issue #211 took 0081 / 0082 while this branch was open)* |
| 0087 | A corpus reader CONSTRUCTS Corrections and keys a frame by IDENTITY | Accepted (grilled 2026-07-31, `/grill-with-docs` on Issue #241 — nine locked decisions, one a mid-grill correction of an earlier one); BUILT. The Decision Gate kept a private raw-JSONL walk and a hand-built Frame Key: it dropped **40** recoverable records on a falsy `agent` and mis-keyed **163** more by reading `seat` off a snapshot that has no `seat` field, leaving only **169 of 372** frames correctly named and **4 of 11** Held-out Ledger rulings unable to reach the gate at all. Extends ADR-0072 decision 4 (one ruling holds a frame out of BOTH gates) and ADR-0049 (identity = the Scope's subject). Decisions: one `keyed_corrections` reader returning PAIRS not a dict (a dict silently collapses a conflict); `correction_frame_key` the only derivation; NO scope filter and NO new labelling rule (a scope-aware predicate was proposed and **withdrawn** — it would have removed 10 standing DECLINE rulings, since `satisfies_human` already owns that per ADR-0085 Amendment J); an allowlist test as ENFORCEMENT, recording the eleven other raw readers against Issue #243; a two-commit landing where the relabel is provably a relabel so the widening reads `added 40, removed 0`; and `ruling_moves`, a shared never-gating channel for a corpus whose RULING moved — which fired on its worked case on the first real run. Corpus 332 -> 372, held-out reachable 7 -> 11, agree 230/331 -> 253/371. |
| 0088 | A VOIDED ruling leaves the agree rate and the gate, and one Ruling Index says so | Accepted (grilled 2026-07-31, `/grill-with-docs` on Issue #239 — seven locked decisions, plus 3a/3b added at build time); BUILT. A ruling the human took back still graded the agent as wrong: the refutation went into `data/corrections/reviewed.json` and the Correction record was never touched, while every grader reads the record. **25 frames** carry a ruling that can no longer grade, and **18 of 101** recorded disagreements were refuted labels — so a build that CORRECTED one failed `main` as a REGRESSION. Extends ADR-0087 (a second value derived from the one corpus walk) and ADR-0072 decision 4 (the never-gating treatment, reached for a different reason). Decisions: `ruling_index`, THE read-only query over every store a ruling can live in, its `review_key` ↔ Frame Key join DERIVED inside `keyed_corrections` because neither keyspace derives from the other; a `Ruling` carrying raw disposition AND source so the index doubles as the *"where is this ruled?"* register, with ONE derived predicate `voids_the_label` consumers key on (any voiding source wins, all matches retained, an unregistered word is non-voiding and LOUD); a voided frame leaving the agree-rate DENOMINATOR and gating, with `satisfies_human` and both verdict rules untouched; a new `transposition` disposition for `81905522-75`, kept distinct from `refuted` because ADR-0085 decision 4 cites that frame's pick (the transposition CLASS is Issue #247); `RECORDED_MISSES` RETIRED — a fourth ruling store in a probe script, whose retirement forced that file's raw walk out too (allowlist 11 → 10); and `agree_delta`, the aggregate half of `ruling_moves`, counts-only because a frame can be re-ruled, voided and moved at once. 3a/3b came from the two-axis review: `orphan_rulings` walks from the LEDGER side (asking the index is TAUTOLOGICAL) and found **two entries ruling on nothing, one a `refuted`**; `fixed`/`deferred-multi-turn` adopted rather than left warning forever, and `blunder/report.py`'s census — a third drift site — now derives from the same tuple. Decision Gate 253/371 → 248/346, Discrimination Gate 193/268 → 183/249, snipe sweep 19 → 23 frames. |
| 0089 | A corpus-reading probe is a **GATE**, a **RULING**, or a routed **DIAGNOSTIC** — never a fourth thing | Accepted (grilled 2026-07-31, `/grill-with-docs` on Issue #243 — six locked decisions); BUILT. Extends ADR-0087 (*how* a reader reads) with the question it did not ask: *what a corpus-reading probe is allowed to be at all.* **Finishes ADR-0085 Amendment J on its fourth, missed sibling.** The issue's own presumption did not survive measurement: it named the four `*_decider_sweep.py` as vacuous, but three had already lost their dead OFF arm to Amendment J and are healthy term-breakdown diagnostics, while `deploy_decider_sweep` — which the issue did NOT name — was the vacuous one. All nine ids its OLD arm zeroed are gone from `src/`, leaving `baseline_bench` holding one rung, so OLD scored a near-empty pile and it could only ever report FIX; it was also the ONLY one of the ten that gated (`SystemExit`) and the last hand-built Frame Key in the tree — **missed by Amendment J precisely because it gated, and a passing exit code read as evidence of health.** Decisions: three **Probe Fates** (a GATE with a committed baseline + CI watchdog, a RULING whose script is deleted once its answer is written down, or a routed DIAGNOSTIC), with *a runnable script nobody watches* named as the failure mode rather than a fourth option; **Corpus Provenance** — a ruling reached by COUNTING across the corpus carries `measured at <commit>, N frames`, while a structural or named-frame ruling does not; the `ALLOWED_RAW_READERS` work queue terminates HERE and the dict is deleted; and the rule widens from *no raw glob in `tools/`* to **nothing outside the store reaches the corrections log**, across `tools/` AND `tests/` — because nine tests globbed the corpus (one carrying the identical 40-record filter) and two named a build directory outright, which no glob can see and which breaks silently on a rename. 5 probes deleted, 5 routed, 11 test conversions behind ONE shared helper each side of the tools/tests boundary. Measured: `budget_sweep`'s ruling re-confirmed at `4be1db3`, 372 frames (371 SAME / 1 SKIP); `needs_sweep`'s acceptance number `discard agree_v2 12/12` HOLDS while its refresh diagnostic's population grew 83 → 96, so “the numbers reproduce” was quietly false and is now stamped. The widening surfaced **no** frame needing adjudication (Issue #238 untouched): `82228640-9`'s “unreplayable” exclusion was false on both counts and the shipped Pilot already makes the human pick. `records_a_decline_it_cannot_state` lifted to `gates.py` **UNWIRED** and gaining a `decision`-scope guard the corpus forced (unguarded it swallowed `86088989|0|turn|0`, a real turn-scope decline) — which frames stop gating is a ruling, not a refactor, so **Issue #251** owns it and argues LOW priority. Both gates' verdicts on `main` are bit-identical before and after. || 0090 | A ruling names its record through a RESOLVER, and every surface prints that name | Accepted (grilled 2026-07-31, `/grill-with-docs` on Issue #250 — four locked decisions plus five side calls); BUILT. The two orphans ADR-0088 decision 3a's `orphan_rulings` found were assumed stale; **neither was** — both named live committed records under the wrong name. `85046350-10` had the wrong EPISODE (the record is ep 85045840 f10, in the SAME store file); `86091435-119` the wrong key SHAPE (the record is turn-scoped, so its `review_key` is `86091435-t14s0`). Root cause was the WRITER, with the report as accomplice: `review_correction.py` took the key as free text and checked it against nothing, its `--help` and `reviewed.json`'s `_note` documented the DECISION shape only (pre-ADR-0049), and `report_md._at` printed the scope-blind Anchor frame — including in the reviewed-entries section — so the operator was copying what the report showed them, making `reviewed.py`/`gates.py`'s *"the same id the reports print"* claim FALSE. Extends ADR-0087 (the same hand-built-key prohibition, applied to the writer and the display rather than the reader) and ADR-0049. Decisions: a **Ruling Locator** RESOLVED against the corpus, never accepted — four spellings (`review_key`, Frame Key, `Correction.id`, Anchor form) onto ONE canonical key, unresolvable REFUSED non-zero with nothing written, while `--remove` alone falls back to the literal ledger key so an ORPHAN stays deletable; near-misses two deterministic rules **UNIONED** (chaining them hid the operator's own episode behind two unrelated ones — caught by the two-axis review, and the stub that missed it had a fixture guaranteeing its own result), never fuzzy; `_at` returns `review_key` so what is printed is what is accepted; the resolver in `blunder/reviewed.py` with the corpus INJECTED, keeping `blunder/` free of any `gates` import; and the ledger **NOT** migrated to Frame Key, recorded explicitly as the alternative the next reader will re-propose. The gates test moves from asserting the known two to asserting **empty**, which also proves all 145 committed entries resolve. One repaired refutation VOIDS `86091435|0|turn|14`: Decision Gate 248/346 → 248/345 (rate UP — it was a disagreement), Discrimination Gate 183/249 → 182/248 (rate DOWN — it was an OK). |
| 0090 | A ruling names its record through a RESOLVER, and every surface prints that name | Accepted (grilled 2026-07-31, `/grill-with-docs` on Issue #250 - four locked decisions plus five side calls); build = Issue #250. Extends ADR-0087 (which ruled on the corpus READER; this rules on the *writer* and the *display*, the two actors 0087 never faced) and preserves ADR-0049's Correction-identity vocabulary rather than migrating it. **Repairs the two defects ADR-0088 decision 3a's `orphan_rulings` detector found.** *(Authored as ADR-TEMP-250, took 0089 at `/open-pr` when both the pointer and a disk scan said 0089 was free, then renumbered to 0090 when Issue #243's PR #252 merged first - the residual race the temp-naming convention narrows but cannot close. Row backfilled 2026-08-01: the renumber landed but the Index row never did.)* |
| 0091 | Indistinguishable options are ONE decision, to the graders AND to the leaf | Accepted (grilled 2026-07-31, `/grill-with-docs` on Issue #247 - nine locked decisions, plus 4a added at build time when a failing test forced it); BUILT. **Closes ADR-0088 decision 6's deferral** (which handled the `transposition` *instance* and named the *class* as this issue) and **amends ADR-0085 Amendment J** - `satisfies_human` graded by option INDEX and read no board, so a ruling naming one of two interchangeable options could never be satisfied on purpose, only excused out of the denominator as a `transposition`. Decisions: an option's identity is a **full-option fingerprint** over every zone the snapshot reveals (an ATTACH names TWO cards, so a body-only key produced **6 measured false equivalences**; and a reference the snapshot hides - the face-down DECK, 262 options - makes the WHOLE option unfingerprintable, so blind implies conservative *structurally* rather than by a hand-maintained list); the map read ONCE from the CORPUS and threaded through capture and diff, never read back off a capture, with `equiv=None` byte-identical to the old predicate so three sweeps and eleven tests needed no edit; the `transposition` LEDGER ENTRY deleted while the WORD survives as the escape hatch for off-board indistinguishability; the Leaf Lab made class-aware (`top_tie` counts CLASSES) and given a **Class Asymmetry** report; and the defect the oracle FOUND - the leaf pricing one decision at **1167.0 and 95.4** on three byte-identical Riolu, reproducible across fresh Pilots, caused by an index-order-dependent greedy rollout rather than any positional rung - fixed by canonicalising to the class MAXIMUM (sound by isomorphism, and cheaper: a 3-member class costs one rollout instead of three), with #178's all-or-nothing defer narrowed to the sims the rung actually consults. 4a came from a failing test: the lab reads its sim TWICE, RAW for the finding (canonicalising first makes the instrument empty BY CONSTRUCTION, the vacuous-gate failure) and CANONICAL for every verdict (grading raw measures what the agent does not do). **Neither gate exercises the rung**: zero committed Corrections carry `search_begin_input`, so decisions 5/6/8 rest entirely on unit coverage - stated because a green gate that never ran the code reads as evidence otherwise. The two-axis review then caught a REAL defect this build shipped: one pick satisfying TWO ruled cards in one class (latent, 0/372 frames hit it) - now COUNTING, not membership, which is exact because classes partition the menu. Root cause of the asymmetry filed as **Issue #254**. || 0092 | The Value System POC builds by DIFFERENCING tracks with WAVE rulings | Accepted (grilled 2026-07-31/08-01, `/grilling` on the POC restructure — four user rulings + announced defaults); plan = `docs/plans/value-system-poc-plan.md`. Scope ends at the Turn Planner + `state_value` (old Issues #165/#145 merged); built bespoke marginals stay while every still-weight-driven family (the ~45-rung fetch ladder, heal, tools, stadium, draw economy, gust whether-to-play) is priced as `state_value(after) − state_value(before)` through a closed-form apply-seam with hypergeometric expectation for stochastic effects. Verification for POC tracks amends ADR-0072's mid-build rule: NO paired A/Bs; both deterministic gates + suite mandatory; flips ruled in ~3 batched wave packets (baselines re-capture only on rulings). No shadows, no fallbacks — every swap is the deletion, applied retroactively to the six pre-tracker shadow emitters and the v1 discard fallback. Six tracks, contract-first: T0 spine (term registry, StateModel completion API, apply-seam, whitelist) → T1 substrate ∥ T2 Phase-1 finish ∥ (T3 `state_value` → T4 Turn Planner) → T5 purge. The Worth↔prize rate stays underived; one authored scaffold `POC_WORTH_PRIZE_RATE` lives module-local to `state_value`, wave-1-ratified, retired post-POC. Absorbs/supersedes Issues #144/#189/#204/#212/#217/#220/#221/#222/#223/#225/#228/#232/#237/#254/#257. |
| 0092 | The Value System POC builds by differencing tracks with wave rulings | Accepted 2026-08-01 (the POC restructure grill, 2026-07-31/08-01). Plan: `docs/plans/value-system-poc-plan.md`. Replaces Issue #136's serial one-issue-at-a-time cadence for everything through the Turn Planner with **six parallel tracks** (POC-T0..T5 = Issues #259-#264), **hybrid differencing** (`value(play) = state_value(after) - state_value(before)` for every unbuilt family; the built per-seam equations stay), and **wave rulings** - corpus flips batched into ~3 user sessions instead of per-swap A/B gauntlets, which amends tracker directive 6 for the POC tracks only. Supersedes 17 issues into the tracks. *(Row backfilled 2026-08-01.)* |
| 0093 | A categorical instrument's ZERO must still pay the keep price: a whiff that TIES is not a hold | Accepted (grilled 2026-07-31, `/grill-with-docs` on Issue #228 — six locked decisions); BUILT. **Corrects ADR-0084 Amendment B point 3**, whose diagnosis of the blocking frame was measurably wrong, and delivers **ADR-0092 T2's first bullet**. Arming deny was blocked by two Discrimination Gate flips read as a leaf card-worth artifact; re-measured against the current baseline it was THREE frames, each moving by exactly `_PLANNER_SURVIVAL_W` 50.0, and the cause was a DEFECT: `_opponent_target_rows` returns `None` mid-sim (the `_planning` reentrancy guard) and `deny_relevance_best: float = 0.0` could not express absence, so the fire rung read a dataclass default as a MEASURED whiff — declining strips worth +22.50 and +74.50, one of which the human ruled a correct Hammer play. The incumbent had no such hole because `opp_denial_best` was computed unconditionally. Decisions: a blocking frame is DIAGNOSED before it is adjudicated (ADR-0072 decision 5's six-flip precedent, where six rulings turned out to be one defect); absence is TYPED as absence; the read goes LIVE mid-sim (the guard was a COST decision, not a correctness one — it moves to `_opponent_target_shadow` where 'no shadow work in rollouts' actually applies) with a pre-registered cost fallback that measured 1.17x and was NOT taken; a genuine whiff DECLINES strictly, closing the asymmetry ADR-0084 decision 8 handed forward; fix and arming share one branch with attribution recovered by a THREE-stage gate; and the mid-sim read covers **gust** too, which had the identical defect with its flag shipped ON. The ADR-0062 magnitude oracle was then DELETED under directive 1 — OFF is **degraded mode, never a rollback**. A wrong first Decision 2 is recorded as REPLACED rather than overwritten: it would have greened the gate by making the agent HOLD Hammers it should play. |
| 0094 | A baseline re-capture may not move a frame's verdict without a ruling | Accepted (grilled 2026-08-01, `/grill-with-docs` on Issue #259, wave-1 packet item 1); BUILT. **NOT in Issue #259's scope** - rides with T0 because T0 is the serial track and all five downstream lanes grade against these baselines. `CLAUDE.md` says a baseline is a RULING RECORD never auto-recaptured (a reference that updates itself lets every regression bless itself - how the old Decision Gate died), and nothing enforced it. `capture` now reads the outgoing baseline and REFUSES to write when a frame moves in the FAIL direction (`OK -> MISS` / `agree -> disagree`) without a ruling, naming every offender, exiting 1, leaving the file byte-untouched; `restamp` splits out the metadata-only path (4 of 12 committed re-captures moved nothing but `git_rev`) so it cannot move a verdict. Each lab reads its fail direction through its OWN diff, so guard and gate cannot drift. **HONEST SCOPE: this repairs nothing.** The motivating claim was wrong - the flip was attributed to `eec06b1`, but tracing the frame across all twelve baseline commits puts it at `d5f7211` (Issue #197) on a frame carrying TWO rulings (`fixed` + held out onto Issue #165). Replaying EVERY transition against the Ruling Index: three re-captures, five absorbed flips, **zero unruled, ever**. The convention has held; what this removes is the RELIANCE on discipline across six rebasing tracks. Expect it never to fire - a green run is the predicted result, not evidence. |
| 0095 | Information precedes commitment, and the apply-seam cannot derive it | Accepted (grilled 2026-08-01, Issue #259, wave-1 item 2). Amends ADR-0069 SS7 one tier up. `_finish_turn_last` states its purpose as *"take the most informative, reversible actions first"* but `_tier()` ends on a bare `return 0`, so every endorsed free `_PLAY` lands in tier 0 where score decides - **tier 0 conflates *free* with *informative***, and a Crushing Hammer (free, reveals nothing) sequences ahead of a Pokegear dig. The free band now splits on information, keyed off a behavioural Function Tag, and the SS6 whitelist entry narrows from the unfalsifiable "the sequencing tiers" to the named boundary. **Decision 3 is the load-bearing one: the apply-seam CANNOT capture information value** - both orderings reach the same end state, so no function of that state separates them; the value of digging first is that later choices condition on the reveal, which needs a contingent policy, i.e. depth-2 search, scoped OUT by ADR-0092. So information-first stays STRUCTURAL, and no later track should expect the planner to discover it. Evidence re-based after PR #265: the motivating regression does not exist (deny armed, both gates PASS 0 moved, `83686860|1|decision|13` fully resolved); what survives is the user's direct ruling plus a STANDING disagreement (`82225643|1|decision|11` still picks `[3]` where the ruling says `[0]`) the gates cannot flag because the baseline shares the wrong pick. Decision 4 (sequence deny arming behind this) WITHDRAWN as moot. |
| 0096 | One guard per fact: the Bench doctrine carries forward as term-family entries | Accepted (grilled 2026-08-01, Issue #259, wave-1 item 3); rules and CLOSES Issue #231. Narrows ADR-0086 decision 7 (makes its unconditionality *dated* rather than permanent), preserves decision 9 unchanged. The doom-gated guard the doctrine asks for **already exists**: `_predicted_loss` (ADR-0064) = bench empty AND `combat.reachable_incoming >= my_hp` -> `-KO_SCORE` - the CombatMath read, wired, with bounded pessimism. It is NOT the sparse `_their_turns_to_ko` the issue argued against, and with that objection void the only survivor is bounded-vs-unbounded, which `-KO_SCORE` already answers by sitting outside `_LINE_CAP`'s band. ONE board fact was on the draft whitelist THREE times (terminal rung + order filter + `keep-a-bench` +60). So: the filter stays unconditional but typed **provisional** with its retirement test written down (after T1, retire iff `reachable_incoming` answers on every post-setup empty-Bench frame AND both gates stay green without it); **`keep-a-bench` (+60) deleted** - it guards nothing the filter does not already guarantee at MAIN and IS the spare-body cliff (1.96 vs 61.96); Set-Up never-bench split onto its own line with its own reason (weak dominance from three source-checked facts); Issues #232/#237/#254 carried forward as term-family entries, not re-grills; the Bench slot priced as a scarce resource whose marginal cost RISES as slots deplete. |
| 0097 | The Worth->prize scaffold is BOUND: reconciled, dated, and its underivability premise is void | Accepted (grilled 2026-08-01, Issue #259, wave-1 item 4). Amends ADR-0078 (discharges its recorded reconciliation debt one direction) and **voids the STRUCTURAL half of ADR-0080/0086's underivability finding** - not by disputing the measurement but because the premise expires. ADR-0086 argued a deploy *cannot trade* against a damage-denominated option; under ADR-0092's differencing `state_value(after) - state_value(before)` puts the card in `hand` (Worth) before and on the board (prizes) after, so **the Worth does not cancel - on every play that spends a card**. `POC_WORTH_PRIZE_RATE` is therefore not a narrow scaffold but the global constant setting what a card in hand costs; equally, the corpus now generates anchors (every ruled spend-vs-hold frame) the old architecture structurally could not. Approved module-local to `state_value`, tagged authored-not-derived, BOUND by three conditions: reconciled at authoring against the three rates `currency.py` catalogues (trainer ~1.0, energy ~6.7, deploy ~0.83) with disagreement RECORDED; a pre-registered retirement test; and the void premise recorded so the post-POC derivation starts from an anchor instead of re-running a twice-failed gate. `common/currency.py` and `test_currency.py` untouched. Pricing the hand at zero was considered and rejected - it makes every free Item strictly worth playing. |
| 0098 | The apply-seam is guarded by detect -> localize -> classify -> CONTAIN | Accepted (grilled 2026-08-01, Issue #259, wave-1 item 6 - the escape-hatch doctrine fork). Extends ADR-0059's parity rails. Names four routinely-conflated objects once: the **native engine** (the authority), **cgpy** (a pure-Python reimplementation, *not complete* - 123/414 episodes green), **Engine Search** (not an engine: the *use* of `search_begin`/`search_step` to play hypotheticals), and **`apply_option`** (the new closed-form one-step transition). The fork - keep an anchored engine route at runtime - was DECLINED. The common objection "the engine cannot fork cheaply" is simply false (it forks an independent position; the Lethal Solver already uses it live). Two survive: past a shuffle a rollout is ONE Monte-Carlo sample (ml f24: same first step scored 7000/162/129/122/89/57.5 across processes), and decisively **offline invisibility** - `search_begin` needs `search_begin_input`, present on **0 of 372 gate frames**, so an engine-routed decision is un-gradeable, un-rulable, un-tunable. The residual exposure (a thin fixture means production finds out) is closed by a mandated loop: an `apply_option` lane on the existing trace rails (reference = recorded NATIVE traces, never cgpy - that would check a model against a model; god frames supply hidden state so it stays DLL-free), a per-option-kind **coverage gate** failing CI below floor, an **auto-quarantine** registry where a divergence marks its kind unverified and the planner refuses to enumerate through it (telemetry mandatory), and a **fuzz lane**. Automated *repair* is not achievable and is not claimed. **Amendment A (ruled 2026-08-01, Issue #263 ordering ruling):** the seam is on the HOT path at 1 ply for EVERY option on a live select menu, so the kind table is TOTAL over `OptionType` and an unmodellable option returns an explicit `Refusal` (always-expand) rather than a silent no-op that would price it at 0.0 delta and read as never-explore. Transitions are LAZY (ADR-0068), `Expectation` is orderable at 1 ply, and `transition_kind` no longer raises - a raise on the ordering hot path is a forfeited grader match. Measured: ABILITY is 17 live MAIN options the pre-amendment table did not declare at all. **Amendment B (ruled 2026-08-01, Issue #259 SS3b/SS3c):** THREE fates, not two - `MODELLED` / `ENGINE-RESOLVED` / `REFUSED`. The engine comes back NARROWLY, and does not reopen decision 1: the fate requires *provably deterministic* (tri-state, unproven REFUSES per ADR-0067) AND a real board AND 1-ply, which excludes both objections by construction. Refused outright: opponent-choice, anything riding the shuffle (no deal-seed => one sample, and nondeterminism breaks the replay both gates depend on), and depth >= 2 (the board is then synthesized). `_search_api` is PRESERVED as this fallback - Issue #263 retires the rollout, not the seam. Telemetry is structural: the route returns an `EngineResolved` wrapper so a caller cannot use the answer without seeing the engine produced it. Adds per-kind READ/WRITE **footprints** for Issue #263's commutative-block collapse (fail closed: unknown footprint commutes with NOTHING; an information-REVEALING kind never joins a block whatever its footprint says), and SS3c's **StateModel completeness contract** (`src/common/snapshot_coverage.py` + audit test): every writable zone is homed, `owed` with a named owning track, or `hidden` with what prices it instead. Finding: `_EVOLVE` and `_RETREAT` both clear Special Conditions (`docs/rules.md` SS4 and SS8) and that zone has no snapshot home yet - a generated T1 work list. **Amendment C (ruled 2026-08-02, Issue #299):** the fate is PER-OPTION, not per-kind. POC-A2's census measured that the `_ABILITY`-only engine route resolved **zero** live options (all 17 corpus `_ABILITY` options are deck-reading draw engines) while **46 deterministic-shaped refusals** sat on MODELLED kinds with nowhere to be sent, and four clause-COMPLETE cards (Drakloak, Lunatone, Dudunsparce, Fezandipiti ex) were unreachable because the kind table outranked their clauses. So `KIND_COVERAGE` stops being the GATE (it is unchanged as a table - nothing promoted or demoted): the engine route is open per-option to every declared non-terminal kind, gated by the per-option proof alone, and `clauses_cover=True` resolves MODELLED whatever the kind says. Decision 1 is NOT reopened - the preconditions are properties of the call, never of the kind. `clauses_cover=False` (Issue #300's `partial` verdict) now REFUSES, so a partial clause set can no longer price its uncovered leg at a silent 0; `None` deliberately does NOT refuse, because it also means 'no printed effect to cover' and separating those is the caller's job. `ENGINE_ROUTE_KINDS` and `KIND_SCOPE` are kept and re-documented rather than repurposed or deleted. Measured: ENGINE-RESOLVED 10 -> 57 sites, MODELLED 312 -> 295, REFUSED 92 -> 62; inert at runtime, since no caller invokes `apply_option` before T4. **Amendment D (ruled 2026-08-04, Issue #263 / Issue #383):** footprint granularity is ELEMENT-level - where a write is an element-level add/remove over distinct instances, two such writes to DISTINCT instances commute, keyed by the engine's own `serial`. A contract-freeze extension, requested before any code and GRANTED by the developer; `snapshot_coverage.ELEMENT_ZONES` is the registry (five zones) and its EXCLUSIONS are a condition of the grant (`bench_occupancy` and the four per-turn allowances must stay whole-zone). Measured motivation: at whole-zone granularity `commutes` licensed ZERO pairs, making Issue #263's own worked triple unprovable. **Amendment E (built 2026-08-04, Issue #391):** not a ruling - the new-in-play bit (`appearThisTurn`) gets a `WRITABLE` zone, the `BodyView.new_in_play` read it never had, and declarations on `_PLAY` (write) and `_EVOLVE` (read AND write). Recorded here because it edits facts B3 and D4 state outright. It stays OUT of `ELEMENT_ZONES` - a declined widening, since joining is a licence and D's membership is the developer's grant. |
| 0099 | A whitelist entry is TYPED, names its fact, and (if provisional) names what retires it | Accepted (grilled 2026-08-01, Issue #259, wave-1 item 5). Amends ADR-0092 SS6 and enforces its own double-counting rule against the whitelist itself. The flat prose list failed its first real test: ONE board fact appeared **three times in three shapes**, and nothing about writing the line prompted the question. Every entry is now typed - `structural` (permanent; names the rule or policy), `provisional` (substrate-gap workaround; **must** carry a dated retirement test), `authored-scaffold` (a constant; **must** carry a reconciliation note + fitting-queue entry) - and names the board fact it guards, so the double-counting rule is checkable against the list itself. **Untyped entries are rejected by the T0 registry** (`src/common/sound_rules.py`, `validate()`); one declared `SCHEDULED_PAIRS` exemption covers the provisional-retires-into-structural pair and nothing else. Note the two entries on that fact share ONE `fact` string on purpose - distinguishing them by suffix made the detector pass VACUOUSLY, which is how it was first written and caught. **Amendment A (ruled 2026-08-01, Issue #263 ordering ruling):** a FOURTH type, `composed-into-the-leaf`, covering the four per-seam equations (`attach_value` ADR-0069, `evolve_value` ADR-0070, `promote_retreat_value` ADR-0073, `deploy_value` ADR-0086) that stop being DECIDERS under uniform 1-ply differencing but survive as `state_value` term-family internals - neither deleted nor whitelisted-as-rules. Mandatory field is the DESTINATION term family; the label string is shared verbatim with Issue #264's disposition table. The list now has two populations and the double-guard detector reads the deciders only. |
| 0100 | The promote/retreat equation is the sub-lethal residual, denominated in damage | Accepted (grilled 2026-07-27, `/grill-with-docs` on Issue #141 - twelve locked decisions); BUILT and shipped ON 2026-07-27 (`promote_retreat_value` in PROFILE). Phase 1c of the Value System (tracker Issue #136). Derives `PRIZE_DAMAGE_RATE` (hoisted to `common/currency.py` by ADR-0078) and retires the shadow record plus the `worth_it` sign bit; all seven `baseline_promote` rungs and the setup-retreat reluctance are DELETED as emergent. **Amendment (2026-07-29, Issue #172):** `ENERGY_RECOVER` is derived too, and was 6 % over. *(Authored as 0073 and NEVER renumbered at merge - it collided with Issue #164's fetch ADR, which merged ~11 h earlier and keeps 0073. The collision sat undetected for five days with this row missing entirely; renumbered to 0100 on 2026-08-01. See the numbering log above.)* |
| 0101 | The refresh SHED is a SET marginal over the hand, not a sum over the copies | Accepted; BUILT 2026-08-01 (Issue #261 POC-T2 item 2b). Amends ADR-0065 (WP-N4b's *the refresh site does NOT swap* is discharged) and ADR-0060 (whose SHED leg this re-denominates a second time). `_refresh_shed_keepcost` IS `needs.set_keep_v2` over the whole shuffled hand, so duplicates price MARGINALLY - a sum charged two copies of a wincon twice while shuffling EITHER alone loses nothing, and a refresh sheds the hand JOINTLY. **The arming bar WP-N4b set itself (sign-flips ~ 0) was never met** - 18 -> 13 -> 11 -> **16** across three amendments, over a corpus grown 83 -> 96 refresh decisions; ADR-0092 retired the bar, and that is stated rather than dressed up as the bar being cleared. Deleted with it: `_refresh_shed_shadow`, `Decision.refresh_shadow`, the `refresh_shadow` telemetry key, `needs_sweep.py`'s REFRESH half (ADR-0089 Probe Fate - a RULING's script dies with its answer), and `_refresh_cycle_adaptive` + `_REFRESH_BENCH_BODY`, newly dead once their only reader went (their promotion question re-homes onto T3's `development` term family). Measured: Discrimination Gate PASS / 0 moved, Decision Gate 2 FIX + 3 REGRESSION - every moved frame a shadow-predicted sign-flip - `discard agree_v2` 12/12 unmoved, and the promoted shed is FASTER than the summation it replaced (0.25 ms vs 0.32 ms). All three regressions are ONE mechanism, named not tuned: a swing inside +-3 of zero read as play/don't-play by `_finish_turn_last`'s `score > 0` promotion, which T4 replaces. **STRIP/GIFT (Issue #222) PARKED on measurement**, not deferred by fiat: the opponent rep is reachable on 115/132 refresh frames, but **59.4% of its cards price `role_value` = 0** (an opponent's attackers/wincons carry none of OUR declared roles), so design A would trade a flat constant for one biased in exactly ADR-0060's CRITICAL direction. The flats stay typed under `firing-equation-constants`; the prerequisite is named - `gusting-keepcost-design.md` SS2's shared role sheet. |
| 0102 | Hand-size relief is the SURVIVAL a refresh buys, on BOTH hands | Accepted; BUILT 2026-08-02 (Issue #261 POC-T2 item 2c, absorbing old Issue #221). Promotes the hand-disruption grill's design-B signal (rulings 1a/2a, 2026-07-19) from REPORTING-ONLY into `score`: `prize_to_damage(survival_value(D turns_to_ko_me at the hands the card leaves))` - two `turns_to_ko_me` reads across one counterfactual on the Damage Formula's own `atk_hand` / `def_hand` keys, in the sub-prize survival currency the deny / gust / snipe family already shares. **BOTH hands, because the set scales off both**: Alakazam's Powerful Hand (20/card, theirs, symmetric refreshes only) AND Mega Froslass ex's Resentful Refrain (861, **50**/card, MINE) + Chandelure's Mind Ruler (98, 30/card), which every refresh moves including a self-only Lillie's - a leg that fires on a real corpus frame (83967840-54, Lillie's at -46.67) and that a `handSizeDamage` guard would have zeroed, which is why decision 5 has no card-fact gate in front of the clock. Lever A returns as `needs.phase_scale`, NOT a second `_DENIAL_UNFAVORED` multiplier (**ADR-0078 decision 6**; ADR-0080 decision 3's withdrawal was conditioned on deny reading `phase_scale` on no surface, and this term reads it directly). Energy policy is `UNCHARGED`, the doom policy, because the CEILING reads an unresolvable cost fail-CLOSED. DELETED by name: `play-harlequin-vs-hand-size` (+25), `disrupt-when-unfavored` (+18), `strip-the-stacked-engine-hand` (+22) with `_STACKED_HAND`, the `disrupt-when-unfavored` tuned.json override, `OptionTrace.hand_size_relief` + telemetry `hs_relief`, and the two Board signals left unconsumed (`opp_has_hand_size_attacker`, `opp_draw_engine_in_play`). `needs.opponent_target_value`'s survival leg is extracted as SIGNED `needs.survival_value`. Measured: suite 4430 green, Decision Gate PASS (0 unruled), Discrimination Gate 2 OK->MISS **held out onto Issue #262 by user ruling** - both caused by the +25's deletion alone (neutralising the new term leaves them; restoring the +25 clears them) and neither is a DECISION move: the agent still plays the human's option on both. |
| 0103 | An exact tie breaks on class identity, not on menu position | Accepted; BUILT 2026-08-02 (Issue #261 POC-T2 item 2e, old Issue #254). **Reverses ADR-0091 decision 5's rejection** of fixing the rollout's order-dependence - not by taking the option that ADR rejected (exhausting the branch / canonicalising states inside the search) but because the premise was wrong: the develop rollout does not search, it re-runs the Pilot's own policy on each intermediate SearchState, so the order-dependence was the POLICY's and lived in one sort key. `_evaluate`'s `(score, needy_line_attach)` sort fell through to the ENGINE MENU INDEX, the one thing about an option that is not a board fact; two isomorphic boards present permuted menus, so the same exact tie resolved toward a different body on each and the lines diverged for good. The third key is now the ADR-0091 fingerprint, with the index demoted below it (it decides only between options that key EQUAL - the same decision, or unfingerprintable). `_greedy_grab` takes the same key. Measured on the committed corpus: **Leaf Lab CLASS ASYMMETRY 5 classes -> 0** (worst spread 2097.25), Discrimination Gate PASS / 0 moved, leaf SOLE-top 38->39, suite 4440 green. Unconditional and LIVE, not sim-only: a policy that breaks ties one way live and another way in its own simulation is simulating a different agent, which is the rung/instrument drift that rotted the decider sweeps. Consequence stated rather than hidden - which of several EXACTLY tied options is picked moves in live play too, because no total order is both permutation-invariant and index-agreeing. One Decision-Gate REGRESSION (`86089120|0|decision|14`, two Basic Energies onto Dreepy tied at 16.5) is wave-2 material and located: the attach decider's `attack_axis = max(this_turn, build)` looked like it discarded the `this_turn` 10.0 that tells an ARMING Energy from a building one - and the USER RULED that reading wrong (*'attaching either F or P energy to Dreepy should be equal value when it has no energy'*), so the frame is a **`transposition`** (ADR-0088): the ruling stands, its `correct` names one of an indistinguishable-by-value pair, both gates PASS with it voided. **Amendment A** (same day, item 2h): deleting the tuned `_DISCARD` ladder would have silenced three live cost-netting fetch rungs, because the ladder was ALSO the shed predictor's scoring basis; user-ruled to re-point the predictor onto the equation that decides the discard (`needs.removal_score`, `_apply_pitch_terms` and `_as_discard_rows` extracted so predictor and decider share one answer). Every ladder case committed as a corpus ruling is reproduced by the keep-value equation with the ladder gone. |
| 0104 | A hand card that can NEVER be played covers no slot: backward line topology is one oracle | Accepted; BUILT 2026-08-02 (Issue #288, POC-T3.5/10 - the term-sufficiency audit's F12). `development.line_topology` asks the FORWARD question; nothing asked whether a hand card's own PRE-EVOLUTION can still reach the board, so `slowking`'s 2x Metagross (Stage 2, `evolvesFrom` **Metang**, id 276 - the list runs neither Metang nor Beldum) priced as coverage. The gate lands on ELIGIBILITY in `pilot._resolve_needs`, not on any slot's value and not in `state_value`, so ONE fix serves both the rung system and the differencing system. **Measured, not recalled:** the shipped `deploy` factor already zeroed the slots keyed on the card itself, and the real defect was elsewhere - `grimmsnarl_ex`'s stranded Froslass COVERED the `draw_engine` slot (so the live Supporter beside it priced 0 and shed for free) and RAISED its band 12 vs 8, because the band reads off its eligible rows. The question is the CHAIN, not one hop, and it includes the **Rare Candy** escape (card text id 1079) - without it the gate would strip `grimmsnarl_ex`'s win condition with the enabler in hand. Fails OPEN throughout (*unreadable is not unplayable*), and as a WHOLE without stats or Function Tags. Consolidates four callers onto `common.playability`: `planner._deploy_odds`' inlined one-hop walk and `_stranded_evolution_set`'s private recursion are DELETED (both latent bugs, both provably silent on the four shipped decks - which is how they survived), `gate_library.deploy_odds` keeps only the arithmetic, and the three identical unseen-deck walks become `_unseen_deck_counts`. `_RARE_CANDY_ID` is deleted for the `rare_candy` Function Tag (ADR-0006) now that it has a second consumer. *(Authored as 0103; renumbered to 0104 at /open-pr rebase time - PR #307 had already claimed 0103 while in flight, and a number claimed by an open PR is taken even though a disk scan cannot see it.)* **Measured** (re-stated against the rebased base): suite 4505 passed / 0 failed, BOTH gates PASS with zero unruled flips and neither baseline re-captured by this branch - Decision Gate 2 FIX (83038055-40, 83665798-39, both now the human's option) and no new REGRESSION; Discrimination Gate no new OK->MISS. The only moved-and-red frames in either gate are the pairs already held out onto Issue #262 / Issue #272. |
| 0105 | A free Item's HOLD price is the keep machinery, FLOORED; and the free band splits on information | Accepted; BUILT 2026-08-02 (Issue #261 POC-T2 item 2f, discharging old Issue #212). Amends ADR-0062 (whose `_DENIAL_ITEM_COST = 10` is DELETED) and ADR-0078 (whose worth<->damage catalogue gains its trainer row as a real constant instead of a ratio between two that never met in an expression); **discharges ADR-0095 decision 1**, the sequencer change that ADR assigns to T2 and no other item owned. The constant's rationale - *an Item is finite, and `_finish_turn_last` tiers a free Item ahead of everything, so a purely positive term can never DECLINE one* - is a property of the SEQUENCER and was priced for Crushing / Enhanced Hammer and no other card in the pool. It becomes `max(needs.keep_v2(card), hold_value.ITEM_HOLD_FLOOR) x currency.ITEM_HOLD_WORTH_RATE`, resolved by `Pilot._item_hold_price` off the same `_resolve_needs` the refresh SHED and the discard decider already decide on. **The floor is load-bearing and was MEASURED before it was written:** a role-less Hammer's `keep_v2` reads **0.00 / 4.82 / 3.57 / 0.00** on the four committed deny anchors, because its only slot is the very `deny` slot the fire rung is pricing - so a pure keep reading collapses to 0 on exactly the boards where the strip whiffs and re-opens ADR-0093 decision 4's defect, and two copies solo-price 0 each against one slot (sets-not-sums), making the copy being spent look free precisely when the hand is richest in it. `max`, not `+`. Because the floor binds on every anchor the swap is identical for deny on every ruled frame - Issue #212's *must not perturb the deny 5/5* met by MEASUREMENT, stated as such rather than as *by construction*, since `keep_v2 > floor` is reachable and would legitimately cost more. **Decision 4 lands ADR-0095's boundary:** the free band splits on INFORMATION rather than cost (`0 informative / 1 committing / 2 Supporter / 3 attach / 4 shuffle / 5 ender`), classified by `Pilot._informative_card` off a `draw`/`search`/`dig` Function Tag or a Pokemon (Bench fill), **untagged defaulting to COMMITTING**, scoped to `_PLAY`. It cannot be a score and never could: digging first and digging second reach the SAME end state (ADR-0095 decision 3), which is why the `information-before-commitment` whitelist entry described an intent until now. **ADR-0095's falsifiable prediction HELD** - `82225643|1|decision|11` takes the human's Pokegear while the Hammer stays ENDORSED at +22.50 (an ordering, not a suppression, exactly as the ruling asks), and the dig is the LOWEST-scoring endorsed option on that menu. ADR-0095's owed Function Tag audit is RUN and pinned: 7 informative / 8 committing across the three shipped decks, no misclassification. **Measured** (re-stated against the rebased base): suite 4540, Discrimination Gate PASS (held-outs unchanged, `82225643|1|decision|12` IMPROVED MISS -> OK), Decision Gate surfaced ONE REGRESSION on that same frame - the next action of the same turn - which was NOT self-ruled but put to the user and **USER-RULED ACCEPT 2026-08-02** (`fixed`, non-voiding): f11's ruling asks for Pokegear-then-Hammer in one turn, and f12's own `correct` was adjudicating Hammer-vs-ATTACK. A `covered` note from 2026-06-29 had already reached the same reading and is preserved verbatim rather than overwritten. Decider baseline re-captured at the ruling commit (its blast radius named in `docs/ci.md`); the leaf baseline deliberately NOT re-captured - it passes, and re-capturing a passing gate absorbs movement nobody ruled. |
| 0106 | Deadness RANKS a discard; it does not price one | Accepted (user-ruled 2026-08-02 on Issue #294, option 1 of two offered); BUILT. Amends **ADR-0065** - it corrects a WP-N3 build note (*"the deploy-dead Cinderace sheds before a live spare"*) that was never true in shipped play - and applies **ADR-0103** decision 1 one layer up, at the needs assignment's removal ranking rather than the Pilot's option ordering. The forced-discard rows have carried a `pitch` deadness term since the seam-D grill's Finding 3; the assignment that DECIDES never read it, so two RULED cases shed the wrong card - the dead-opener tie fell through to the MENU INDEX, and `83454549-36`'s spent burst was KEPT because residual worth (`worth x deploy`) reads the CATALOG tier a corpse still carries (Ignition 30 vs a live spare's 0). `needs.cheapest_removal`'s key becomes `(removal_score, -sum deadness, sum residual_worth, indices)`. **Deadness RANKS, it does not PRICE**: for a dead card `P(met|keep) == P(met|pitch) == 0` and ADR-0065's own `keep_cost = Worth x Odds x Gates` is a product of clamped non-negative factors, so the issue's own preferred reading (*"a dead card's keep is not 0, it is negative"*) would have to INVENT a magnitude - the rung-fitting ADR-0092 deletes, refused on the same ground ADR-0103 Amendment A refused a replacement `live` band. The leg is also ORDERING-ONLY, so no amount of deadness can make a removal look cheaper than one that genuinely costs more. Scope item 3 answered without spin: the doctrine RE-DERIVES v1's ordering leg for leg; what answers the issue is that it is derived from ADR-0065's algebra and lives in ONE place, since what item 2h deleted was a second DEFINITION of "cheapest to lose", not the order. Two defects found by building it: **fuel is not deadness** (it FILLS a slot and `pitch_gain` already prices it, so ranking on the `pitch` count double-prices it and shed the attack's only funder on `83966336|0|decision|27`, Discrimination `OK -> MISS` rank 1->3 - the row now carries a categorical `deadness` bit beside the unchanged `pitch` COUNT); and the leg was first written as a SUM of five booleans, the very magnitude reading the ADR forbids. Both legs spelled ONCE in `Pilot._removal_ranking_legs` - a fourth seam in ADR-0103 Amendment A's series - because the shed PREDICTOR must rank identically to the decider or it stops predicting it; that seam is where the change actually bites (5 of 59 corpus calls move, all `_shed_signals`) and it had NO test. **Issue #294's own scope item 2 did not survive measurement**: it required a wave ruling because "it moves every forced discard in the corpus", and it moves ZERO - both gates byte-identical to the pre-change tree (Decision 251/345, context-8 10/11; Discrimination 178/247), so no ruling was owed and none was made, the ADR-0093 decision-1 outcome. Re-measured after the /open-pr rebase, where main had moved: Decision 251/345 with 0 picks moved, Discrimination 179/247 with 3 - and all three shown to be the BASE's by re-running with `src/` at origin/main, so the branch's own gate contribution is still zero. `discard agree_v2` cannot be re-read at all - `needs_sweep.py` compared v2 against the live v1 decider and both died with item 2h. Suite 4405 green. |
| 0107 | A prize-denominated marginal enters the Worth DP as a FRACTION of a band, not as a number; and the gust-target instrument was binding on one corpus frame in eighty | Accepted; BUILT 2026-08-02 (Issue #313 item 2g, the target-side half carved out of Issue #261 POC-T2). Amends ADR-0076 (whose decision 1 priced the `gust_target` slot in prize-equivalents inside a Worth-summing DP, and whose Amendment E recorded that as a debt calling it *"latent, not firing"*) and ADR-0078 (whose scale catalogue gains its first **prize<->worth** row); discharges the debt **ADR-0080 decision 4** re-inherited here with Amendment F reversed, and old Issue #189's target/keep side. Follows ADR-0086 amendment C and ADR-0105 decision 3 in FORM: a seam-scoped rate, stated plainly, carrying a reconciliation debt. **"Latent" was too kind, and the honest measurement had to be taken twice.** A first pass argued the inertness *structurally* - *"3.9 < 4.5 on every board there is"* - and `/code-review` refuted it: the competing `general` slot is `worth x deploy x _GENERAL_WORTH_W x liq`, so 4.5 is its CEILING, not a floor, and a de-duplicated general slot leaves a SECOND copy of a gust card facing no competitor at all. Asked properly (zero the kind's slots, re-run `assignment_value`, watch V drop), the assignment covered a `gust_target` slot on **1 corpus frame in 80** - so the flag armed 2026-07-27 after a gauntlet, a 331-frame sweep and the Discrimination Gate had been deciding one board in eighty, and all three reported clean because of it. **The fix needs no general rate:** `opponent_target_value` has a DERIVED ceiling (`needs.TARGET_VALUE_CEILING` = `MAX_PRIZE_VALUE` 3 + `_SURVIVAL_CAP` 0.9 = 3.9, exactly the *"max ~3.9"* Amendment E quotes), so dividing by it turns the marginal into a dimensionless [0,1] fraction of `currency.GUST_TARGET_BAND` (= `TAG_TIER["gust"]`) - ADR-0086 amendment B's deploy argument with the prize points cancelling instead of the worth points. `test_currency.py`'s guard against a general `WORTH_DAMAGE_RATE` / `prize_to_worth` is untouched and still passes, which is the check on the claim. The two members of the opponent-target slot family are finally one SHAPE: `deny` is `TAG_TIER["gust"] x relevance[0,1]` (ADR-0080 decision 3) and `gust_target` now is too. **The band is a PRESERVATION choice with a measured incumbent**, not a range: over the same 228 slots the routing ADR-0076 replaced (`TAG_TIER["gust"] / 2**t`) priced median **2.500** / mean 2.695, the new denomination median **2.564** / mean 3.082. It adds no new NUMBER - both ends already shipped. **A ~39x disagreement is RECORDED, not reconciled:** composing the shipped legs (`PRIZE_DAMAGE_RATE` 100 / `ITEM_HOLD_WORTH_RATE` 1.0) says ~100 worth per prize against this seam's ~2.56, which is evidence about the Worth scale (0-30 by construction, so a 100-point slot would erase every other card's contribution rather than price one) and is `state_value.POC_WORTH_PRIZE_RATE`'s to settle (ADR-0097, Issue #262). `gusting-keepcost-design.md`'s *"There is NO worth-points<->prizes conversion"* gains a scope qualifier rather than being reversed by side effect: it governs the play-side equation it sits in. **Measured:** assignment coverage 1/80 -> 25/80; suite 4544; BOTH gates PASS with ZERO movement (Decision 0 picks moved / agree 251/345; Discrimination byte-identical to baseline), so there are no wave-2 flips to rule and neither baseline is re-captured. The five gust whether-to-play rungs are UNTOUCHED - their deletion is T4's, Issue #263. |
| 0108 | A shipped override carries the evidence that established it, and an unaudited one says so | Accepted; BUILT 2026-08-02 (Issue #224; user-ruled option 1 of four - classify + freeze, value-preserving). **Refines ADR-0032** and **completes ADR-0083**, which fixed the FITTER and left the auditability gap it created as owed. `attack_overrides.json` ships 117 facts the damage oracle trusts over the text parsers; their measurements lived in gitignored `reports/attack_audit/` and, as of this build, exist NOWHERE - so an override could be re-derived but never CHECKED. That is how 274 (Skeledirge, Torcherto) shipped `atk_hand`/5 for a combined-bench scaler: bench was the one variable the harness neither swept nor recorded, so hand size was the only thing the conservative fitter could see, and nobody could look. **A committed SIDECAR** (`attack_overrides.provenance.json`), emitted in the SAME pass as the table so the two cannot describe different derivations - sidecar not inline because ADR-0032 wants the table readable and `load_attack_overrides` parses it on every provider build, on the grader, inside a 10-min match budget; `tools/submit/package.py` excludes it, whose `_IGNORE` pruned docs by EXTENSION and would have shipped 24 KB of evidence called `.json` (bundle 30 KB -> 6 KB, measured). Each of the three derivation rules now returns `(fields, used)`, so provenance is a BY-PRODUCT of deriving rather than a second drift-prone description. **A fit keeps the REJECTED axes**: a flat hand axis proves hand size was measured and ruled out, and its absence is what made 274's fit possible - the gate demands a measured FLAT axis, since 'at least two axes' is satisfied by the two winners of a `both_bench` fit. Three methods, because *measured* / *read off the card* / *nobody knows* must not look alike: **2 `engine_fit`** (274 + 371, transcribed from the issue's preserved table, the only surviving copy - per-row hand/energy are `null` because the raw file is gone, not invented), **4 `text_verified`** (Issue #225's scalers, owner Issue #275; 120 and 425 provably unfittable on today's axes), **111 `unaudited`** frozen at their exact shipped values. Gate teeth: an override with no row fails, a row whose `fields` differ from the table fails, and the debt is MONOTONE in both the id set and the counts - backfilling costs no test edit, growing either is loud. Merge rule, the part that generalises: **the generator may retract what it AUTHORED** (a fit that no longer holds is dropped - the 274 outcome) **never what a human RULED**, or a per-attack recapture would delete the other 116 entries and revert 425 Tenacious Tail to computing ZERO. Re-driving the pool was REJECTED on correctness, not cost: it changes shipped values rather than documenting them (ADR-0083's own Consequences call the old measurements stale) and, the generator being authoritative, silently deletes all four text-verified entries. **Nothing shipped changed value** - a regenerate over an EMPTY measurement set reproduces both stores byte-for-byte, asserted end-to-end. Found here and ruled next door: `_coin_bounds` keyed its fork pair on `coin` alone, so forks from different sweep points overwrote each other and the bound was an arbitrary survivor - out of scope for a value-preserving change, so every vanilla fork record is now recorded (which is what made the collapse legible) and a test fails when a fit's own evidence contradicts itself; **resolved by ADR-0083 Amendment A**, which scopes the bound to a board and forbids it beside a fitted scaler. Writes pinned CRLF (`write_text` inherits the platform newline, so one run emitted different bytes per OS). `.github/filters.yml` names both stores under `sim`: `common_agent_core` matches `src/common/*.py` - .py ONLY - so a table-only diff reached the suite via the `any` fail-safe, by accident. Suite 4581 passed / 4 skipped / 3 xfailed. |
| 0109 | A conditional payoff is the damage oracle's question, not a new condition vocabulary | Accepted; BUILT 2026-08-02 (`/implement` on Issue #287, T3.5/9). Implements the term-sufficiency audit's **F11**. `readiness` read its payoff off the PRINTED `CardStat.maxDamage`, so a Solrock with no Lunatone benched scored exactly the one that had it and benching the enabler moved the term by **0** — never explored under 1-ply ordering. `StateModel.payoff` now returns `(attack_id, damage)` for the best attack a body can pay off with ON THIS BOARD, priced through the shipped damage oracle (matchup-free, `bound="exact"`), so the `requiresBench` gate ADR-0032 already owned reaches the term; a gated maximum falls back to the best paying attack rather than zeroing the body, and the odds leg is asked about THAT attack. `BodyView.payoff_attack` DELETED (it could not see the Bench). **REFUTES Issue #287's own fix**: the proposed `condition`-clause vocabulary is unnecessary (the parser already reads the card text) and 2 of its 3 named cards are not this bug — Metagross's +150 was never in `maxDamage`, Kyurem's Plasma Bane is a COST gate. Measured: 1.80 µs/body, no `state_value` regression. |
| 0110 | A gate flip caused by a re-ruling is LABELLED, never excused | Accepted (agent-grilled 2026-08-02 on Issue #230, batched issue-sequence run); BUILT. Extends **ADR-0087** decision 7 (`ruling_moves`, the never-gating detector this consumes) and leaves **ADR-0072**'s Discrimination Gate verdict rule EXACTLY as it was; rides beside **ADR-0094** - that one guards *whether* a re-capture may be written, this one writes down *where it must be taken from*, the half 0094's guard structurally cannot see. `leaf_lab_diff` compares `correct_is_top`, a field FROZEN into each capture and computed under that capture's own `correct`, so when a human re-rules a frame the diff grades its two halves under two different oracles and prints `REGRESSED ... OK -> MISS` about a build that did not move. **The defect is the LABEL, not the redness** - the gate is right to be red (its reference was captured under a ruling that no longer stands, so it cannot speak about that frame), it was wrong about why. Probed directly with a positive control: a ruling-move flip and a genuine regression were indistinguishable in both the readout and the verdict. **Decision 1: a Ruling-Move flip still FAILS the gate; it is not excused.** `discrimination_gate_verdict` reads `ok_to_miss` WHOLE and never consults the new key - an excuse keyed on "the ruling also moved" would let a real regression hide behind a same-commit re-rule, and a gate getting quieter as a side effect is the one direction a gate must never move (ADR-0085 Amendment I). Enforced STRUCTURALLY, not by convention: `stale_baseline` holds the very entry objects `ok_to_miss` holds, so "it stays gated" is an identity rather than a promise, and the verdict function returns the identical answer on every input before and after - **verdict-neutral by construction**, which is why neither committed baseline is owed a re-capture. |
| 0111 | A DECLINE is a ruling, and the writer must accept the shape the reader already grades | Accepted (agent-grilled 2026-08-02 on Issue #229, batched issue-sequence run); BUILT. **Amends ADR-0049** - the Scope contract's `decision` row read *"`correct` mandatory"* and now carries one exception; leaves **ADR-0015** untouched (a `correct` that names options still indexes the Anchor) and re-rules no record. An OPTIONAL engine select (`minCount == 0`) puts *"take none"* on the table as a legal answer, and a human ruling exactly that could not say it: `build_correction` required a non-empty `correct` at `decision` scope, so the only writable record named the option the agent had already taken - `chosen == correct`, which reads as *"the pick was right"*, the opposite of the intended ruling. **The issue's framing did not survive inspection.** It proposed a `declines: true` field because an empty list "already means something at `turn` scope"; but `gates.satisfies_human` ALREADY reads an empty `correct` as a recorded DECLINE, matched exactly and never by subset, and it takes no `scope` argument at all - so the READER has always graded this shape at every scope, and a new field would have been a second way to say what one already meant. The writer is what disagreed. **Two refusals, not one**: the tagging pane (`shell.py`) rejected the shape client-side too, unconditionally, which the issue did not mention. Census taken through the Corpus Reader and never by walking raw JSONL (23 records carry no explicit `scope` and only default to `decision` inside `Correction.from_dict`, so a raw walk mis-scopes them), with a 362-record positive control: the degenerate shape is **two** frames, not the three the issue body tabulated - the third is a legitimate `turn`-scope decline. |
| 0112 | A gate REPORTS what it cannot grade properly; it never quietly stops grading it | Accepted (agent-grilled 2026-08-02 on Issue #251, batched issue-sequence run); BUILT. **Closes the question ADR-0089 left open** - that ADR lifted `records_a_decline_it_cannot_state` into `gates.py` UNWIRED and named Issue #251 as the owner of *"which frames stop gating"*. The answer is **none of them**. Builds on **ADR-0111**, which made the record repairable and is the reason this ruling can go the way it does; amends nothing, and neither gate verdict function changes. Two states were on the table and **a third was not noticed until building: unwired-and-unreported is not neutral, it is the weakest of the three** - the exposure was already invisible, with nothing saying so. So the predicate is REPORTED and never excluded. Three reasons in binding order: an exclusion makes a `main` watchdog permanently quieter, which is the exact property that let four `*_decider_sweep.py` report PASS for weeks in a state where PASS could not be told from FAIL (ADR-0089); the exclusion OUTLIVES its cause, keeping the frames ungraded after the record is repaired and they become perfectly gradeable; and a report costs nothing a gate needs. Verified with positive controls throughout - the predicate fires on exactly 2 of 372 records; `is_leaf_frame` is False on both, with a control showing it True on 278 of the same records, so the Discrimination Gate genuinely does not share the exposure; and the spec's own tally claim *"the tally reads `unstatable 0`"* is refuted, it is 2. |
| 0113 | The store is an ARCHIVE, so the writer's rules are re-applied as a REPORT, never as a loader | Accepted (agent-grilled 2026-08-02 on Issue #256, batched issue-sequence run); decisions 2 and 4 BUILT. Decision 1 is now **zero-instance** and decision 3 was **SUPERSEDED** (Amendment A, 2026-08-03): the developer reviewed the packet, found the spec's own reason for rejecting a `decision`-scope re-tag was false (the rationale was never a whole-match note) and that a hoped-for backup fixture (`ml_dont_bench_redundant_solrock_f51.json`) was orphaned (no `frame_key`, unwired), and ruled a fourth option — re-scope `85709280|1|match|` to `decision`/subject 51, keeping `correct: [0]` AND its genuinely match-level `span` (an ADR-0045 Game Plan trace). Executed; also required re-keying one `reviewed.json` entry the scope change orphaned. Both gates verdict-neutral (Decision agree 250/347 unchanged; Discrimination 197 gated, was 198). **ADR-0087 already flagged this exact fix as deferred to the developer** — a citation Issue #256 missed at filing. Sits beside **ADR-0112** (that one reports a record that cannot *state* its ruling; this one reports a record the writer would not have *created*) and builds on **ADR-0111**. `Correction.from_dict` - THE loader - validates nothing, so the store can silently hold a shape `build_correction` refuses; the shape audit (decision 4, BUILT) reports such shapes without excluding them, and the corpus holds zero today. Positive controls for every zero in the shape audit: the reader returns 372 records and the audit calls all of them clean now, so it is neither an empty corpus nor a predicate that flags everything. |
| 0114 | A coverage claim names a rule that EXISTS, and expiry is made loud | Accepted (agent-grilled 2026-08-02 on Issue #238, batched issue-sequence run); decisions 1-6 BUILT. Issue #238's items 1-3 - opening 27 frames and ruling each - are **handed back to the developer**, not built. Sits beside **ADR-0113** and **ADR-0112**, repeating their ruling one store over: those re-apply the writer's rules to committed **Correction** records and REPORT, this re-applies one to the committed **review ledger** (`data/corrections/reviewed.json`) and reports. All three report; none excludes, and none moves a gate number. A `covered` disposition claims a named rule already handles the frame, and nothing checked that the rule still EXISTS - a renamed or deleted rung leaves the claim standing and the frame permanently set aside on the authority of something that is gone. The validator resolves each claim against the live rule vocabulary at source, and expiry is made LOUD (a worklist) rather than silent. |
| 0115 | A scaler predicate that is OPEN takes an argument; one that is CLOSED keeps its flat name | Accepted (developer-ruled 2026-08-03 on Issue #361 - "GROW THE VOCABULARY: a filtered-count family"); BUILT. Extends ADR-0083 §4 and supersedes, for open predicates only, the flat-names-only reading Issue #225 / POC-T1 recorded in `src/common/CONTEXT.md`. Two shipped `unaudited` overrides (attacks 651 Team Rocket's Weezing, 708 Palpitoad) froze a per-unit "X damage for each..." attack at ONE board's count with no `scaleVar` at all, an `over_prediction` invisible only because neither sat on `_GATE_ATTACKS`. The split: a filtered count is **closed** (finite, engine-known argument set - a stage, a rule box, a damage counter) and Issue #225's flat-name/pre-reduced-integer reading stands unchanged for those three; it is **open** (an arbitrary name substring, an arbitrary attack name) when flattening would hardcode a card list into the vocabulary instead of naming a fact - the next card with the same text needing a new NAME rather than a new argument is ADR-0083 §4's own test for growing a class, and the pool already holds four `Round` attacks differing only in per-unit. Mechanism (ruled over a rejected alternative, priced not assumed): the argument rides on a new `AttackStat.scaleFilter`, resolved at lookup beside the existing `atk_discard_energy` exception - generalizing the one documented exception to "every variable name IS a context key" into a class of three - rather than a filter-to-count mapping in `damage_context()`, which cannot work because the context is built once per direction and the filter lives on an attack the builder has never seen. Engine and agent are bound by VALUE parity, not spelling (they have always differed); `chain_overrides.json` un-defers attack 651, which cgpy had raised `UnsupportedCard` on. Both rows move `unaudited` → `text_verified`; the card-identity sweep axis needed to promote either to `engine_fit` does not exist and is owed to **Issue #364**. Three sibling `Round` attacks (707/710/1214) stay under-read on purpose, owed to **Issue #365** - shipping them here would add three more moved numbers to one gate-attribution window. Measured leaf-cost regression (+13.7 µs `damage_context`, +29.7 µs whole leaf, interleaved paired A/B over 40 frames) accepted and pinned in `test_leaf_profile.py`. |
| 0116 | Committed line-ending policy | Accepted; BUILT 2026-08-04 for Issue #367. Adds `.gitattributes` with `* text=auto`, LF-normalized Python, byte-preserving `data/**` and `src/cg/**`, and binary rules. Chooses Issue #367 option 1; rejects one-time `.py` renormalization to avoid a large mechanical conflict window; rejects accepting recurrence because the issue records repeated whole-file conflict costs. Guarded by `tests/test_line_endings_policy.py`: `git check-attr` verifies policy and two temp repos prove CRLF/LF-written Python commits to the same blob. `data/**`, `src/cg/**`, `data/leaf_lab/baseline.json`, and `data/decider_lab/baseline.json` remain untouched. |
| 0117 | The survival clock is read fractionally, and the Flat Tie is measured to be STRUCTURAL | Accepted (grill of Issue #398, 2026-08-05); BUILT (PR (a) of a three-PR sequence). Amends **ADR-0071 decision 4**. `CombatMath.turns_to_ko_me` accumulates `incoming()` and rounds to the first integer turn `dealt >= hp`, discarding the crossing's exact position; `survival_clock()` now returns `SurvivalClock(turns, exact)` from the SAME accumulation, so the integer stays byte-identical (asserted literally) while `pilot._opponent_target_rows`' `survival_shift` opts into `.exact`. **Self-corrected before merge, twice, both against its own build-time measurement.** First: the ADR's own premise — *"the cause is quantization"* — measured false; quantization is 10.0% of the Flat Tie (a strict majority is a **Structural Zero**, `incoming()` being a per-turn MAX means only a body that uniquely LEADS that max at some turn can score, at any resolution), so the two alternatives the grill rejected (an authored feature blend, and roles-supplying-magnitude — Issue #395's shape) are RE-OPENED, not retained. Second: the correction's OWN tie instrument compared `survival_shift` rather than `value` (the field the ranking actually sorts on and that falls to list order), which biased both directions at once — corrected to 281/343 (81.9%) tied pre-change, 253/343 (73.8%) post, 28 groups recovered. A third claim of the author's own — *"at most one body per board can score"* — was constructed-counter-example-refuted during review (the per-turn lead can change as energy budgets grow) and is pinned as a regression test. Decision Gate PASS both before and after, `agree 251/340`, **0 picks moved** — this PR ships no behavioural change on the corpus and is offered as a **prerequisite for**, never as, the fix to Issue #398. `tools/train/probes/fractional_clock_sweep.py` is the standing instrument, reporting a mandatory sham arm per **ADR-0118**. |
| 0118 | An argmax probe reports nothing without a sham leg as its null | Accepted (grill of Issue #398, 2026-08-05); BUILT. Methodology, applying to every probe that reports "how often does the top pick move" — the first application caught the false headline **ADR-0117** was originally built on: a real denial-credit leg moved a bench argmax on 69/241 frames, five frames from what a meaningless `cid % 7` leg of the same magnitude band moved (64/241), which was never evidence and reached an issue body, a committed docstring, and a commit message before a reviewer challenged the premise rather than any instrument. Two controls are required and are not interchangeable: a **null control** (an arm against itself, expect 0) proves the comparison is stable; a **sham control** (a term in the same magnitude band with no causal claim) proves observed movement is attributable — the null alone cannot, since it introduces no term. Extracted as shared vocabulary (`tools/train/probes/_sham.py`, on `_corpus.py`'s precedent) once a second probe needed the same shams and started drifting from the first. **Amended same day** by its own first re-application: a movement number is the WRONG instrument for a leg whose correct behaviour is to leave a Structural Zero alone — under-movement against a sham is not thereby falsified, and the reading note says so. |
| 0119 | Denial is the LINE's prize, and the Structural Zero is correct rather than a defect | Accepted (grill of Issue #398 PR (b), 2026-08-05); BUILT. **Amends ADR-0117** and supersedes ADR-0051 Phase 3b's `_WINCON_DENIAL_PRIZES`. ADR-0117 measured the Structural Zero correctly and then called it the defect; `docs/rules.md` §3 says otherwise — *"Attack — 1, and it ends the turn"* — so `incoming()`'s per-turn MAX matches the rule and `survival_shift`'s winner-take-all shape is CORRECT, not broken. No counter-example was constructible. What was actually missing is the prize a removed LINE would have presented: `needs.line_prize_advance` reads `own + (max_line_prize - own) x halve(hops)` over the new card-knowledge primitives `CombatMath.forward_line_prize` / `TheirSide.forward_line_prize`, so a 1-prize Staryu one hop from a 3-prize Mega Starmie ex prices at 2. Bounded by `MAX_PRIZE_VALUE` BY CONSTRUCTION, which is load-bearing rather than tidy: `TARGET_VALUE_CEILING` stays 3.9 and neither rate derived from it (`state_value._THREAT_W`, `currency.GUST_TARGET_WORTH_RATE`) moves — an additive leg would have rescaled both before the new reading did anything. Hops are hops to the best-PRIZE form, a THIRD aggregation off `_forward_hop_depths`, distinct from `ForwardPayoff.hops` (best-DAMAGE, Issue #285). `state_value._denied_forward_payoff` DELETED — it priced forward DAMAGE, which `incoming()`'s all-descendants gate already puts inside `survival`'s clock, so it was one quantity counted twice at a band 11x off the ranking's; `threat.reads` swaps `denied_forward_payoff` for `denied_line_prize` and `double_counted()` could not have caught the old overlap because the two families spelled one fact differently. `threat` keeps `survival_shift=0` and Issue #260's owed removal-delta accessor is RETIRED AS NOT OWED — passing a real delta there would double-count against `survival`; the live ranking passes one because it has no "after" to difference against, two idioms for one quantity and the split is structural. `_gust_forward_denial` moves off the PRINTED forward index (which reads Alakazam at 10 and scored a lethal scaling line at exactly 0) onto board-priced `forward_threat_ceiling`, as a magnitude rather than a threshold, riding Issue #213's existing `scaled_threat_rank` lever so OFF still restores the printed read byte-for-byte — the developer ruled that over the spec's "delete both constants", which would additionally have BROKEN `_EVOLVING_THREAT_DMG`'s two live consumers. **Measured, and the two halves read differently on purpose:** Flat Ties on `value` 253/343 -> 139/343 (114 groups, against PR (a)'s 28) is decisive and is a direct count; the sham comparison is WEAK — the leg beats content shams by 8 and 14 frames of 241 and loses to the position sham, and 8 frames is not a pass when ADR-0117's retraction turned on rejecting 5. The decision rests on the tie count and the constant deletions, NOT on discrimination, which the ADR flags as OPEN. Decision Gate PASS, agree 251/340, 0 picks moved. Suite 5207. Also amends **ADR-0118** with a SPARSITY-match rule: band alone is not a match, because a dense sham beats a sparse leg on volume. |
| 0120 | The opponent role sheet is an ORDINAL PRIORITY, not a worth — and it did not measure | Accepted (design session on Issue #395, 2026-08-05); BUILT. **Amends ADR-0051** (its role table becomes a declared `ROLE_REGISTRY` and gains `attacker` 50 / `enabler` 40 / `support` 0 / `unknown` 0) and supersedes `gusting-keepcost-design.md` §2, which designed the same sheet as a WORTH. The ordinal home is FORCED, not chosen: `test_needs.py` asserts every `card_worth.ROLE_TIER` role names a slot kind it can supply and every slot kind is a need of OURS, so an opponent role there turns CI red with no honest slot to name; the two tables also already collide on `engine` (12.0 *keep it* vs 0 *do not boost*). Three measured defects closed: **530** shipped `attacker` assignments resolved to 0 through `_ROLE_PRIORITY.get(role, 0)` with no error, log or test; the `avoid` −80 fired on Mega Kangaskhan ex (3 prizes) and Fezandipiti ex (2) at FULL strength (γ-independent `_GENERAL` provenance) and OVERWROTE the dossier's `primary_attacker`, now gated on `prize_value == 1` off the existing primitive with no new constant; and eleven hand-authored Function Tag instances — `prevent_ex_damage` on 345 Crustle among them — were deleted silently by `build_card_functions.py --fresh`, which `test_card_functions_oracle.py` could not see because it asserted overrides ⊆ table and only that direction. `card_tags.py` closes the tag vocabulary the way `snapshot_coverage` closed the clause one (declare → walk the artifact → teeth → vacuity guard + positive control), with `DERIVED_TAGS` EXPORTED from the classifier rather than re-transcribed. The general tier becomes the pure `derive_general_roles` over `BodyFacts` the Pilot supplies; every input already shipped, so `enabler` needed no new tag. One deliberate divergence from the issue's spec, recorded: `avoid` reads `_UTILITY_TAGS` not `_ENGINE_TAGS`, because the literal rule would put −80 on a 1-prize `energy_accel` body — on Cinderace, a deck's MAIN attacker — and `strategy/context.py` had already ruled that distinction for the mirror-image question. ⚠️ **The measurement is a NULL result and the ADR says so**: on the FULL gust path (`doctrine_gust` included, arm-patched at `MatchupPlan.priority`), sparsity-matched per ADR-0118, ties counted on the sorted field — the sheet moved 5/247 bench argmax against matched shams at 7, 8 and 29, losing to the position floor, and dissolved ONE Flat Tie group of 237. The probe's pre-registration predicted it would clear and is left standing unedited. So the change is justified by the defects it closes and NOT by discrimination, ADR-0119's shape. Also fixed: the Brief Tiebreak read the RAW role priority while the relevance multiplier stands a positive one down via `brief_boost_gated()`, so the benched-Tera structural veto lifted off `-KO_SCORE` — latent before, reached daily by the widening. Does **NOT** discharge ADR-0101's parked hand-disruption prerequisite: that needs a WORTH over cards we cannot see, this is an ordinal priority over bodies in play; both copies of the claim were rewritten in the same commit and `firing-equation-constants.entry` extended to name `_ROLE_PRIORITY`. Suite 5237, both ADR-0072 gates PASS. |
| 0121 | A Deferred-Target Option is EXPANDED — not refused, and never scored at zero | Accepted (grilled 2026-08-04 on Issue #392, seven locked decisions); BUILT (PR #407). **Amends ADR-0100 twice** (decisions 5 and 6) and **blocks Issue #385**; spawned Issue #395. A `_RETREAT` option is the bare dict `{"type": 12}` — **5807 of 5807** offered options in the parity corpus, 146 of 146 in the corrections corpus — so the engine poses the cost (`_DISCARD_ENERGY` ctx 30) and the promotion (`_SWITCH` ctx 3) as separate selects and its 1-ply delta is the retreat-allowance bit ALONE. The grill called that a near-zero; the build measured it **exactly 0.0** on both acceptance frames, because `board_delta._retreat` writes `allowance_retreat_used` and no `state_value` family reads it. Expanded, the same option scores f32 **+0.00075** over 2 classes and f35 **+0.00186** over 4. **The census is what turned a fix into an architecture**: 63 further `_PLAY` steps defer their target the same way (Boss's Orders ×30, Crispin ×16, Wally's Compassion ×14, Rosa's Encouragement ×2), and it is **not a coverage gap** — all four are `covers: "full"` and `CLAUSE_SELECTORS["target"]` already records the target CLASS; only the INSTANCE is absent, which only the board supplies. The defect was one shape with two fates: a `_PLAY` REFUSED loudly while a `_RETREAT` priced ~0.0 and was pruned SILENTLY. D1 puts expansion in a composer-local module (`common/board_choice.py`) — **not a beam whitelist and not a seam change**; `board_delta._retreat` stays allowance-only and parity-honest. D2 is data-driven off the compendium target vocabulary (24 declared values → 14 board predicates, 10 category errors, 0 scoped gaps) — and **was not implemented on the first pass** (`_gust_space` hardcoded `if target != "any": refuse`, the per-card branch D2 forbids, dressed as fail-closed). D3's Target Rankers prefilter and the leaf decides; two rankers because the sides are asymmetric — their-side cannot use `role_value` at all, since ADR-0101 measured **59.4%** of the opponent's build pricing 0 through it, *"exactly their attackers and wincons"* (owned by Issue #395). D4: an expanded family holds **ONE** beam slot taken by its best child, so `k` keeps its pre-expansion meaning. D6: the follow-up select REPLANS through the same evaluator — and the honest reason is not that the board moves (for a retreat, provably nothing does) but that Crispin's *"reveal them"* already breaks the commitment today. D7's resolution-parity lane: **377 traces / 37 983 frames / 2254 choice steps — 2200 verified, 0 diverged, 0 unenumerated**, 14 refused (12 untagged Special Energy, 2 Ethan's Magcargo — a fourth free-retreat grant shape nothing parses, now Issue #404), 40 unsettled. Build record adds four facts the grill could not know: the near-zero is exact; the class identity needs TWO collapses because `option_fingerprint` compares attached-Energy lists **including order** (measured at `boomer_9001` f39) and provably cannot do the discard one; the probability denominator is the FULL enumeration; and on the f32/f35 margin criterion **f35 discharges it** (0.001125 → 0.002985, a 2.65× widening) while **f32 cannot at any `k`** — its menu scores one candidate, which is COVERAGE not ordering. Both review corrections are recorded as the self-filed spec's own failure modes: D2 looked implemented, and a narrowed margin criterion looked like an honest negative result. **Citation correction:** Issue #392 and Issue #263 both cite `promote_retreat_value` as ADR-0073; the correct reference is **ADR-0100**. |
| 0122 | The `_TO_HAND` grab equation was BUILT to spec and measured worse; ONE seam-local fix shipped by differencing instead | **The proposed equation is REJECTED as specified** (Issue #406, built and measured 2026-08-06; build preserved at `bd9187d7`, reverted at `8dbde0fe`; Issue #406 closed `not planned`). Renumbered **0121 → 0122** at `3f84a1cc` (collision with open PR #407). ADR-0119's shape — a change justified by what it MEASURED rather than by what it shipped. Issue #406 proposed replacing the 23 `_TO_HAND` rungs with a `needs.keep_v2` add-marginal; built to spec and graded through the real Pilot it scored **17/30** against the incumbent ladder's **23/30**, and at four times its band **14** — below doing nothing at all. None of that was visible from the seam's own unit tests, which all passed. **Decision 0 is the finding that reframes the issue and it generalised past it:** half the ctx-7 validation base is UNGRADEABLE, because *a follow-up select is only gradeable if the decision that opened it was correct* — `retest_span`'s own ADR-0049 doctrine applied WITHIN a turn instead of across one. Applying it inverted the conclusion: the incumbent ladder went from "misses 5 of 30" to **"misses 1 of 15 gradeable"**, one demonstrable defect instead of five, none of which justified the proposed equation. **One targeted fix DID ship** (Decision 5, `_grab_refresh_value` / `_refresh_swing`) on a direct developer ruling discharging that single defect — a flat draw-tag credit losing to a computed one — built through the differencing/value-equation framework rather than by tuning a rung, with the category rung it replaces retired. F1–F7 and the 23-rung retirement remain an **unbuilt register**. **Does not amend ADR-0023** — the shared-oracle invariant is untouched and this ADR is evidence *for* it. Retains `tools/train/grab_sweep.py`, which now also verifies the shipped fix and whose `_off_policy` detector is the one working implementation of Decision 0. Notes the asymmetry that is the reason to run the sweep at all: ctx 5 — the `_TO_BENCH` seam whose Deploy Marginal the equation was modelled on — has exactly **ONE** ruled corpus frame, so a design validated at one frame was carried onto a seam with thirty. Spawned **Issue #412** (the corpus-wide off-policy exposure: 93 of 372 correction frames are non-MAIN follow-ups, none checked against their predecessor). Also records that a tie count is VACUOUS unless counted by distinct card class. |
| 0123 | The HEAL target is SURVIVAL minus the BOUNCE, and "most damaged" is the wrong rule | Accepted (Issue #409, 2026-08-06); BUILT (PR #415). Renumbered **0122 → 0123** on 2026-08-06 — Issue #406's grab ADR merged to `main` first (`78bb5943`) and keeps 0122; this one (`0926eba7`) collided and renumbers past the highest on disk, per the numbering convention above. `SelectContext.HEAL` (17) appeared **nowhere** in the strategy layer: not as a named constant — `strategy/context.py` defined `_DAMAGE = 15` and `_REMOVE_DAMAGE_COUNTER = 16` and jumped straight to `40` — not in any `Hypothesis.when`, not in any `*_tactical`, not in any decision row. Measured on `44734ca5` with a positive control: `grep -rn "_HEAL\b|== 17" src/common` returns **0 matches**, against `_REMOVE_DAMAGE_COUNTER` finding its constant plus two live gates. So the pick was falling out of a JSON string sort and always landing on the Active. The ruling is that the heal target is the **survival gain net of the bounce** — what healing this body buys, minus what it gives back — and that "most damaged" is the wrong rule. **Does NOT amend ADR-0091/ADR-0103** — the canonical-fingerprint tie-break stays exactly as specified; this removes a select from the set where it was deciding *by default*. **Does NOT touch ADR-0025's `baseline_heal` rungs**, a different question (whether to play, not which body). Ships the first ctx-17 rulings — three of four, the fourth ruled out of scope — in `data/corrections/mega_starmie_20260806_parity-ctx17/`, moving the corpus denominator 372 → 375. `/code-review` refuted one claim the spec itself made, which is recorded rather than quietly dropped. |
| 0124 | The `EVOLVES_FROM` target is the DEPLOY DELTA, and the delta IS the argmax | Accepted (Issue #417, 2026-08-06); BUILT. ADR-0123 (heal)'s shape one card-mechanic over. `SelectContext.EVOLVES_FROM` (18) appeared nowhere in the strategy layer, so Salvatore's *"put it onto that Pokémon to evolve it"* target fell out of `_order_key`'s canonical fingerprint. Two shipped deciders look like they cover it and provably cannot: `_evolve_decision` and `_prefer_soonest_arming_evolve` both gate on `type == _EVOLVE` (9, a MAIN-menu ACTION type) and **all 12 options across the corpus's 7 ctx-18 steps carry `OptionType.CARD` (3)**. The trap is that `ctx.card_id` names the WRONG card here — the option resolves to the PRE-evolution and the target rides on `select["contextCard"]["id"]`, so a reader assuming MAIN semantics compares each body against ITSELF and returns a flat zero while looking like it priced them. `_evolve_target_tactical` is the 5th context-gated target term, reusing **ADR-0070**'s `evolve_value` UNCHANGED: no new equation, no new constant, and `income_gain`/`income_loss` left at their defaults because Salvatore's clause carries `no_ability: true` — a CARD fact, not a deck assumption. **The delta is derived to be the argmax**, not inherited from MAIN: board value after picking *i* is `Σ_j deploy(B_j) + [deploy(R_i) − deploy(B_i)]` and the sum is constant. A SIBLING of `_evolve_decision` rather than a gate widening (two tested consumers), but NOT a copy — the body-substituted delta is extracted to `_evolve_substitution` and shared. ctx 19 is MOOT, measured (all 20 steps are `area=DECK` copies of one species) with a tripwire test rather than a closure. Validation BY CONSTRUCTION (zero ruled ctx-18/19 frames): the 7 real boards as fixtures, every negative assertion carrying a positive control, both ADR-0072 gates PASS and the Decision Gate diff **byte-identical** stashed vs unstashed. ⚠️ Two things left OPEN for a developer ruling on `ms_mirror_1001` f15 rather than guessed: the equation is PRIZE-BLIND (ADR-0070 §5), so a 3-prize Mega ex landing in a doomed Active spot is priced only through `p_survive`; and both benched options tie at exactly 0.0 because the hop changes neither `p_survive` nor `turns_to_afford` there. **Part B found the `ATTACH_FROM` (21) half ALREADY BUILT** — `_attach_value`'s `is_from` branch matches the human on both gradeable ctx-21 Cinderace frames, so it ships as regression coverage and no new code — though the issue's own "2 of 3 pass" framing OVERSTATES its evidence and the ADR corrects it: only `83116081-21` is an unambiguous pass (the sole frame where the human overrode the agent). The third frame, `82224509-31`, carried a RATIONALE-vs-FIELDS conflict, now RULED: its rationale named the empty Staryu while `correct` recorded the full Mega ex, and the developer amended `correct` `[0] -> [1]`, so all three frames are gradeable and covered. ⚠️ NOT a schema violation — `correct == chosen` on a MANDATORY select is deliberately supported here (13 committed records rely on it; `test_unstatable_decline_records.py` rules it means *the pick was right*), so ADR-0015's "differ from `chosen`" clause settled nothing on its own; it only sharpened the conflict, and exactly ONE record had it. The reusable finding is that **no instrument could see it** — both gates passed over it for six weeks, because a gate grades fields against fields and never reads the prose. Also repaired here, incidentally: the ADR-0122 collision that left `test_adr_index.py` RED on `main`, and two index rows (0121, 0122) that PR #415's merge dropped along with the next-free pointer. |
| 0125 | The Energy PROVISION is ONE seam, and the EXPIRY ships with it or not at all | Accepted (Issue #418, 2026-08-06); BUILT. Discharges the paragraph ADR-0069 §5 left open and `CombatMath.without_expiring_energy`'s docstring NAMED and declined (Issue #286) — *"It is NOT the codebase's only reading of that quantity… folding those three into it would move `attach_value`, which is not this issue's to move"* — and the count was **four**, not three. `CombatMath.provision_codes(card_id, holder_stat)` becomes the ONE composition of `CardStat.energyType` with `CardFunctions.energy_provision`; `board_delta._provided_units` is MOVED there rather than rewritten, `pilot._attach_provision` is DELETED (it took `(target_stat, burst)` and could not express the colour — half the fact), and the four hardcoded copies in `pilot`/`planner` all read the seam. The hardcode was right only by a coincidence: Ignition Energy (17) is the ONLY card in the pool carrying either `discard_eot` or `provides_evo`, and it carries both, so two synthetic cards are what separate the predicates. **`extra_energy_ids` is renamed `extra_unit_codes` and re-typed** — it appended CARD ids onto `energies`, a UNIT-code list, so card 17 read WILD and Jetting Blow's {W} came back payable for 120.0 where the engine's `[0]` gives 0.0 (Issue #297's split, one door over). `_evolve_substitution` re-derives the evolved body's `energies` through `CombatMath.restage_energy`, and **RULED 2026-08-06** `_evolve_side` excludes expiring Energy on **BOTH** sides: measured on `ms_mirror_1001` f15 bench 0, D3 alone reads +157.50 (*armed now* off a card discarded at end of turn) and the asymmetric result-only fix reads −26.25 (evolving priced as worse than not evolving), against 0.00 — a clean tie with the Ignition-less bench beside it — for the ruled build; the asymmetric shape is pinned as a NEGATIVE test. Both ADR-0072 gates PASS with **0 picks moved** over 375/268 frames, isolated against a clean-`e8141b8` capture and controlled by a sham that moves 3 and 4 picks and turns both red. No card data moved (ADR-0032 tags consumed unchanged); extends ADR-0087, applies ADR-0067 at two contracts. Lifts ADR-0124's freeze on the FIRST of its two recorded `_evolve_substitution` limits only — `payoff_damage`'s pre-credit stands. |
| 0126 | A follow-up frame is OFF-POLICY only when its predecessor actually CHANGES it; the same-turn rule was over-broad by more than 2x | Accepted (Issue #412, 2026-08-06); BUILT. **Amends ADR-0122 Decision 0** — that finding stands, its instrument does not. A follow-up select is only gradeable if the decision that opened it was correct (ADR-0049's `retest_span` doctrine applied WITHIN a turn), and Issue #412 called its detector *"sound but incomplete"*. Verified corpus-wide it is incomplete AND **over-broad**, which is the expensive half because it deletes good evidence: the same-turn scan flags **34** frames, only **15** survive. `mega_starmie 81785223-39` is the refutation — its predecessor rules *"play Pokegear 3.0 earlier in the turn"*, and Pokegear (id 1122, top 7 of YOUR deck for a Supporter) cannot touch the opponent's Bench the snipe ruling names. Two further defects were pure bugs: **15 endorsement records** (`chosen == correct`, e.g. `85058574-109`'s explicit *"match planer note"*) were read as blunders, and **`episode_id is None` collided across sources** so f82 of parity trace `v2_ms_mirror_5000` scanned as a predecessor of f100 of `_5001`. The fix SPLITS what the helper conflated — `candidates()` is a mechanical scan whose output is a QUESTION, `RULINGS` is the human's verdict with the reason and the card id that settles it, and an unruled candidate is `UNRULED` (reported, never filtered). `classify()` never returns OFF_POLICY on its own reasoning. All 34 candidates ruled (14 OFF + 20 GRADEABLE, plus ADR-0122's scan-invisible `84889011-7`); two needed a developer ruling because the record could not settle them (`82752604-62` — Jetting Blow keeps the bench-snipe select, Nebula Beam would remove it; `85164605-48` — Salvatore evolves onto a body, Mega Signal only reaches the hand). Every non-MAIN context censused for the first time (`tools/train/off_policy_census.py`): **96 non-MAIN of 375**, 15 off-policy. **Gates FLAG AND WARN, never drop** — excluding would shrink the gated set without a ruling, the same class of act as the auto-recapture both gates already refuse (ADR-0094), and the identical argument Issue #251 settled for the Unstatable Decline; asserted structurally by a test, since the tempting edit is one line inside either verdict. ⚠️ **The exposure is LIVE, not historical:** ADR-0124 (merged the same day) claims *"all three ctx-21 frames are now gradeable and covered"* and one of them, `82224509-31`, is OFF-POLICY — mechanically, in the option encoding: f29's ruled-correct EVOLVE targets `inPlayArea=5/inPlayIndex=1` and f31's ruled-correct attach is `area=5, index=1`, the SAME bench slot. ADR-0124's decision stands (its unambiguous pass, `83116081-21`, is gradeable) — only its count is wrong: two frames, not three. Nothing was wrong with its reasoning; no instrument existed to say the denominator was. Renumbered **0125 → 0126** — Issue #418's provision ADR merged first (`278063cc`) and keeps 0125; this one renumbers past the highest on disk. |
| 0127 | The `hand` family is a LEDGER — supply without demand inverts every card play | Accepted (Issue #400 Phase 2, 2026-08-06); BUILT. `state_value.hand` priced only what my hand COVERS, so under differencing spending a card moved supply down and demand not at all and every play read as a loss — the ruled Energy attach priced NEGATIVE on 31 of 31 corpus frames. Gains a fourth leg `slot_demand` off the SAME `needs.Resolution`: `(assignment_coverage + re_access + hand_worth − slot_demand) × POC_WORTH_PRIZE_RATE`. No new constant, no new currency — one Energy retires two `fund_attack` slots (16 Worth of demand) for 8 of supply. The `max(0.0, ...)` floor is DELETED (a cost clipped at zero makes the need free). Half two: `needs` was pinned to the ROOT observation via a closure `rebuilt` forwarded verbatim, so the family contributed exactly 0 to every 1-ply delta; the supplier is now BOARD-BOUND `(obs, my_index)` and `build` binds it. Agreement 82 -> 86 of 270. Residual (CORRECTED in place — the first reading was a guess): 5 of 6 survivors retire exactly 0.00 because `fund_attack` slots are ACTIVE-scoped, 189 of 189 corpus-wide; a bench slot was built, measured a WASH, NOT shipped, and is an acceptance case on Issue #386. |
| 0128 | The selection tie-break needs a FLOAT-NOISE FLOOR — one ULP was deciding corpus frames | Accepted (Issue #400 Phase 2 flip review, 2026-08-06); BUILT. Issue #263 rules the composer's ordering *"must NEVER fall through to raw option/sequence generation order"* — and `selection_key` compared its score leg EXACTLY. Two orderings of one commutative block reach the same end board, so their scores are one sum added in two orders, and float addition is not associative (`state_value._terms`' own docstring says so, and pins its iteration order for exactly this reason — it simply stopped at the leaf boundary). On `82226116|0|decision|70` the two differed by 1.11e-16, the score leg separated on that bit, and the Worth leg — which knows the answer, 30.0 for a Mega Starmie ex against 0.0 for a bare retreat — never ran. Now rounded at `_SCORE_PLACES = 12`: six orders of magnitude above the noise and six below anything real (observed 1-ply deltas 1e-5 to 1e-3), asserted in BOTH directions. `EPSILON` is deliberately not reused — that is the search admission band, this is a selection noise floor. Agreement 88 -> 90. Any future ordering key led by a computed float owes the same floor. |
| 0129 | A truncated sequence carries the turn's REMAINING terminal EV, not zero | Accepted (Issue #400 Phase 1, 2026-08-06); BUILT. `score = state_value(end board) + EV(terminal action)` defined the second summand only for the two ways a turn ENDS, so a line the composer CUT at a reveal replan point carried 0 and lost to every full attack line by the size of its prize. A reveal candidate now carries `composer.continuation_ev` — the best attack still reachable from the pre-reveal board, the SAME `attack_ev_legs` / `attack_ev` pair `terminal_ev` already composes, read at a second seam; no new math, no constant. `_stop_here` is excluded because `attack_ev_legs` answers from the BOARD and the root stop-here has `first_index is None`, so crediting it let *"commit nothing"* win outright on 4 of 270 frames; a REFUSAL is excluded and the wider arm measured byte-identical, since `selection_key` leads with `coverage_gap`. Measured on top of ADR-0127/0128: agreement 90 -> 92 of 270, `_PLAY` picks 9 -> 15, 2 fixed / 0 broken, `leaf_evals` identical. ONE residual, named not smoothed: a class fetching 1142 Fighting Gong makes Riolu's Accelerating Stab affordable, so the pre-reveal read UNDER-credits it — the conservative direction. The `_PLAY` gap left is COVERAGE, not valuation. |
| 0130 | A reveal is resolved by the ZONE it reads, and a cost is charged from a seam the caller supplies | Accepted (Issue #394, 2026-08-06); BUILT. Widens `board_expectation` (Issue #383) and `board_choice` (Issue #392) rather than rebuilding either; apply-seam coverage **81 -> 244 of 663 (12.2% -> 36.8%)**, re-measured per commit and never projected. **The largest single item is a ROUTING defect, not a missing capability:** `board_expectation` refuses a discard search saying *"that zone is visible -- so it is a pure CHOICE node, not an expectation"*, and the node it names shipped at Issue #392 with no `fetch` entry, so **46 steps** sat in a bucket whose own sentence said where they belonged. The split is by ZONE, not clause kind -- hidden deck -> a distribution from `deck_odds`; face-up discard -> a pure choice with **no `deck_odds` call at all** -- so `fetch` is deliberately NOT a `CHOICE_CLAUSES` member and `FETCH_DISCARD` is a third declared family under one `CHOICE_KEYS` closure. **Two cards had their relation backwards:** 1097 Night Stretcher and 1142 Fighting Gong both print *"a X **or** a Y"* and were declared as CONJUNCTIONS, i.e. as delivering two -- 69 of the 98 steps in that bucket. `choice` + `amount` already discriminate (union / exclusive either-or / conjunction), verified 1:1 against three structurally different engine ops, and the relation reader now lives in `fetch_closure` so both nodes read ONE. The audit that would have caught it did not exist and now does -- a cross-STORE check against `chain_overrides.json` with the no-override skips named, since every audit reading only the compendium agreed with itself. **Three leg failures are kept distinct:** empty-on-this-board SKIPS (the engine's own *"empty buckets skip with a tac bump"*; Dawn's product is 0 on all 8 of its steps), reach-gated on a MULTI-leg card REFUSES (Secret Box would otherwise model three quarters of a card), and reach-gated on a SINGLE-leg card does NOT -- a first draft refused both and moved eight real cards off refusal reasons that describe them. **The cost's discard set is PASSED IN**, because measurement says it cannot be computed here: `_resolve_needs` reads 16 `self.X` including declared Roles, production passes no `needs=`, the planner's is the LEAF resolution pinned to the ROOT obs, and `composer.py` has no `Pilot` anywhere. So `shed` is the fourth caller-supplied seam beside `search_api` / `deterministic` / `clauses_cover`; unpaid pricing, `model.mine.needs` and re-deriving are each rejected with reasons, as is `keep_v2` (ADR-0122 measured it worse). Cost applied BEFORE the search -- the engine's order -- so a delivered card cannot pay for its own search; an unpayable cost refuses as an ILLEGAL play, never a free one. The count becomes DATA (`COST_CARDS`), replacing a hardcoded `2` right only for Ultra Ball; `bottom_2` refuses on CORRECTNESS (it returns cards to the DECK, moving `unseen_counts`) and `discard_hand` on SCOPE. **A multi-card delivery is a MULTISET** -- Cyrano's pool is one distinct id on all four of its steps, exactly where a subset enumerator is wrong -- needing `deck_odds.p_contains_at_least`, whose summand is `planner._prize_split_hit`'s existing PMF; `p_contains` DELEGATES at k=1 and the identity is brute-force pinned. **Gate order is part of the answer:** clause gates now precede the card-type floor, landing 11 on a new ABILITY gate (Lunatone / Fezandipiti ex -- modelling those is WRONG, not under-scoped), 12 on the reach predicate, and **0** on the floor. **NO rung, Hypothesis, weight or tuned constant** -- the one ordering input added is ordering-only and asserted to price nothing. **`BRANCH_CAP` now BINDS** (1 step truncates) where the header recorded zero. Every module is dark except one refactor, so `score_diff` and both ADR-0072 gates are NULL CONTROLS and say so; the live commit measured **0 divergent over 375 frames**, a meaningful null since `cost_discard`'s only carrier is in all five decks. Against the population that matters, this retires **13 of Issue #400's 76** ruled-frame refusals -- recorded so the corpus-step table is not read as an agreement forecast. Repaired in passing and flagged rather than folded in: the census had not run since PR #407 renamed `apply_parity._load`/`_chosen_option` and lacked the `sys.path` bootstrap its own documented command needs; `target_space` carried a latent `NameError`; `docs/plans/apply-seam-coverage.md` was stale since PR #436. |
| 0131 | A decider must not decide what its own numbers say it has no view on | Accepted (Issue #386, 2026-08-07); BUILT. POC-T4/5 arms `common/composer.py` as the MAIN decider and deletes the rung ladder — and walks straight into the boundary `sound_rules` had already named: ADR-0095 decision 3 rules the information-before-commitment ordering *"NOT derivable by the planner — both orders reach the same end state, so no function of that state separates them."* **A composer is a function of the end state**, so a whole class of decisions is definitionally out of its reach, and the swap broke it TWICE over. (1) `_finish_turn_last`'s `score <= 0` gate dropped the dig once `dig-before-commit` was deleted — on ADR-0095's own anchor frame the Pokégear prices **0.00** and falls to `_TIER_ENDER` while the COMMITTING Crushing Hammer (22.50) sequences first. The boundary was intact the whole time; its INPUT had been removed, which no failure pointed at. Fixed by a four-fenced carve-out, and the fourth fence is the finding: a score of zero means either *nothing prices this* (`fired` empty) or *a rung NEUTRALISED it* (Mega Signal under `dont-tutor-the-held-wincon`), and the old gate was RIGHT about the second — without it the carve-out sequenced a search the ladder had just refused FIRST. (2) The composer overruled the sequencer anyway, on a **7-way exact 0.0 tie**. So `_composer_line` gains a FOURTH defer: when another option's best sequence ties the chosen one it ABSTAINS, compared at `_SCORE_PLACES` — the same float-noise floor `selection_key` uses (ADR-0128), deliberately not `EPSILON`, so a decision cannot be a tie to one mechanism and not the other. Measured because a defer that fires everywhere hollows out the swap: the composer still decides **63.4%** of MAIN corpus frames (104/164), unruled Decision-Gate regressions go **58 → 44**, and NINE xfails retire — four hyperclosure PINS, three flip-table rows, the `ko-score-band` veto gap, and the anchor itself. One of the nine was a REFUSAL row that resolved with **no widening of the seam at all**: the refused option was never the problem, the composer committing a line it had no view on was. Two rows still disagree and the disagreement is now the LADDER's, stated rather than smoothed; near-ties above the floor (0.0003 prizes) are deliberately NOT caught, since widening to a band is a ruling nobody has made. Consequence: **`_finish_turn_last` is not retired by POC-T4/5** and should stop being described as if it were — it owns the sequencing claims no state function can express. |
| 0132 | A merge that is coherent as a revert goes green, and a retired id in a live string literal reads as live | Accepted (2026-08-07); BUILT. PR #447 silently REVERTED PR #432 on `main`: it branched from the merge before #432, and its first commit `c68ad9e4` carries #432's merge as its PARENT and a tree that predates it — all eighteen files back in one commit, nine byte-for-byte pre-#432, all five added files deleted (1240 lines of Stadium tests), and the three Mega Lucario rungs #432 had MEASURED into retirement alive again with their ~30-line evidence record gone. **Nothing went red**, because a revert is internally consistent by construction — it restores the tests that agreed with it. Blast radius proved to be exactly one PR (the nine preceding merges check `reverted=0 of N` against their own first parents); the A/B-restore explanation for the stale tree is marked INFERENCE. Restored #432 ON TOP of #447 rather than reverting: 14 files clean, 4 three-way, one conflict where both PRs appended to `test_attach_decider.py` (kept both). `pilot.py` takes BOTH changes — they are disjoint, and the merge is what makes that visible. Restoring then exposed a SECOND, independent defect: `_finish_turn_last` sequenced a KO-enabling gust and the wall retreat by matching a rung ID in an INLINE STRING LITERAL, POC-T4/5 deleted all three rungs, and both branches had been unreachable ever since — while `baseline/__init__.py` asserted *"`_finish_turn_last`'s tiers SURVIVE"* in the very fold record that deleted their triggers. Deleted with the loss recorded (the gust hole is TOTAL — `_covers` refuses the transition too, so rung, tier and leaf are all silent; the retreat is a narrowing the composer covers on 63.4% of frames). The invariant: `tests/strategy/test_rung_id_literals_are_live.py` AST-walks `src/` for every literal compared against a Hypothesis `id` and holds it to the shipped 70-rung roster — an AST walk and not a grep, so a fold record naming a deleted rung in prose stays green. It found 14 more: `_CLASS_B_SPEND_IDS` named THIRTEEN unshipped rungs and `_ABILITY_FIRE_IDS` one, so ADR-0069's spend account summed over a vocabulary two thirds of which could not occur. It then paid for itself twice inside its own commit — catching a LIVE rung pruned alongside the dead ones (*"no dead members"* is satisfied perfectly by an empty set, so it gained the symmetric half), and exposing `test_line_account_ignores_wrong_sign` as VACUOUS (its spend leg named an id in neither the set nor the roster, so the zero came from membership, not from the sign filter it claimed to test). |
| 0133 | A dig is a WINDOW over the unseen pool, and its whiff is a class | Accepted (Issue #440, 2026-08-08); BUILT. `board_expectation` refused every `dig` through `fetch_is_unconditional`, costing **30 Pokégear 3.0 options across 27 of the 279 MAIN corpus frames** — options the composer records as a gap and never ranks. Three facts made the obvious change the wrong one. (1) Before Issue #454, `Expectation.best()` was a MAX *"because both producers emit CHOICE nodes"* — a whiff is DEALT, so adding the class without the resolution split would have priced Pokégear at *"I always find my best Supporter"* with nothing raising and no test failing. (2) Un-gating `dig` ALONE is worse than the refusal: `supporter` is `FETCH_DEADNESS_ONLY_TARGETS`, so the pool comes back **empty** (verified against the whole decklist) and Issue #456's 1.0-probability `_whiff()` would price all 30 as a card spent for nothing. (3) The predicate binds `energy_type` to Energy targets, so Bug Catching Set's *"{G} Pokémon"* leg reaches **12 ids where 10 are legal** over hydrapple — and ADR-0073 §3's reason for allowing that (*"unresolvable from `CardStat`"*) is STALE, since `CardStat.energyType` is populated for Pokémon. **`fetch_is_unconditional` is NOT split** — the issue's own framing, rejected: the enumerator simply stops asking an endorser's question, so all four endorser readers keep byte-identical behaviour. Instead a THIRD reading, `reading=REACH | DEADNESS | WINDOW` (the `deadness: bool` migrated, not aliased), each safe only under its consumer's quantifier. **The prize split disappears into the pool**: deck-then-window composes to one uniform subset of the `D + H` unseen, so ranging the hypergeometric over `deck_count + prizes_hidden` handles it EXACTLY with no mixture — checked against that mixture at eight real corpus shapes, ≤ 1.1e-16. Classes are one closed form whose `M = ∅` member IS the whiff, telescoping to exactly 1.0 off `deck_odds._none_of` promoted to public `window_miss_probability`; **the ranking is the policy priced**, at ADR-0128's floor (moved to `apply_option.SCORE_PLACES` so two orderings cannot drift). `resolution=DEALT`, and the whiff is **PINNED past `BRANCH_CAP`** — a greedy ranking makes truncation drop the WORST classes, biasing `expected()` up — but never takes the ONLY slot, where dropping it and reporting `truncated` is the smaller lie. `expectation()` takes `score=` and REFUSES without it (`_cost_indices`' `shed` discipline). Result: **all 30 options enumerate**, 5–7 classes, zero truncated, mass 1.0, whiff median **0.0535** (0.0074–0.1150); the census over all 377 traces goes **368 → 394 of 663** enumerated (55.5% → 59.4%), and the old 61-step `not the unconditional` bucket is fully accounted — 22 Pokégear + 4 Bug Catching Set enumerate (**+26**, exactly the delta) and the other 35 move to a bucket naming their OWN field. **ADR-0131's anchor frame changed and its ruling did not**: the ruled dig moves from unpriced sentinel to **−0.0093** — ADR-0095 speaking, since a 1-for-1 Item→Supporter swap barely moves the end board — the six-way 0.0 tie still holds, the defer still fires, and the sequencer still plays it; the test asserting it stays UNPRICED is re-ruled to assert it prices BELOW the tie. Bug Catching Set ships DARK offline (hydrapple has **no** corrections) by the owner's explicit call, on unit tests plus a brute-force pin over every `W`-subset. **One card outside the named scope changed, and it is a correctness FIX**: 1142 Fighting Gong (`mega_lucario`) already enumerated and its Pokémon leg carries `energy_type: 6`, so the narrowing bites — printed text is *"a Basic {F} Energy card OR a Basic {F} Pokémon"* and 1071 Meowth ex is **{C}**, so it had been enumerating a class the card cannot deliver. Over its 27 trace-corpus steps no class delivers Meowth ex any more, which is why the class-count distribution moves three steps DOWN. The rest of the family still refuses, each naming its OWN field — a test, not a claim. **Four defects found at review** (self-filed spec, both premises re-verified at source and both TRUE): the bracket promotion was a LIE in the first build (`window_classes` computed its own whiff, so the public function had no dig caller) — now SEEDED from it; a refusal that named all three undecidable fields made its own regression test VACUOUS on two rows (Hassel, Hop's Bag passed on the explanation, not the diagnosis); an unknown `reading` string was a FOURTH wider reading, the ADR-0073 unsoundness class by accident; and the promotion left `draw_hit_probability` a duplicate, now delegating and pinned byte-identical over a 12k grid. |
| 0134 | `latent_worth` is read on BOTH boards of a play, so discounting it cut the SPEND CHARGE, not the fetch credit | **Rejected as specified** (Issue #444, built and measured 2026-08-09; build preserved at `74115fa0`, reverted at `d6add0d7`). ADR-0122's shape, without its salvage. Issue #444's premise reproduced at HEAD (three `latent_worth` sites, no demand counterpart; positive control: `slot_demand` returns 7 incl. a live registry entry) — its ANSWER did not. `latent_worth` is read on both boards a play is differenced across: a card leaving hand gives up its worth (the CHARGE), a card arriving adds its own (the CREDIT). The two do NOT move together — measured, the discounts cut `82752045-18`'s root latent **40.5 -> 31.5** while `85163634-17`'s stayed **0.0 -> 0.0** with a bit-identical composer score, so **card plays got cheaper and the fetch stayed exactly as attractive**. Measured: `composer_lab` `composer == ruled` **99/270 -> 94/270** (re-taken on `afe81905` after PR #473 rebased under it; the four Issue #444 frames are bit-identical); ADR-0072 Decision Gate **52 -> 50 unruled** (the gated metric; 82 -> 81 counts held-out and voided rows too, and the gate is already red by 52 before the change). Flips RULED, not counted: three of the seven on-policy losses are the composer declining a ruled ATTACK to play a card, `82752045-115` among them (*"Attacking will win the game"*). Decisive: the build reaches **NEITHER** acceptance frame — `85163634-17`'s fetched Staryu has `deploy 1.0`, `liquidity 1.0` and no duplicate, so every discount is a no-op on the frame the issue is named for. Keyed on `card_effects.json` clause kinds over 64 search/draw cards, the developer rules **FOR** a search on **55** frames and **AGAINST** on 59 (46 on-policy) — so neither a flat penalty nor a flat credit can be right, and the discriminator is a demand read `latent_worth` cannot host. Recorded, not shipped: the leaf pays full latent worth for `deploy 0.0` cards (`82522698|62`, +9.9 Worth against ADR-0104), inseparable because zeroing it is a pure charge-side cut. **Its own `/code-review` Spec axis caught three false numbers in the first draft**, all from quoting an instrument without checking which population it counted; they are listed in the ADR. Also: the Decision Gate is RED on `main`. |
| 0135 | A retreat Tool is priced by the MOBILITY it buys, and the composer structurally cannot price it | Accepted (Issue #423, 2026-08-09); BUILT. `_attach_value` refused every Pokemon Tool at its guard and the composer prices one at EXACTLY 0.0 — 25 applications over the 7 Air Balloon corpus frames, every one `+0.000000` with an empty leg set, `hand` included; positive controls in the same runs move `readiness` +0.004482, `development` +0.104167 and `survival` +0.755063. The only value-consuming reader of `tool_ids` in `src/` is `state_model.damage_boosts`. **The composer cannot be the home:** `board_delta._retreat` writes ONLY `retreated=True` — no swap, no discard — so a retreat in a composed sequence has no consequence to score and no `state_value` term could reach it (Issue #385's beam admission). The channel differences ADR-0100 §9's whether-site as a counterfactual: `mobility(A) = 0` when the retreat is unaffordable else `max(0, best_B promote_value(B, A))`, clamped into ADR-0069 §1's Retreat Equity band. 0.0 off the Active by ARITHMETIC — both legs read `_my_active`, so no area gate exists to get wrong. The issue's ORIGINAL equation (differencing `RetreatSide.retreat_cost()`) is REFUTED: that leg prices the Energy a retreat DISCARDS, so it returns 0 exactly when the Tool makes an illegal retreat legal and +270 when it does not — right on 1 of 7, wrong by +270 on 2. The clamp is **half principled, half fitted and recorded as such**: 4/6 ruled frames vs 3/6 uncapped with 0 regressions against a baseline of 1/6, at the cost of going FLAT above the band. Two variants built and rejected on measurement, incl. a flat per-recipient credit that moves the pick ONTO the Balloon on the two frames whose category is literally `wasted_resource`. Re-points two false `FOLDED` rows (`deploy-hp-tool` -> UNREPLACED; the +HP class moves `survival` on 1 of 6 sampled Cape frames). Decision Gate 199/343 -> 202/343 with the gated number 49 before and after; Discrimination Gate byte-identical; neither gate green, neither was. Its own review caught a VACUOUS tautological test guarding the design's core property, an acceptance criterion with no test, and a stale table in the self-filed spec. |
| 0136 | Absolute state potentials own consequences and difference once | Accepted; BUILT 2026-08-10 (Issue #494). Keeps the six state families and terminal `attack_ev`, preserves scalar arithmetic and `("state_value",)` memoization, and adds immutable public breakdown/difference contracts. Ownership is by globally unique economic consequence; shared raw inputs require distinct reasoned consequences. Diagnostics carry status, bounds, deterministic JSON, and registry content identity. Runtime Composer selection remains unchanged until #495. |
| 0137 | Combat realization aligns each attack and Energy owner | Accepted; BUILT 2026-08-10 (Issue #495). One multi-attack profile aligns payoff, condition, typed cost, persistent build, legal-now reach, and future clock. Held Energy is a weighted `NeedGraph`; acceleration uses exhaustive shared allocation. Attach, retreat, evolution, promote, dig, and deploy consume the same owner. |
| 0138 | Exact frontier identity precedes continuation-aware admission | Accepted for production activation by developer request; measured 2026-08-10 (Issue #496). Corpus P95 4.559 s exceeded both the committed ceiling and grader floor, and continuation showed no completed-reference benefit; the accepted alpha-normalized Mega identity result is 6→3 because duplicate Wally copies are interchangeable. |
| 0139 | Mega Starmie uses one Bellman-style full-turn planner | Accepted and implemented (Issue #507, 2026-08-11). Post-Set-Up Mega Starmie uses one recursive benefit-minus-cost objective with exact-zero End, full action/nested transition generation, chance and opponent-response nodes, complete attack resolution, and no legacy strategic fallback. The unfiltered 259-row corpus closes with zero excluded/unexplained rows and 62 explicitly named tuning errors. |
| 0140 | Free-peek dominance orders the search walk | Accepted for production activation by developer request (2026-08-16, PR #534), knowingly overriding part of the ruling corpus; the corpus is split on peek-first vs commit-first and the armed gate resolves the two disputed budget-flip frames while contradicting four prior rulings. Row added here at the ADR-0141 collision repair — PR #534 shipped the file without one. |
| 0141 | The Bellman potential prices the opponent's hand, special conditions, and the stadium zone | Accepted; built from the 2026-08-16 src/ crash-and-valuation audit. opponent_hand family + refresh_opponent_hand row ARMED by the 2026-08-16 amendment at `value.opponent_hand_share` 0.1, scaled by root-pinned board parity after both overturned frames were re-ruled and upheld; special-condition forecast shares and pending poison/burn ticks plus own-stadium board Worth are live. |
| 0142 | Bound the Lethal Solver by a wall-clock ceiling no layer can lift, and leave its node cap alone | Accepted. `terminal.max_seconds` 1.2 with `maximum` equal to the default, and the 60s pinned-clock override removed from runtime.py and strategy_lab.py; `terminal.max_nodes` stays 1024 because lowering it to 64 saves 0.38s per decision and turns 53% of refutations into budget-limited guesses. |
| [0143](0143-unified-pokemon-card-store.md) | One frozen record per card printing under `common/cards/` | Accepted; built for every card in the three shipped decks' lists. PokemonCard/TrainerCard/EnergyCard modules (one per printing, generated by `tools/build_pokemon_cards.py`) with effects as open-parameter Clause legs; `card_store()` serves the union and the legacy stores stay live for off-deck cards until consumers repoint. |
| [0144](0144-boardstate-reusable-typed-observable-board.md) | BoardState: the observable board as reusable typed pieces | Superseded by ADR-0154; the package and type are retired. |
| [0145](0145-ledger-decider-replaces-bellman-live.md) | The Ledger decides live; Bellman becomes the offline teacher | Accepted (2026-08-20); BUILT. `src/common/ledger/` — a 1-ply worth-differencing decider over ObservationState and EvaluationModel replaces Bellman on the live decide() path. Plan: `docs/plans/PokemonAI_Ledger_Plan.md`. |
| [0146](0146-preview-seam-skips-decisionstate.md) | The preview seam: Ledger successors skip the DecisionState build | Accepted (2026-08-20); BUILT. `_bind`/`_key` hooks on both transition providers; `common/ledger/seam.py` PreviewState + identity-token keys. One transition 0.53–0.80→0.24–0.27 ms; decisions p50 13→9 ms, max 355→190 ms, zero choice changes on 80 frames. |
| [0147](0147-honest-corpus-grading.md) | Honest corpus grading: the own-prize anchor and retired rulings | Accepted (2026-08-20); BUILT. `stamp_own_prizes` gives archived frames the live shell's prize anchor (469/471 lacked it; every previewed option had paid a ~0.15–0.21 phantom deck loss End turn never paid), and the sweep partitions `reviewed.json` dispositions into a Retired section. Floor 16.7%→32.6% with zero live-brain changes; this dashboard is the tuning baseline. |
| [0148](0148-scouting-priced-into-the-ledger.md) | Scouting priced into the Ledger: opponent roles, Brief overrides, turn ordering | Accepted (2026-08-20); BUILT. EvaluationModel's `OpponentEvaluation` interprets evidence-level OpponentBelief; Brief claims + `ledger_overrides` are γ-blended, fail-open. |
| [0149](0149-bellman-quarantined-under-deprecated.md) | The Bellman era moves to `deprecated/`; live source is Ledger-only | Accepted (2026-08-20); BUILT. `deprecated/bellman/` importable quarantine — src/ and tools/submit/ never import it (import-hygiene gate); `BellmanTeacherRuntime(AgentRuntime)` replaces `brain="bellman"`; search-only provider hooks move to `BellmanNativeProvider`/`BellmanCgpyProvider`; bundle ships Ledger-only (manifest schema 7); the moved suites run only on demand (`pytest deprecated/tests`), never in CI. Crash fallback = last-resort (`last-resort-fallback`). |
| [0150](0150-concentration-and-rental-energy.md) | Energy concentration and rental energy: the spread-vs-concentrate ruling priced | Accepted (2026-08-20); BUILT. Owner's ruling: concentrate to the big attack by default, diversify only as a threat-read tempo exception (planner scope). `concentration` weight × squared progress toward `top_attack_cost` (default 0.0 = off); `discard_eot` energy on a benched body prices zero. Both re-ruled twin-attach frames agree at defaults; starmie 148→152, zero regressions. |
| [0151](0151-evolution-underpricing.md) | Evolution underpricing: line continuity under the body, size as a dark lever | Accepted (2026-08-20); BUILT. Every evolve priced negative (hand pays full, board credits role parity). `zone_under_body` 0.10→0.65 adopted — evolving is a zone transfer, not a spend: floor 40.9%→44.2%, starmie 50.0%, zero regressions. New `hp_value` (size-as-worth, chip damage real) ships DARK. Four maneuver rulings owner-dispositioned deferred-multi-turn. |
| [0152](0152-brief-deck-store-and-doomed-active.md) | Brief-deck store records and the doomed-active read | Accepted (2026-08-20); BUILT. Store 50→180 (every Brief-named printing; roles derive from the Briefs; 96 attacks + 47 ability/trainer cards clause-encoded with _covers verdicts). Agreement net flat — the ADR-0148 layer already carried the worth; value = gap census 275→208 + their-side facts + the new dark `doomed_active_discount` (floor 48.1% at 0.4, 11 flips). One tie-accident frame deferred by the owner. |
| [0153](0153-card-store-absorbs-the-json-tables.md) | The card store absorbs the JSON tables; a card is one record module | Accepted (2026-08-21); BUILT. Owner's directive: one class definition per card, no long JSON. Store 197→~493 (CORE strict / TAIL honest-partial); `Attack` carries typed engine corrections; `covers` verdicts on records; `CardFunctions` + `load_attack_overrides` = store views (equivalence proven pre-deletion: 0 tag / 0 verdict mismatches, 117/117 patches identical, teacher pins exact). Authoring inputs live under `tools/meta_tracker/`; teacher's table frozen in `deprecated/`. |
| [0154](0154-observationstate-is-the-legal-view-boundary.md) | ObservationState is the legal-view boundary | Accepted (2026-08-21); BUILT for Issue #556. Immutable typed state, explicit LegalKnowledge/ProviderState/EvaluationModel boundaries, Position and Decision keys, hidden-safe Observation Records, typed events and deltas, and raw-access CI gates. |
